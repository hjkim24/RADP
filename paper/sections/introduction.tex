\section{Introduction}\label{sec:intro}

% Outline — ~1 page, 4 paragraphs:

% Paragraph 1: edge LLM inference is becoming real (privacy,
% latency, cost). Petals / Llama.cpp / EdgeShard show it works
% at all. But edge clusters break two data-center assumptions:
% (a) homogeneity — Jetson Nano vs AGX Orin differ by 16x in
% measured per-layer compute (\S\ref{sec:eval:setup}); (b)
% reliability — boards crash, networks partition, disks fill.

% Paragraph 2: existing systems treat placement and recovery
% as separate problems. EdgeShard's DP optimizes placement
% assuming layer redundancy exists. Petals' greedy placement
% leaks 2.38x throughput when devices differ. Jupiter's
% throughput-optimal DP has no recovery story. The systemic
% point: $R$ and $\psi$ have *coupled* feasibility regions on
% memory-tight edge — decoupling them gives up feasibility or
% performance, never both.

% Paragraph 3: RADP unifies them. One alternating DP picks
% $\psi$ and $R$ together; backup-memory reservation folds into
% placement feasibility. Two runtime mechanisms make the joint
% optimization viable on a real fleet: an async mirror cache for
% fault recovery on chain topologies (\S\ref{sec:design:mirror}),
% and async chain forwarding to unblock pipeline parallelism the
% synchronous nested-response unwind silently serializes
% (\S\ref{sec:design:async}).

% Paragraph 4: contributions, condensed.
% \begin{itemize}
%   \item A formal $\psi$+$R$ alternating DP with polynomial
%         convergence, generalising EdgeShard (latency, $\alpha=0$)
%         and Jupiter (throughput, $\alpha=1$) under one tunable
%         cost function (\S\ref{sec:design:dp}).
%   \item Asynchronous mirror cache + chain-aware recovery
%         with gRPC-trailer attribution
%         (\S\ref{sec:design:mirror}).
%   \item Asynchronous chain forwarding with per-request
%         locking and a \texttt{ResultReady} reverse channel,
%         yielding +17--47\% throughput at $C{=}16$
%         (\S\ref{sec:design:async}).
%   \item Live measurement on an 8-worker Jetson fleet across
%         28 operating points: latency placement strictly
%         dominates throughput placement at every point —
%         structural to $\psi$+$R$, not architectural
%         (\S\ref{sec:eval:matrix}).
% \end{itemize}

\todo{Write 4 paragraphs from outline, ending in the contribution
list. Approx 1 page in two-column sigconf.}
