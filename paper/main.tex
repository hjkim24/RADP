% RADP: Recovery-Aware DP for Distributed LLM Inference on Edge Clusters
% Target: IEEE Transactions on Mobile Computing (IEEEtran journal, two-column)
%
% Build:    pdflatex main && bibtex main && pdflatex main && pdflatex main
% Or:       latexmk -pdf main.tex
%
% Sections are included from sections/*.tex so multiple authors can work
% in parallel without main.tex merge conflicts. The section bodies use only
% standard LaTeX (\section, \cite, \ref, figure/table/algorithm), so they are
% class-agnostic and needed no changes in the acmart -> IEEEtran port.

\documentclass[journal]{IEEEtran}

% --- Packages ----------------------------------------------------------
% IEEEtran (unlike acmart) auto-loads almost nothing, so every package the
% body relies on is listed explicitly here.
\usepackage{cite}                 % IEEE-style citation compression [1]-[3]
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{subcaption}
\usepackage{listings}
\usepackage{xcolor}               % for \todo color
\usepackage{xspace}
\usepackage{textcomp}
% hyperref last; hidelinks keeps clickable refs without coloured boxes,
% which is the convention for IEEE camera-ready.
\usepackage[hidelinks]{hyperref}

\graphicspath{{figures/}}

% --- Typesetting tweaks ------------------------------------------------
% emergencystretch lets TeX widen interword spacing as a last resort to
% absorb the sub-20pt overfull boxes that \texttt{} identifiers create in
% two-column layout, without the whole-paper spread that global \sloppy
% causes. Float separations are trimmed so a wide table + algorithm don't
% cost an extra page.
\setlength{\emergencystretch}{2em}
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\dbltextfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\intextsep}{6pt plus 2pt minus 2pt}

% --- Macros ------------------------------------------------------------
\newcommand{\sys}{RADP\xspace}
\newcommand{\psiR}{$\psi$-R\xspace}
\newcommand{\todo}[1]{{\color{red}\textbf{[TODO: #1]}}}

\begin{document}

% --- Title -------------------------------------------------------------
\title{\sys{}: Recovery-Aware Layer Placement for Distributed LLM Inference on Edge Clusters}

% --- Authors -----------------------------------------------------------
% IEEEtran packs affiliations/emails/funding into \thanks blocks rather
% than acmart's \affiliation/\email. IEEE membership grade is omitted
% pending confirmation.
\author{Hakjae~Kim,~\IEEEmembership{Member,~IEEE,}
  and~Wansu~Lim,~\IEEEmembership{Senior~Member,~IEEE}
  \thanks{H. Kim and W. Lim are with the Department of Electronic and
  Electrical Engineering, Sungkyunkwan University, Suwon 16419, Republic of
  Korea (e-mail: hjkim24@g.skku.edu; wansu.lim@skku.edu).}%
  \thanks{Corresponding author: Wansu Lim.}}

% Running heads: left = journal, right = author + short title.
\markboth{IEEE Transactions on Mobile Computing,~Vol.~XX, No.~X, Month~2026}%
{Kim \MakeLowercase{\textit{et al.}}: \sys{}: Recovery-Aware Layer Placement for Distributed LLM Inference}

\maketitle

\input{sections/abstract}

\begin{IEEEkeywords}
Distributed inference, edge computing, large language models, pipeline
parallelism, fault tolerance, dynamic programming, heterogeneous devices.
\end{IEEEkeywords}

% No-op in journal mode; in peer-review (single-column) mode it re-emits the
% title after the abstract. Recommended by the IEEEtran template.
\IEEEpeerreviewmaketitle

\input{sections/introduction}
\input{sections/background}
\input{sections/design}
\input{sections/evaluation}
\input{sections/discussion}
\input{sections/related}

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
