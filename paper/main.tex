% RADP: Recovery-Aware DP for Distributed LLM Inference on Edge Clusters
% Target: OSDI/NSDI/EuroSys (systems venue, two-column, ~12 pages)
%
% Build:    pdflatex main && bibtex main && pdflatex main && pdflatex main
% Or:       latexmk -pdf main.tex
%
% Sections are included from sections/*.tex so multiple authors can work
% in parallel without main.tex merge conflicts.

\documentclass[10pt, sigconf, anonymous=false]{acmart}

% --- ACM metadata ------------------------------------------------------
% Capstone submission: not a real ACM venue, so we suppress the ACM
% reference block, CCS concepts list, and copyright stamp. acmart's
% sigconf layout is reused only for the two-column formatting.
\setcopyright{none}
\acmConference[SKKU Capstone]{Sungkyunkwan University Capstone Design}{2026}{Suwon, Republic of Korea}
\acmYear{2026}
\acmISBN{}
\acmDOI{}
\settopmatter{printacmref=false, printccs=false, printfolios=true}

% --- Common packages ---------------------------------------------------
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{subcaption}
\usepackage{listings}
\usepackage{algorithm}
\usepackage{algpseudocode}
% acmart already loads amsmath + amssymb; double-loading amssymb
% triggers a "\Bbbk already defined" clash. amsmath we keep implicit
% via acmart too.
\usepackage{xcolor}      % for \todo color
\usepackage{xspace}

% --- Typesetting tweaks ------------------------------------------------
% acmart loads microtype by default in final mode; we just give it more
% room. emergencystretch lets TeX widen interword spacing as a last
% resort to absorb the dozens of sub-20pt overfull boxes that
% \texttt{} identifiers create in two-column layout. We do NOT use
% \sloppy globally — it makes the body widen by ~1 page over the
% 7-page budget. Per-paragraph \sloppy via \sloppypar{} would also
% work but emergencystretch alone handles everything we have.
\setlength{\emergencystretch}{2em}
% Slightly compress floats so a wide table + algorithm don't eat
% an extra page of body text.
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\dbltextfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\intextsep}{6pt plus 2pt minus 2pt}

% --- Macros ------------------------------------------------------------
\newcommand{\sys}{RADP\xspace}
\newcommand{\psiR}{$\psi$-R\xspace}
\newcommand{\todo}[1]{{\color{red}\textbf{[TODO: #1]}}}

% --- Title -------------------------------------------------------------
% The [short] form sets the running head on odd pages; without it,
% acmart shoves the full title up there and it overlaps the
% authors line in the right-hand corner.
\title[\sys{}: Recovery-Aware Placement and Async Chains for Edge LLM Inference]{\sys{}: Recovery-Aware Layer Placement and Asynchronous Chain Forwarding for Distributed LLM Inference on Edge Clusters}

% --- Authors -----------------------------------------------------------
\author{Hakjae Kim}
\email{hjkim24@g.skku.edu}
\affiliation{%
  \institution{Sungkyunkwan University}
  \department{Department of Electronic and Electrical Engineering}
  \city{Suwon}
  \country{Republic of Korea}
}

\author{Wansu Lim}
\authornote{Corresponding author.}
\email{wansu.lim@skku.edu}
\affiliation{%
  \institution{Sungkyunkwan University}
  \department{Department of Electronic and Electrical Engineering}
  \city{Suwon}
  \country{Republic of Korea}
}

% Short form for the running head on even pages — without this,
% acmart auto-derives "Kim and Lim" but the spacing collides with
% the running title on the same line.
\renewcommand{\shortauthors}{Kim and Lim}

\begin{document}

% acmart requires the abstract environment to be defined BEFORE
% \maketitle; otherwise it complains "Abstract must be defined before
% maketitle command." Counter-intuitive but documented.
\input{sections/abstract}

\maketitle

% 7-page target. Implementation is folded into design (one short
% subsection covers the runtime), discussion + conclusion live in
% one section together, and related work is a single dense
% paragraph at the back of the discussion.
\input{sections/introduction}
\input{sections/background}
\input{sections/design}
\input{sections/evaluation}
\input{sections/discussion}
\input{sections/related}

\bibliographystyle{ACM-Reference-Format}
\bibliography{references}

\end{document}
