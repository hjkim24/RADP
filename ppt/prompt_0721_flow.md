# 0721 덱 — 흐름만 고치는 프롬프트 (전체 재생성 아님)

`Progress Report_0721.pptx` 를 열고 아래를 붙여넣는다.
서식·문체·쪽번호·그림은 이미 규격에 맞다. **건드리지 마라.**

---

이 덱은 서식이 이미 맞다. 고칠 것은 **§2 의 슬라이드 순서와 빠진 슬라이드 하나**뿐이다.
다른 슬라이드의 좌표·폰트·색·문구를 손대지 마라. 규격은 `ppt/DESIGN_SYSTEM.md` 에 있고,
특히 §8 「§2 안의 순서」가 이 작업의 근거다.

## 왜 고치나

이번 주에 parity 가 처음 등장한다. 지금은 **3종 비교(4번)가 원리(5번)보다 앞**이라
청중이 모르는 기법의 수치를 먼저 평가하게 된다. 그리고 **장단점 슬라이드가 없어**
parity 가 이기는 그림만 남아 있다.

## 목표 순서

| 현재 | 바뀔 것 |
|---|---|
| 4 · 3종 비교 | **4 · 기존 2종의 한계** (그림 교체) |
| 5 · parity 원리 | **5 · parity 원리** (그림 교체 + 전폭 배치) |
| — | **6 · 장단점 표** (신설) |
| 6 · 일반화 | 7 · 일반화 (그대로 밀림) |
| 7 · TTR | 8 · TTR |
| 8 · 수치표 | 9 · 수치표 |

## 작업 1 — 4번 슬라이드

제목·불릿을 아래로 교체하고, 그림을 `fig_recovery_families_before.png` 로 바꾼다
(7.14 in, 배율 100%, 현재와 같은 자리 L5.30 T1.75).

- 소제목(굵게): `a. 기존 두 방식은 둘 다 재계산`
- 불릿:
  - `• full-replay — 체인 전체 재-forward`
  - `• surgical — 죽은 stage만 재-forward`
  - `• 둘 다 모델을 다시 돌림 → 비용이 장애 깊이 P에 비례`
- 캡션 유지: `OPT-350M 24층 / 5-stage 이종 체인 / position 1개당 비용`

## 작업 2 — 5번 슬라이드 (배치가 바뀐다)

원리 그림이 3패널 전폭판으로 커졌다. **좌측 불릿 3줄을 지우고** 그림을 전폭으로 놓는다.
3패널 자체가 설명이라 옆에 라벨을 또 붙이면 같은 말이 두 번 나온다.

- 소제목(굵게): `b. parity 원리 — 재계산 대신 XOR 역산`
- 그림: `fig_parity_mechanism.png` — **11.79 in**, L 0.63 T 2.10, 배율 100%
- 그림 아래 한 줄(16pt): `저장량은 stage 수와 무관하게 KV 1개분 (RAID-5)`
- 캡션(12pt): `① 무엇을 저장하는지 ② XOR이 바이트 단위 ③ 역산`

## 작업 3 — 6번에 장단점 슬라이드 신설 (P4 표 패턴)

5번 뒤에 새 슬라이드를 넣는다. 밴드 `2. Progress last week`, 워크스트림 `1. RADP`.

- 소제목(굵게): `c. 공짜 아님 — 3종 장단점`
- 표 (L0.63 T1.75 W12.05, 6행 4열, 헤더 `202843` 흰 글자, 본문 `E8EAF0`/`F6F7F9` 교대):

| | full-replay | surgical | parity |
|---|---|---|---|
| 복구 시 재계산 | 체인 전체 | 죽은 stage | 없음 |
| 깊이 P당 비용 | 164 ms | 16 ms | 0.87 ms |
| 정상 운영 추가 비용 | 없음 | 입력 미러 | 입력 미러 + KV 컬럼 전송 |
| 적용 범위 | 전부 | 전부 | non-head + downstream 생존자 필요 |
| 동시 장애 | — | — | 1대 (RAID-5) |

- 표 아래 결론 한 줄(16pt 굵게): `깊이 비용을 정상 운영 중 네트워크 세금과 맞바꾼 것`

**`동시 장애` 행의 앞 두 칸은 `—` 그대로 두어라.** 측정한 적 없는 값을 채우지 마라.

## 작업 4 — 한계 슬라이드 정리

장단점 표로 올라간 항목은 한계 슬라이드에서 뺀다. 남길 것:

- `• tail victim은 여전히 surgical 폴백 …` (현행 유지)
- `• KV shipping 비용은 장단점 표에 있음. 정량화는 안 함`
- `• prefill(position 0) 장애는 재계산 0 아님`
- `• 선행연구: KV parity 자체는 GhostServe가 먼저 함 …` (현행 유지)

## 끝나고 확인할 것

- §2 가 6장인지 (§1 상한). 넘으면 알려라 — 슬라이드를 임의로 지우지 마라
- 쪽번호가 전 장에 있는지
- 새로 넣은 표에 PowerPoint 기본 표 스타일의 밴딩이 살아나지 않았는지
- 새로 쓴 문장이 음슴체·명사 종결인지, 영단어 뒤 조사가 붙어 있는지
