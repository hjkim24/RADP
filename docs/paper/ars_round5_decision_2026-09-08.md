# Editorial Decision — Round 5 (Fourth Verification) — KV-CARE (IEEE IoT-J)

**Date:** 2026-09-08 · **R1:** Major · **R2:** Major · **R3:** Minor · **R4:** Accept with editor-verified errata (5 MUST) · **R5:** see §6
**Basis:** five Round-5 verification re-reviews (`R0/R1/R2/R3/DA_round5.md`) checked against `EDITORIAL_DECISION_R4.md` §8, errata 1–27 (MUST 1–5, SHOULD 6–16, OPTIONAL 17–27), and §4 L1–L9.
**Standing constraints honoured:** the testbed is closed — nothing below asks for a measurement; where a reviewer asks the authors to "read it off the board" (R3 on `ao-2`) the panel substitutes the closed-testbed form. The page budget is lifted (11 pp.; the eleventh page is references only — R0 W11); the R4 "declared page-budget decline" protection therefore no longer applies to items the authors chose to spend the page on, and those items are graded as content (R3 §0).
**Scope this round:** both the Round-4 errata *and* the post-decision additions — the environment paragraph (§IV-A L44–50), the coordinator-sizing paragraph (§IV-C L244–251 + Limitations L82–85), §II-B with five IoT-J references, the reframed introduction, the ~20 sentence splits, the redrawn Fig. 1, and the abstract's four-sentence restructure.
**Rule for MUST-FIX, unchanged since Round 3:** MUST only if (a) a **verified factual or internal-consistency error against the authors' own files, code or logs**, (b) an **upheld DA CRITICAL**, or (c) **three or more reviewers name it blocking**. The Round-4 clarification stands: a docketed clause neither applied nor declared carries its grade forward; and, this round's corollary, a docketed SHOULD that is neither applied nor declared **and is then contradicted by new text** is graded on the new text under rule (a).

---

## 1. Panel table

| Reviewer | R4 rec | **R5 rec** | Counts this round | Items still named for the editor | Score movement (R1 → R2 → R3 → R4 → **R5**) |
|---|---|---|---|---|---|
| **R0** Journal-Fit | Accept, subject to editor-verified corrections | **Accept** — "Nothing blocks acceptance"; one label at proof (N18) | 26 errata rows traced: A 16 · P 5 · N 5 (1 declared, 2 optional-unclaimed, 1 undeclared SHOULD [item 14], **1 claimed-as-applied [item 18]**). **MUST 1–5: 5/5 A.** Own N14–N17: **4/4 A**. 17 numbers re-derived; 0 fail; 1 loose ("about 30"). Six DOIs resolved, five abstracts read. | N18 (JetPack label), N19 ($N_{\mathrm{ctx}}$/units/"about 30"), N20 (letter §2 a–f); nits N23–N28 | **Fit 6→6→6→6→7** (first movement in five rounds) · Originality 6→7→7→7→**7** · Significance 5→5→5→5→**5** · Clarity 6→7→8→9→**9** · **Claims-vs-evidence 4→6→8→8→9** |
| **R1** Methodology | Accept, subject to two one-clause corrections | **Accept** — "Nothing blocks acceptance … I do not need to see the manuscript again" | **Both R4 blocking items (NEW-13, NEW-14) closed.** MUST 5/5 A. SHOULD 11: A 8 · HALF 2 (11, 12) · N-declared 1 (10) · **N-undeclared 1 (14)**. 16 numbers recomputed: **14 exact · 1 loose · 1 wrong qualifier** ("as built"). Environment paragraph checked clause by clause: 8 of 10 clauses trace to the repository. | NEW-20 (JetPack), NEW-21 (256 MiB caps), NEW-22 (six-vector "as built"), NEW-23 (letter), NEW-24 (`ao-2`, LAN rate), S-1 | Exp. design 5→6→6→6→**6** · Stat. validity 4→4→5→6→**6** · **Metric defs 6→7→8→8→9** · **Reproducibility 3→4→5→6→7** · Limitations 6→7→9→9→**9** |
| **R2** Domain | Accept, subject to one editor-verified clause | **Accept** — one proof-stage erratum (N5-1, rule (a)); "no reviewer needs to be re-engaged" | N4-1…N4-5 **all A**. MUST 5/5, **each string grepped across all six section files — no surviving twin**. SHOULD 6, 7, 8, 9, 11, 12(half), 13, 15, 16 A; 10 declined-declared; **14 neither applied nor declared**. Fatal triggers F1–F6: none fired; GhostServe re-checked against its PDF. | N5-1 ("as built"), N5-2 (third "coordinator state" site), N5-3 (§II-B two words), N5-4 (letter), N5-5 (cosmetic) | **Literature 5→6→7→7→8** · Tech. soundness 5→6→7→8→**8** ("would be 9 with N5-1's one phrase") · Novelty 5→5→5→6→**6** · **Fairness 5→7→8→8→9** |
| **R3** IoT practitioner | Accept, subject to one editor-verified erratum | **Accept** — "Nothing blocks"; "I would deploy from this paper" | 27 graded rows: **A 19 · P 3 · N 3 · D 3 · W 0.** R4 blocking NEW-1 closed. All three Fig. 1 asks taken, the first in the stronger (redraw) form. Clean rebuild with `tectonic`: 11 pp., 0 overfull, no undefined citations. | NEW-1 (256 MiB caps), NEW-2 (six vectors), NEW-3 (memory-only bound), NEW-4 (`ao-2` mode), NEW-5 (letter), NEW-6 (§V-A L68) | **Deployability 4→5→6→6→7** · Motivation–eval 3→5→6→6→**6** · **Practical impact 5→6→6→7→8** · **Clarity for practitioners 6→6→6→7→8** |
| **DA** Devil's Advocate | "Nothing blocks acceptance" — accept after errata | **"Nothing blocks acceptance"** — accept after editor-verified errata (five listed) | R4 errata 1, 2, 3, 5 discharged; 4 declared. K1, K3, K6, K9, K10, K11, K15, S-4, S-10 in. **0 new CRITICAL.** Every pre-existing derived number reproduces (499.6 ms; $P{=}32$ means to three decimals; ±26.67; 9 864 090). New attacks: §3.1(i)–(vi) sizing, §3.2 environment, §3.3 §II-B, §3.4 introduction. | Errata 1 ("as built"), 2 (30 GB free / "about 34" / 256 MB caps), 3 (JetPack), 4 (Zhang "must"), 5 (letter) | no rubric |

**Panel arithmetic: 5 of 5 recommend acceptance; 4 write "Accept" with items for the editor at proof, 1 (DA) writes "accept after editor-verified errata". No reviewer asks for another external round. Across the 18 scored dimensions, 9 moved up and none moved down** — the largest single-round movement in the five rounds (R4: 7 up). Two ceilings the panel has carried since Round 1 moved for the first time: **R0's Fit 6→7** on §II-B and the reframed introduction ("earned … I checked the five characterisations against the abstracts"), and **R3's Deployability 6→7** on the coordinator-sizing paragraph ("the one thing holding this at 6 for four rounds").

**What the panel unanimously records as delivered** (all five, independently, against the files, not the letter): every Round-4 MUST — "backup room" gone from every section (K1; `grep` → 0 by R0, R1, R2, R3, DA), the 493 parenthetical deleted with 500 ms standing on the 60 gated rows (K2; 499.6 re-derived by R1 and DA), "In an offline placement analysis" in the Conclusion (K3; byte-for-byte the abstract's), the Pareto class replaced by the endpoint clause four reviewers asked for since Round 3 with Petals' direction stated (K4; 2.314 s, crossover $P{=}10.14$, 3.71×, 1.31 steps reproduced by R1 and DA), and Fig. 1's caption "retains stage inputs" (K5). Beyond the MUSTs: **Fig. 1 regenerated with an asymmetric $R$** — the OPTIONAL stronger form, verified against `make_architecture.py` L15–16 and the PNG by all five; "coordinator KV state" at both sites (K11); the head-failure path named and marked unmeasured (K9); 9.9 million (K10); 527 ms named as its own quantity (S-4); ±27 (K16); "whose recovery contract holds" (S-12); Fig. 3 generator aligned to its caption (S-10); `kosaian2019parity` paged (K15). **The four-round propagation pattern did not recur for any MUST string** — R1, R2 and R3 each grepped every erratum string across all six files (R1 §4: "`backup room` 0, `493` 0, `solely` 0, `least state` 0, `interrupted position's input` 0"). And the page the authors bought was spent, in R0's words, "where four rounds of reviewers asked them to spend it".

---

## 2. Consensus residuals (≥2 reviewers)

Every item is in text that did **not exist** at the Round-4 decision, or is a docketed R4 item left unapplied; none touches a measured result (R0, R1, R2, R3, DA all say so).

### 2a. The coordinator-sizing paragraph (`evaluation.tex` L244–251)

| # | Defect | Location as quoted | Raised by | Minimal fix |
|---|---|---|---|---|
| **Q1** | **"as built" attached to the five-vector design mirror; the prototype keeps six.** R4 SHOULD 14 (DA's S-2) was neither applied nor declared, and the new sentence now labels the accounting count as the build's. Four reviewers independently read `gateway.py:1729–1735` ("prime the mirror cache locally for the chain-head stage … `self.cache.put(request_id, first_key, position, blob)`", per position, unconditional). Six 8 kB vectors = 48 kB/tok = **96 MiB** per 2 048-token request (R1 row 11, R2, R3); DA adds that each blob also carries an `int64` attention mask and estimates ≈200 MiB (single-reviewer, "torch was not available locally", uncomputed for the container — conflict **B1**). The two bounds survive under every count (96 → "roughly 100"; 33 → "about 30"); §IV-C L230–231's 3.0×/42 % become 2.9×/41 % on six vectors | L246–247 "plus 80\,MB of mirror **as built**" | **R2-N5-1 (rule (a), "should not ship")** · **R1-NEW-22** ("undeclared and propagated") · **R3-NEW-2** · **DA §3.1(ii) / erratum 1** ("the words 'as built' make it false") · R0 item 14 / N25 (undeclared; soft) — **all five** | Keep 80 MB and replace "as built" with **"by Table I's five-vector accounting (the prototype also primes the head's input, 8 kB per token)"** — R2's second form plus R1/R3's parenthetical; exact under five, six or DA's mask-inclusive count. Then scope L230–231's 3.0×/42 % "on the five shipped vectors" or recompute 2.9×/41 %. Letter: file item 14. **MUST** — rule (a), four code readings, and the R4 K2 species (a docketed item made worse). |
| **Q2** | **"leaves room for roughly 100 such requests in flight" beside two 256 MiB stores.** `parity_cache.py:33` and `activation_cache.py:32` default to 256 MiB; `ParityCache` is constructed at `gateway.py:163–165` **without** `max_bytes`; no config key in `server.py` or `deploy/`; eviction is whole-request LRU. One 2 048-token single-parity column is 224 MiB, so as built the coordinator holds about one request's parity and three requests' mirrors before eviction (R1, R3, DA agree on the mechanism). The sentence is true as memory arithmetic ("leaves room"; the preceding sentence ends "a derived projection from the measured layer shapes" — DA's own concession) | L249–251 | **R1-NEW-21** (minor, omission) · **R3-NEW-1** (SHOULD; "the one that would page someone at 3 a.m.") · **DA §3.1(iii) / erratum 2** (require) — three | One clause: "(a derived projection; the prototype's parity and mirror stores default to 256 MB each and evict whole requests, so the bound assumes they are raised)". **SHOULD, first-ranked** — rule (a) not met (true as written, labelled a projection one sentence earlier), rule (c) not met (one blocking). Conflict **B2**. |
| **Q3** | **"about 30 GB free" is the fleet table's RAM column, not a free-memory measurement.** `REPORT.md:41` "ax-1 … 30 GB" sits in a column whose Nano entries read 7.4 GB and Orin entries 29 GB — `MemTotal` as `free` reports it (R1 row 12: "the RAM column; total as `free` reports it; ~29 GB free"; DA §3.1(i)). No saved profile records `ax-1`'s free memory (DA: `d29_coupling_threshold_20260903.json` lists the six workers, no `ax-1`). DA alone cites a 2026-08-12 design note "Coordinator has only ~7 GB memory available" on a shared machine — a risk note, not a measurement, and unsettleable now | L249 "has about 30\,GB free" | **DA §3.1(i) / erratum 2** · R1 row 12 (corroborates "total"; grades "acceptable") · R0 item 23 ✓ · R3 §2 ✓ — one attacks, three accept, one corroborates the column's meaning | "is a 32\,GB board with about 30\,GB of RAM" (DA's form). Exact under every reading; costs nothing; sidesteps the ~7 GB question no one can settle. **SHOULD.** Conflict **B3**. |
| **Q4** | **"about 30" for replication is 33.7** (31.4–33 under decimal GB or six vectors); the pair "roughly 100 … about 30" reads as 3.3× where the paragraph's own all-family ratio two sentences earlier is 3.0× (L230). The rounding runs in the authors' *disfavour* on replication's room but *favours* the comparison | L250 | R0-N19 ("about 35" or "a third as many") · R1 row 13 (loose) · R3 §2 (conservative; "34 would be closer") · **DA §3.1(iv)** ("rounds the wrong way") — four | "about 34" or "about a third as many". **SHOULD.** Precedent: K10's 9.8→9.9 was SHOULD; the relative gap here (11 %) is larger. |
| **Q5** | $N_{\mathrm{ctx}}$ used without definition; "224/448/80/832 MB" are MiB under the paper's kB = 1 024 convention, which Table I's note declares for kB only — J8's fourth site | L245–248 | R0-N19 · R3 K14 row ("the units are binary throughout and the numbers check") — two | "$N_{\mathrm{ctx}}$, the context length in tokens"; J8 remains declared. **OPTIONAL.** |

### 2b. The environment paragraph (`evaluation.tex` L44–50)

| # | Defect | Location | Raised by | Minimal fix |
|---|---|---|---|---|
| **Q6** | **"The workers run JetPack 6.1" is contradicted by the authors' own log for at least one worker.** `PHASES.md:1995` records `ao-1` at **L4T R36.4.3**, which NVIDIA ships as **JetPack 6.2**; `PHASES.md:1981` records `ao-2` re-flashed to "JetPack 6.1 Rev1 (L4T R36.4.0 …)". The same paragraph prints "MAXN SUPER" for the Nanos, a mode R1 and DA identify as a JetPack 6.2 feature (R1 flags this as NVIDIA release-note knowledge, not a repository fact; the panel rules on `PHASES.md:1995` alone). CUDA 12.6 / cuDNN 9.3 / Python 3.10 / Ubuntu 22.04 are common to both releases and stand (R0, R1, DA). R3 read the same line and accepted "R36.4 covers both"; R2 checked pair-consistency only | L44–45 "JetPack 6.1 (L4T R36.4, …)" | **R0-N18** ("the only candidate" under rule (a); "correct at proof") · **R1-NEW-20** ("factual, contradicted by the authors' own log") · **DA §3.2 / erratum 3** ("WRONG for five of six workers"; require) — three verify; R3, R2 do not contest | "JetPack 6.1–6.2 (L4T R36.4.0–36.4.3, …)" or per board. **MUST** — rule (a) is satisfied by three independent readings of `PHASES.md:1995`; a version label carries no "about" to cover it. Conflict **B6**. |
| **Q7** | **"`ao-2` as flashed" names no power mode** for the board holding seven layers (19–25) in the measured 7B placement; no record of `ao-2`'s mode exists anywhere in the repository after the 2026-06-30 re-flash; the authors' own note (`PHASES.md:1122`) records the AGX Orin factory default as MODE_30W, the mode in which `ao-1` was slower per layer than a Nano. "As flashed" is honest (R0: "the disclosure itself runs against the authors — the two AGX were not configured alike") | L48–49 | R0-N18 ("the mode name would be better") · **R1-NEW-24a** ("asserted without a source … say the mode or say 'not recorded'") · **R3-NEW-4** ("read it and print it") · **DA §3.2** ("unverifiable and underspecified") — four | The testbed is closed, so R3's "read it" is not available. "`ao-2` at its flashed default (power mode not recorded; the factory default is 30\,W)" — or the mode, if the authors hold a log. **SHOULD.** Conflict **B9**. |
| **Q8** | **No LAN rate** — the K20 clause three rounds running. R3: the repository holds only an early application-level "~10 MB/s gRPC" figure, "not a link rate — nothing to print". DA: the scheduler's own probe *is* saved — `b1_steady_r2_protection_off_20260830.json` `scheduler/network_profile/bandwidth_bps` (`on-1->on-6` 22 295 123, `on-2->on-6` 23 602 725, `on-2->on-1` 16 445 263; 1 MiB payloads × 10 rounds, `all.yml:179–180`), i.e. 16–24 Mbit/s pairwise as the placement saw it | L50 "one wired campus LAN" | R0 §4 · **R1-NEW-24b** · R3 §3 · **DA §3.2** — four ask; R3 says nothing printable, DA locates a number | Print what exists, labelled as what it is: "one wired campus LAN (the scheduler's gRPC probe measured 16–24\,Mbit/s between worker pairs)"; or the nominal link rate if the authors hold it. **SHOULD.** Conflict **B5**. |
| **Q9** | **No artifact / code-availability statement** — the last clause of K20; "the repo, the result JSONs and the figure generators … are the thing every number in this paper was verified against for four rounds" (R3) | §IV-A | R0 §4 · R1 §2 · R3 §3(e) · DA §3.2 — four | One sentence naming where the code, result JSONs and figure generators will be deposited. **SHOULD.** |
| **Q10** | Reproducibility details R3 alone lists: grpcio/protobuf as installed (the paper prints the pin "1.60+", honestly — R1, DA); the JetPack 5 minor on the Xavier (5.0 per `PHASES.md:324`) | L46–47 | R3 §3(b)(c) (R1 and DA accept "1.60+" as a floor honestly stated) | **OPTIONAL.** |

### 2c. §II-B (`related.tex` L23–40) and the reframed introduction

| # | Defect | Location | Raised by | Minimal fix |
|---|---|---|---|---|
| **Q11** | **Sun et al. "migrate tasks away from failed nodes"** — the cited abstract is a *proactive* framework: "fault identification and task migration decisions" ahead of the loss; "failed" reads as reactive. Not a mis-characterisation of the mechanism, "a mis-description of when" (DA) | L27–28 | **R0 §3.2** ("loses the paper's operative word"; suggest "migrate tasks proactively, ahead of predicted node failures") · **R2-N5-3** ("nodes predicted to fail") · **DA §3.3** ("failed" → "failing") · R3 §4 ("✓ fair") — three flag | "migrate tasks away from nodes identified as failing". **SHOULD.** One word. |
| **Q12** | **"the substitute node is chosen after the service it replaces has been placed" over-generalises for Ma et al.**, whose reliability-aware scheduler has no substitute node: availability is learned *inside* the scheduling decision — closer to KV-CARE's own coupling than the sentence admits. True of Mudassar, Sun, Zhang (fog) | L32–34 | R0 §3.2 (soft; "fits Ma least") · **R2 §3 / N5-3** · R3 §4 ("fits Ma least … still true of what Ma protects") · **DA §3.3** ("fits two or three") — four | R2's clause: "These mechanisms protect a task's completion; **where a substitute node exists,** it is chosen after the service it replaces has been placed." **SHOULD.** |
| **Q13** | **Zhang et al. (joint) "show that coupled placement decisions must be solved together"** — the abstract says the two are "coupling issues in practice … a complete process combining edge server and service placement" via "a two-step method" (clustering + nonlinear programming). "Must" is the authors' thesis in the cited paper's mouth; and the method is the sequential kind §IV-E calls "cost-first" | L34–36 | R0 §3.2 ("a shade stronger … acceptable") · R2 §3 ("'must' is the authors' inference … 'benefit from being solved together' is the safer verb") · **DA §3.3 / erratum 4** ("Overclaims"; require) — three | "argue that the two placement decisions are coupled and solve them in one combined process" (R2's verb, R0's abstract quote; avoids DA's "one formulation", which its own reading of the two-step method undercuts). **SHOULD** — not the authors' own files, so not rule (a). Conflict **B4**. |
| **Q14** | "assistants in homes and buildings" is the one uncited example in the new opening, and the one the isolation argument that follows does not cover | `introduction.tex` L4 | R0 §3.2 (nit) · R3 §4 ("the one clause I would cut") · DA §3.4(2) — three | Cite or cut. **OPTIONAL.** |
| **Q15** | "Zhang et al." names two groups nine lines apart; IEEE numerals disambiguate, the prose does not | `related.tex` L29, L34 | R0 §3.2 / N26 · (R2 and DA distinguish them as "(fog)" / "(joint)" in their own tables) | "P. Zhang et al." / "X. Zhang et al.", or "(fog)"/"(joint)". **OPTIONAL.** |
| — | **Recorded as verified, no action:** the five characterisations are accurate at the granularity claimed (R0, R2, R3, DA — Mudassar "nearly verbatim"; Zhang (fog) confirmed from the abstract by R0 and DA, closing R3's title-level doubt; Ma "exact"); the closing delta holds against all five (DA: "a real difference from all five, including Zhang (joint), whose solver is two-step"); the paragraph is "not padding" (R0 §3.3), correctly placed (R2, R3); the introduction's reframing moves the scope word, not the claims (DA §3.4, R3 §4: "still honest to the industrial motivation"); the dropped `zhang2026finetuning` is cited nowhere (R0 §1.3); GhostServe re-checked against its PDF (R2 §3). | | | |

### 2d. Carried and propagated residuals

| # | Defect | Location | Raised by | Minimal fix |
|---|---|---|---|---|
| **Q16** | **K9's fix landed nineteen lines downstream of the sentence it targeted.** L68–69 "the head stage and the coordinator are unprotected" is unchanged; the new L87–88 "A head failure falls to full-prefix replay on its preloaded backup, a path we did not measure" disambiguates it for a reader who reaches it. The propagation pattern in its mild form | `discussion.tex` L68–69 | R0-N24 · R2-N5-5 (cosmetic) · **R3-NEW-6** (rec. 5) — three | "the head stage is outside the parity guarantee and the coordinator is unprotected". **SHOULD.** One clause. |
| **Q17** | **The eight-device search cap declined as "unverified" — it is a literal in the authors' own files.** `scheduler.py:289` `max_search_devices: int = 8`, L322 `if max_search_devices < M:` falls back to spec order; `server.py:129, 205`; `deploy/group_vars/all.yml.example:79` / `all.yml:143`. It sits beside the newly printed "about 9.9 million at ten devices", a number the prototype would never enumerate ($\sum_{k=2}^{8}P(8,k)$ = 109 592 at eight) | `design.tex` L225; letter "(unverified)" | R0-N27 / §2(e) · **R1 S-1 / row 12** · **R2-L-c** ("not a defensible ground") · **R3 S-1** ("the reason given for not applying it is not a reason") · DA — **all five** | R1's clause: "the prototype searches up to eight devices (109 592 orderings) and falls back to heartbeat order above that." Letter: replace "unverified" with applied or "by choice". **SHOULD.** |
| **Q18** | K13's other half: "tolerate **any number** of simultaneous protected-stage failures" for replication, where `_recover_replicate` attributes one victim and one was measured | `evaluation.tex` L232 | R3 §1a / rec. 6 · **DA K13 row** ("should-fix, unchanged") · R2 SHOULD 11 row — three | "(one was measured)" after "any number". **SHOULD** (carried from R4 item 11). |
| **Q19** | "At $P{=}32$" grammatically governs "recovers faster than \sys{} only below $P\approx10$", a cross-$P$ fit property | L211–213 | R0-N23 · R1-NEW-25a · R2 MUST-4 row (cosmetic) — three | "Petals is the low-state endpoint (40\,kB per token; 2.3\,s at $P{=}32$) and recovers faster …". **OPTIONAL.** |
| **Q20** | "42\% for two" still lacks the "less" its parallel "46 % less than replication" carries | L231 | R0 · R2 · R3 · DA — four | One word. **OPTIONAL** (carried). |
| **Q21** | `main.log`/`.bbl`/`.aux` in the repository are stale (Aug 27–28, 9 pp., an undefined `sec:discussion`); the Sep 8 PDF was built elsewhere. R3 rebuilt clean: 11 pp., 0 overfull, no undefined citations | `paper/main.log` | R0-N28 · R1 §6 · R3 (materials) — three | Build from the repository once and commit the log. **OPTIONAL**, production. |
| **Q22** | `background.tex` orphaned; the page-budget reason no longer applies | `main.tex` L93–97 | R0 · R2 · R3 (declared, item 34) — three | Delete. **OPTIONAL**, declared. |
| **Q23** | The five §II-B PDFs are not in `paper/refs/` and `PAPERS.md` does not catalogue them — the repository's own convention; `PHASES.md` L2604 records a Crossref check, not a reading | `paper/refs/` | R1 §6 · R3 §4 — two | Housekeeping; not a manuscript defect (R0 and DA read the abstracts and found the clauses accurate). **OPTIONAL.** |
| — | **Declined by declaration, recorded, not pressed:** Table I note (a) Reconfigure discard reasons (K12/item 10) — "by choice"; true as written; runs against the authors (R0, R1, R2, R3, DA all record it). | | | |

---

## 3. Single-reviewer residuals worth acting on

| # | Defect | Location | Reviewer | Minimal fix |
|---|---|---|---|---|
| **S-1** | **The ≈200 MiB as-built mirror** — six blobs per position each carrying an `int64` attention mask of length `past_len+1` (`gateway.py:606`; `stage_runner.py:341–344`; `worker/server.py:439–451` mirrors the blob verbatim; `tensor_io.py:16–20` `torch.save` container): hidden 96 MiB + masks ≈96 MiB + container | `evaluation.tex` L247 | **DA §3.1(ii)** — uncorroborated (R1, R2, R3 compute 96 MiB on the hidden vectors alone; DA: container "unmeasured here") | **Not adopted as a number.** Q1's relabelling is exact whether the build holds 96 or 200 MiB; the panel will not supply a figure it has not gated (the R4 B7 lesson). If the authors hold a per-request RSS delta, print it; otherwise the relabel suffices. |
| **S-2** | **Recovery-time working memory is outside the bound**: a parity recovery fetches the four surviving columns ((416−112) kB/tok × 2 048 = 608 MiB) before the XOR, plus the 224 MiB rebuilt column — ≈3.7× a request's steady footprint, transient; replication installs one column and fetches nothing | `evaluation.tex` L249–251; `gateway.py:1097–1155`; L173–174 | **DA §3.1(v)** | One clause if the authors touch the paragraph: "a recovery additionally holds the surviving columns transiently (about 0.8\,GB at 7B)". **OPTIONAL.** Runs against the authors; R3's Q24 clause is the cheaper cousin. |
| **S-3** | The bound is memory-only; coordinator compute under concurrent folding (one XOR, plus one GF($2^8$) multiply-accumulate at $k{=}2$, per protected column per token per request on eight Carmel cores) was not measured; Limitations L104–106 says so twenty lines from where the number is read | L249–251 | **R3-NEW-3** | Five words in place: "a memory bound; coordinator compute under concurrency was not measured". Fold into Q2's clause. **SHOULD** (as part of Q2). |
| **S-4** | A third "3.7× less coordinator state" site, not on K11's list of two; it points at the section that prints 3.0× | `evaluation.tex` L177–178 | **R2-N5-2** | "coordinator **KV** state". One word. **OPTIONAL.** |
| **S-5** | The introduction's governing phrase "deployments that run on site" is asserted of CASIT, whose abstract describes an LLM-agent IoT system and does not say the LLM runs on the devices | `introduction.tex` L3–4 | **DA §3.4(1)** — R0 §3.2 checked the clause "collective agents over IoT devices" against the title and found it accurate | "LLM agents over IoT devices~\cite{zhong2024casit}" without the "run on site" binding, or move CASIT after the colon's second item. **OPTIONAL.** |
| **S-6** | "so …, and reconfiguration grows from 24.25 to 439.4 s" hangs reconfiguration's growth off recomputation's "so" | `evaluation.tex` L402–403 | **R1-NEW-25b** (trivial) | Full stop before "Reconfiguration grows". **OPTIONAL.** |
| **S-7** | Mudassar's operative word "adaptive" (choosing checkpoint/restart vs replication by latency requirement) is compressed away | `related.tex` L26–27 | **R2 §3** (soft; "not wrong") | No action required; recorded. |
| **S-8** | Fig. 1's grey backup labels (≈7 pt printed) could be darker; stage $n$'s column drawn like stage 2's though never parity-recoverable | Fig. 1 | R0 S-11 · DA S-8 (both carried, both OPTIONAL in R4) | **OPTIONAL**, unchanged. |

---

## 4. Letter-vs-file findings

**Headline: the first false change description since Round 2 — on an OPTIONAL item — plus one false reason, one miscount, one missing pointer and two undisclosed editing passes. No false description of a required change.** All five reviewers checked the Round-4 header, all six table rows, every clause of the "Applied after the Round-4 synthesis" paragraph (13–14 clauses) and the "Post-decision additions" paragraph (5 clauses), plus the Round-3 corrections R4 §4 asked for. **Verified true, five for five:** rows 1–3, 5, 6; the six MUST descriptions in the words the files use; K3, "coordinator KV state", the head-failure path, "whose recovery contract holds", "parity falls to replay beyond $k$", 527 ms, ±27, 9.9 million, the Fig. 1 coordinator caption, Fig. 3 mean (generator verified), `kosaian` pages; the Fig. 1 row ("the best row in the section" — R2); the three post-decision additions as a list; 34 → 39; the Round-3 "spread" row refreshed (R4 L1 ✓); J8 filed and J10 credited (R4 L3 ✓); Fig. 1 and the +1 renumbering disclosed (R4 L6 ✓); "one numeric clause … was false" (R4 L7 ✓ — three reviewers; conflict **B7**).

| # | Letter clause | Verified status | Found by |
|---|---|---|---|
| **L1** | Applied-¶: "the tolerance sentence says parity falls to replay beyond k **and fetches surviving columns at recovery**" | **False for the second clause.** No such words at L232–237 or anywhere new (`grep "at recovery"` → L10 only; "surviving columns" → the pre-existing L173–174). OPTIONAL item 18, closed as MUST in R4 B9, reported as applied. The manuscript loses nothing; "described the intended edit rather than the one made" (R0) | R0 §2(a) · R1-L5/NEW-23a · R2-L-b · R3-L8 · DA §2 — **all five** |
| **L2** | Row 4: "§III-A now notes … **pointing to §IV-C's accounting**" | **Half true.** The parenthetical is at `design.tex` L34–35; `grep "ref{sec:eval"` in `design.tex` → nothing | R0 §2(b) · R2-L-a · R3-L4 · DA §2 — four |
| **L3** | "Not applied: … the eight-device search bound (**unverified**)" | **The reason is false of the authors' own code** — `scheduler.py:289`, `server.py:129, 205`, `all.yml.example:79` / `all.yml:143` (Q17) | R0 §2(e) · R1-L8 · R2-L-c · R3-L10 · DA §2 — **all five** |
| **L4** | Post-decision ¶: "IoT-J entries 1 → **6**" | **7**: `zhang2024edgeshard` + five §II-B + `zhong2024casit`, all with `journal = {IEEE Internet of Things Journal}`, all cited. R2 counted 6 (omitting CASIT, which enters via the introduction). Under-claim in the authors' own favour | R0 §2(c) · R1-L10 · R3-L11 · DA §2 (7) vs R2-L-h (6) — conflict **B8** |
| **L5** | Row 6: "J10's exactness novelty clause was applied (**§II-E**)" | **Stale by one.** The post-decision §II-B pushed Parity to **§II-F** (pdftotext: A Distributed, B Fault Tolerance in Edge and IoT, C Recomputation, D Replication, E Reconfiguration, F Parity) | R0 §2(d) · R2-L-e · R3-L7 — three |
| **L6** | Post-decision ¶ lists three additions only | **Undisclosed:** the introduction's reframed opening with CASIT (commits `8777dc0`, `0644c81`), the **dropped** `zhang2026finetuning` (the bibliography's only deletion — "34 → 39" is right only because one was dropped and six added), the abstract's four-sentence restructure, the ~20 sentence splits across six files (`dfb0520`, `91af728`, `0c064a6`), the microscopy paragraph's restructure. Same species as R4's undisclosed Fig. 1 (R2) | R0 §2(f) · R2-L-d · DA (commit list) — three |
| **L7** | Not-applied lists | **Still not exhaustive** (fourth round — R3): item 14 (six vectors) on no list; item 21's "42 % less"; item 24; K13's "(one was measured)"; item 18 claimed instead of listed | R0-N25 · R1-L13/NEW-23d · R2 · R3-L12 · DA — **all five** on item 14 |
| **L8** | Round-3 preamble "No false statement was found in the Round-2 letter" | **Closed.** R0, R2 and DA quote L85 verbatim as amended: "No false change description was found in the Round-2 letter; one numeric clause ('within two standard errors') was false and is corrected". R1-L12 reads it as uncorrected; three verbatim quotations carry it (**B7**) | R1-L12 vs R0 item 16 · R2-N4-5 · DA errata 5 row |
| **L9** | Round-2 closing "Abstract 258 words; 10 pages" | Historical; now 255 / 11. Harmless, third round stale | R0 §2(g) · R2-L-f · DA §2 |

**Editorial note.** Round 2: four false change descriptions. Round 3: one false clause, three overstatements. Round 4: one stale row, one omission, one under-claim, one undisclosed figure. **Round 5: one false change description on an OPTIONAL item, one false reason, one miscount, one missing pointer, two undisclosed passes.** R3: "the letter remains a usable instrument"; R0: "both a grep away from detection, neither touching a number". Because no reviewer is re-engaged, the letter's role is the record: L1–L7 go into one addendum paragraph with the errata (SHOULD 12 below). The panel notes the species — "described the intended edit rather than the one made" — is the same one that produced "as built" in the manuscript (Q1), and that the fix for both is the same: describe the file, not the plan.

---

## 5. Devil's Advocate adjudication

DA raised **no new CRITICAL**, opens with "Nothing blocks acceptance", discharges four of its five R4 errata and records the fifth as declared. Every item DA asks the editor to require, and every should-fix, adjudicated visibly.

| DA item | DA's R5 status | Corroboration / contradiction | **Adjudication** |
|---|---|---|---|
| **Erratum 1** — "as built" on the 80 MB mirror; the build primes a sixth blob with `int64` masks, ≈200 MiB | Require: delete "as built" or print ≈200 MB | **Six vectors corroborated by R1 (row 11), R2 (N5-1, rule (a)), R3 (NEW-2) — each read `gateway.py:1735` independently.** The mask/container count is DA-only and uncomputed ("torch was not available locally"); R1, R2, R3 compute 96 MiB. R0 records the item as undeclared (N25) | **UPHELD as MUST-FIX (Q1)** on rule (a): a stated as-built quantity contradicted by the authors' own code, four independent readings, and an R4-docketed item neither applied nor declared and then made worse (the K2 species). **DA's 200 MB is not adopted** (S-1): the panel prescribes the relabel that is exact under every count and will not supply a number it has not gated. Conflict **B1**. |
| **Erratum 2 (a)** — "about 30 GB free" is the RAM total; the authors' own note said ~7 GB | Require: "32 GB board (about 30 GB of RAM)" | R1 row 12 corroborates the column is total ("~29 GB free; acceptable"); R0 and R3 accept `REPORT.md:41` as the source. The ~7 GB design-note is single-reviewer, a risk line dated 2026-08-12, not a measurement, and unsettleable | **DOWNGRADED to SHOULD-FIX (Q3)** — the word swap DA proposes is adopted because it is exact and free; the claim that the true base was ~7 GB is recorded, not upheld. Conflict **B3**. |
| **Erratum 2 (b)** — "about 30" is 33.7; implied 3.3× vs the paper's 3.0× | Require: "about 34" | R0 (nit), R1 (loose), R3 (conservative ✓) — all four see 33.7 | **SHOULD-FIX (Q4).** "About" covers 26.67→27 and 9.86→9.9 in the panel's own precedents; 33.7→30 is wider and the comparison reads 10 % better than the paper's own ratio, so it is recommended, not required. |
| **Erratum 2 (c)** — "room for roughly 100" beside three 256 MiB stores with whole-request eviction | Require: "(a derived projection; the prototype caps each store at 256 MB)" | R1-NEW-21 (minor), R3-NEW-1 (SHOULD, "would page someone at 3 a.m.") — same mechanism, same code lines; both say the sentence is not false | **DOWNGRADED to SHOULD-FIX, first-ranked (Q2).** Rule (a) is not met — the sentence is memory arithmetic labelled "a derived projection" one sentence earlier (DA's own concession) — and rule (c) is not met (one of three blocks). It is the S-1 species the panel graded SHOULD in R4 and R3 grades it identically. Conflict **B2**. |
| **Erratum 3** — "JetPack 6.1" for a fleet with `ao-1` at R36.4.3 and Nanos in MAXN SUPER | Require: "6.1/6.2 (L4T R36.4.0–36.4.3)" | **R0-N18 and R1-NEW-20 verify from `PHASES.md:1995` independently**; R0 names it "the only candidate" under rule (a). R3 accepted "R36.4 covers both"; R2 did not check the log. The MAXN-SUPER ⇒ 6.2 inference is release-note knowledge (R1's caveat), not needed for the ruling | **UPHELD as MUST-FIX (Q6)** on rule (a) from the repository fact alone. R1's argument that it is "of the size the panel has been grading SHOULD" is weighed and declined: 9.8 and ±26 were panel-supplied numbers covered by "about"; this is an author-supplied version label with no hedge, and the rule is not size-graded. Conflict **B6**. |
| **Erratum 4** — Zhang (joint) "show … must be solved together" overclaims; the method is two-step | Require: "argue … coupled … one formulation" | R0: "a shade stronger … acceptable"; R2: "'must' is the authors' inference … safer verb"; R3 checked at title level only | **DOWNGRADED to SHOULD-FIX (Q13).** A characterisation of a cited paper is not "the authors' own files, code or logs"; rule (a) does not reach it. Adopted with R2's verb and R0's abstract quote rather than DA's "one formulation", which DA's own two-step reading undercuts. Conflict **B4**. |
| **Erratum 5** — letter: strike "fetches …"; IoT-J 7; "unverified"; §III-A pointer | Require | All five find L1 and L3; four find L2 and L4 | **SHOULD-FIX (§4, SHOULD 12).** No reviewer round follows; the letter is the record. |
| **Should-fix: recovery-time transient (608 + 224 MiB)** | should | Single reviewer; not contradicted; R3's compute caveat (S-3) is the adjacent ask | **OPTIONAL (S-2).** Runs against the authors; recommended if the paragraph is touched. |
| **Should-fix: LAN rate from `network_profile.bandwidth_bps`; `ao-2` mode; artifact statement** | should | R1-NEW-24a/b, R3 §3, R0 §4 ask for all three; R3 contends the probe is "not a link rate" | **SHOULD (Q7, Q8, Q9)** — the probe may be printed *as a probe*; `ao-2` in the closed-testbed form. Conflicts **B5**, **B9**. |
| **Should-fix: CASIT "run on site"; "homes and buildings" uncited** | should | R0 §3.2 checked CASIT's clause and found it accurate; R0 and R3 also flag the uncited example | **OPTIONAL (S-5, Q14).** |
| **Should-fix: Sun "failed" → "failing"; narrow the "after placement" generalisation** | should | R0 §3.2, R2-N5-3 (both); R3 §4 (Ma) | **SHOULD (Q11, Q12).** |
| **Should-fix: K13's "(one was measured)"; S-1's eight-device bound; "42 % less"** | should | R3 rec. 6 / R2 (K13); all five (S-1); four (42 %) | **SHOULD (Q18), SHOULD (Q17), OPTIONAL (Q20).** |
| **C7 residual** — three sequential procedures; no retrying baseline | UNCHANGED; standing MAJOR-not-blocking | R2 Fairness row: "the single-shot cost-first baseline (standing MAJOR-not-blocking, unrunnable)" | **Standing MAJOR, not blocking, unchanged.** No testbed can settle it; DA does not re-open it. |
| **§5 "strongest remaining counter-argument"** — "every number in [the sizing paragraph] that is not a multiplication is unsupported or wrong in the paper's own terms"; the transcribe-don't-derive pattern "has moved from reviewer-supplied numbers to author-supplied ones" | — | R1: 14 of 16 numbers reproduce exactly, one loose, one wrong qualifier; R0: 17 re-derived, none fail; R3: "every arithmetic in §IV-C reproduces" | **Accepted in part.** "As built" is wrong (four reviewers) and "free" is a label on a total (R1 corroborates); "about 30" is loose; the caps are an omission. But the ~7 GB base and the ≈200 MB mirror are DA-only and unmeasured, so "unsupported or wrong" overstates by two; every multiplication and both bounds reproduce under every reading. The panel adopts DA's *instruction* — any new paragraph carrying a number is re-derived from the file it cites before typesetting — and records that this paragraph is the case study. |

**Summary.** Two DA errata upheld MUST (1, 3), both on rule (a) with two or three independent corroborations from the authors' code and log; erratum 2 split into three SHOULDs; erratum 4 downgraded to SHOULD as outside rule (a)'s reach; erratum 5 SHOULD (letter). No CRITICAL raised. DA's five R4 errata are discharged or declared, corroborating the R3/R4 note that its verification is actually performed — this round including a re-derivation of every $P{=}32$ mean to three decimals.

---

## 6. Decision

### **ACCEPT WITH EDITOR-VERIFIED ERRATA** — two MUST-FIX items, each one phrase, both in paragraphs that did not exist at the Round-4 decision; no further reviewer round.

**Rationale.**

*The panel has converged.* Five of five recommend acceptance; four write "Accept" outright with items for proof and the fifth "accept after editor-verified errata"; none asks for another external review; the DA opens with "Nothing blocks acceptance." **Every Round-4 MUST-FIX is applied and verified in the file by every reviewer independently** — and, for the first time in five rounds, verified *at every site*: R1, R2 and R3 each grepped every erratum string across all six section files and found no surviving twin. Both R1 conditions, R2's clause, R3's erratum and all five DA errata from Round 4 are discharged or declared. Nine of eighteen scored dimensions moved up and none down; the two ceilings the panel had named since Round 1 as movable by desk work alone — R0's Fit and R3's Deployability — moved on the paragraphs the lifted page bought, and R0 records the bibliographic-fit objection carried for four rounds as "answered on the merits".

*What holds this short of an unconditional Accept is two verified factual errors in new text, and the panel's own rule.* Under rule (a) — which produced MUST-FIX K1 and K2 last round for defects of exactly this size — a stated quantity contradicted by the authors' own code and a version label contradicted by the authors' own log are MUST regardless of consequence. **MUST 1** (Q1): "80 MB of mirror **as built**" is the five-vector accounting; the coordinator primes a sixth vector per position (`gateway.py:1735`, read independently by R1, R2, R3 and DA). It is the R4 SHOULD 14 that was neither applied nor declared and was then contradicted by a new sentence — the K2 "made worse" species — and R2 invokes rule (a) for it by name. **MUST 2** (Q6): "The workers run JetPack 6.1" where `PHASES.md:1995` records `ao-1` at L4T R36.4.3 (JetPack 6.2) — R0, R1 and DA from the same line; R0 calls it "the only candidate" under rule (a). Neither touches a measurement, a derived bound, or a headline; each is verifiable by the editor against one repository line; each is one phrase.

*Why not plain Accept.* R0 and R2 each recommend "Accept" while naming one rule-(a) item for proof; R1 argues the two slips are "of the size the panel has been grading SHOULD". The panel declines to grade its rule by size: K1 was six words and K2 was a deletion, and both were MUST because they were *verified false against the authors' own files*. Waving two such items into unverified production corrections would be inconsistent with four rounds of the panel's own practice, and the editor's check costs one grep and one log line each.

*Why not Minor Revision.* The Round-3 yardstick: Minor when four statements are contradicted by the authors' own files plus self-created contradictions. This round: two contradicted phrases, both in a paragraph and a sentence added after the decision, and nothing a reviewer as opposed to an editor needs to see. R2: "I would apply it at proof … it does not need a reviewer." R1: "I do not need to see the manuscript again." R3: "I would not convene a panel for any of them." R0: "I would not hold a paper for it; I ask that it be corrected at proof."

*Against that, recorded.* The residue has migrated (DA §5): the measured claims have been stable and reproducible since Round 2, and what remains is prose written after the last decision. Two patterns survive in reduced form. The propagation pattern R1 named in Round 4 did not recur for any MUST string, but K9's fix landed nineteen lines downstream of its target (Q16) and SHOULD 14 was skipped, undeclared, and then contradicted by new text (Q1). And the letter's species — "described the intended edit rather than the one made" (R0) — produced its first false change description in two rounds (L1) and is the same habit that produced "as built". The editor should ask, for the two MUSTs and SHOULD 12, that the authors describe the file and not the plan.

**Verification instruction for the editor.** MUST 1: `grep -n "as built" sections/evaluation.tex` → the phrase must not qualify the 80 MB mirror; confirm the relabel names Table I's five-vector accounting; the code fact is `radp/coordinator/gateway.py:1729–1735` (`self.cache.put(request_id, first_key, position, blob)` for the chain-head stage). MUST 2: `grep -n "JetPack 6.1" sections/evaluation.tex` → must read "6.1–6.2" (or per board) with "L4T R36.4.0–36.4.3"; the log fact is `PHASES.md:1995` (`ao-1` R36.4.3) beside `PHASES.md:1981` (`ao-2` R36.4.0). Neither needs the testbed. No reviewer needs to be re-engaged.

**Ceilings (recorded, not blocking).** Significance 5 (R0) — the sizing paragraph is "the first place the paper says *when* its advantage binds", still a derived projection, W4 stands; Novelty 6 (R2) — §II-B positions, adds no mechanism; Motivation–evaluation 6 (R3) — a link-varying experiment, unreachable by construction; Experimental design 6 and Statistical validity 6 (R1) — "nothing measured, nothing could be". Fit 7 (R0), held below 8 by a 19-token wired-LAN workload and no traffic or energy — testbed-bound. All five call the resulting size publishable for IoT-J.

---

## 7. Errata list R5

Numbered, deduplicated, conflicts resolved. Every item is a phrase the authors already hold; none needs the testbed or a page.

### MUST-FIX (editor-verified before the version of record)

1. **`evaluation.tex` L246–247** — "80\,MB of mirror as built" → "80\,MB of mirror by Table~I's five-vector accounting (the prototype also primes the head's input, 8\,kB per token)"; scope L230–231's 3.0×/42 % to the five shipped vectors or recompute 2.9×/41 %. *(Q1 — R2-N5-1, R1-NEW-22, R3-NEW-2, DA erratum 1, R0-N25; conflict B1)*
2. **`evaluation.tex` L44–45** — "JetPack 6.1 (L4T R36.4," → "JetPack 6.1–6.2 (L4T R36.4.0–36.4.3," or per board, per `PHASES.md:1981, 1995`. *(Q6 — R0-N18, R1-NEW-20, DA erratum 3; conflict B6)*

### SHOULD-FIX (same pass; no page)

3. **`evaluation.tex` L249–251** — append "(a derived projection and a memory bound: the prototype's parity and mirror stores default to 256\,MB each and evict whole requests, so the bound assumes they are raised; coordinator compute under concurrency was not measured)". *(Q2 + S-3 — R1-NEW-21, R3-NEW-1/NEW-3, DA erratum 2c; conflict B2)*
4. **`evaluation.tex` L249** — "has about 30\,GB free" → "is a 32\,GB board with about 30\,GB of RAM". *(Q3 — DA erratum 2a, R1 row 12; conflict B3)*
5. **`evaluation.tex` L250** — "about 30" → "about 34" (or "about a third as many"). *(Q4 — R0-N19, R1 row 13, R3 §2, DA erratum 2b)*
6. **`evaluation.tex` L48–49** — "`ao-2` as flashed" → the mode if the authors hold a log, else "`ao-2` at its flashed default (power mode not recorded; the factory default is 30\,W)". *(Q7 — R1-NEW-24a, R3-NEW-4, DA §3.2, R0-N18; conflict B9)*
7. **`evaluation.tex` L50** — "(the scheduler's gRPC probe measured 16–24\,Mbit/s between worker pairs)" labelled as a probe, or the nominal link rate if held; plus one artifact/code-availability sentence. *(Q8, Q9 — R1-NEW-24b, R3 §3, DA §3.2, R0 §4; conflict B5)*
8. **`related.tex` L27–28, L32–34, L34–36** — Sun: "away from nodes identified as failing"; "where a substitute node exists, it is chosen after…"; Zhang (joint): "argue that the two placement decisions are coupled and solve them in one combined process". *(Q11–Q13 — R0 §3.2, R2-N5-3, R3 §4, DA erratum 4; conflict B4)*
9. **`discussion.tex` L68–69** — "the head stage is outside the parity guarantee and the coordinator is unprotected". *(Q16 — R0-N24, R2-N5-5, R3-NEW-6; K9's twin)*
10. **`design.tex` L225** — "the prototype searches up to eight devices (109\,592 orderings) and falls back to heartbeat order above that". *(Q17 — R1 S-1, R2-L-c, R3 S-1, R0-N27, DA; all five)*
11. **`evaluation.tex` L232** — "(one was measured)" after "any number". *(Q18 — R3 rec. 6, DA K13 row, R2)*
12. **Letter addendum** — strike "and fetches surviving columns at recovery"; drop "pointing to §IV-C's accounting" or add the `\ref`; "unverified" → applied or "by choice"; "IoT-J entries 1 → 7"; "§II-E" → "§II-F"; disclose the introduction reframe, the dropped `zhang2026finetuning`, the abstract restructure and the split pass; list item 14 and the other unapplied items. *(§4 L1–L7 — all five; conflicts B7, B8)*

### OPTIONAL

13. `evaluation.tex` L211–213 — "(40\,kB per token; 2.3\,s at $P{=}32$)" so "at $P{=}32$" no longer governs the $P<10$ clause. *(Q19 — R0-N23, R1-NEW-25a, R2)*
14. `introduction.tex` L3–4 — cite or cut "assistants in homes and buildings"; "LLM agents over IoT devices" without binding CASIT to "run on site". *(Q14, S-5 — R0, R3, DA §3.4)*
15. `evaluation.tex` L231 "42\% less"; L245 define $N_{\mathrm{ctx}}$; L177–178 "coordinator KV state"; `related.tex` "P. Zhang" / "X. Zhang". *(Q20 — R0, R2, R3, DA; Q5 — R0-N19; S-4 — R2-N5-2; Q15 — R0-N26)*
16. `evaluation.tex` §IV-C — "a recovery additionally holds the surviving columns transiently (about 0.8\,GB at 7B)". *(S-2 — DA §3.1(v))*
17. `evaluation.tex` L46–47 — grpcio/protobuf as installed; the JetPack 5 minor on the Xavier. *(Q10 — R3 §3)*
18. `evaluation.tex` L402–403 — full stop before "Reconfiguration grows". *(S-6 — R1-NEW-25b)*
19. Repository — delete `background.tex`; build once from the repository and commit `main.log`; catalogue the five §II-B PDFs in `paper/refs/` per the repository's own convention. *(Q22 — R0, R2, R3; Q21 — R0-N28, R1, R3; Q23 — R1, R3)*
20. Fig. 1 — darker backup labels; a caption word that stage $n$'s column falls to replay. *(S-8 — R0 S-11, DA S-8; carried)*

---

## 8. The five-round trajectory

Round 1 was a unanimous Major: eight DA CRITICALs, a Pareto sentence that credited a competitor 0 kB/tok, a footprint that omitted its backup weights, and no figure of the system. Round 2 stayed Major on the letter as much as the paper — four false change descriptions, two stale figures, 163 ms and 183 ms fighting across sections. Round 3 reached Minor when the letter retracted its falsehoods by name, fifteen MUSTs became nine word-level ones, and the DA recomputed every derived number and found one wrong. Round 4 reached Accept-with-errata when the nine were applied, the CRITICAL discharged, the figure the paper had lacked since Round 1 added and verified against the code, and the residue was five lines — one of which the panel had written. This round the five are applied at every site, the page the authors bought went to the three paragraphs four rounds of reviewers had asked for, the figure was redrawn beyond what was required, nine dimensions moved and none fell, and the two ceilings that were movable by desk work — journal fit and deployability — moved. The residue is two phrases in new text, both verified against the authors' own repository, both one grep for the editor. What the five rounds show is a class of defect shrinking monotonically — false letter statements, then false scope claims, then stale rows, then one transcribed parenthetical, now one mislabelled quantity and one version string — and, unchanged throughout, a revision that moved against the authors' interest wherever it mattered: the backup weights charged, both ratios printed, the Reconfigure boot failures left in the authors' own disfavour, the second AGX disclosed as not configured like the first. DA's closing observation is the right one and the panel adopts it as the only instruction that survives this decision: the measured claims have been stable and reproducible since Round 2, and any number written after the last measurement must be re-derived from the file it cites before it is typeset. **With MUST 1 and MUST 2 applied and editor-verified, the panel considers the paper ready for the version of record.**

---

## Reviewer conflicts arbitrated

**B1 — Q1's fix: 96 MB (R1, R3) vs relabel-and-keep-80 (R2's second form) vs delete "as built" or print ≈200 MB (DA).** Four reviewers verify six vectors from `gateway.py:1735`; only DA adds the `int64` mask and container, and DA reports the container uncomputed. **Resolution: relabel.** "By Table I's five-vector accounting (the prototype also primes the head's input, 8 kB per token)" is true whether the build holds 96 or 200 MiB, and the panel — having supplied 493, 9.8 and ±26 in earlier rounds — will not print a number it has not gated. If the authors hold a per-request RSS delta, they may print it; the panel does not ask.

**B2 — Q2 (256 MiB caps): MUST (DA) vs SHOULD (R1, R3).** All three read the same code lines and agree on the mechanism; R1 and R3 both say the sentence is not false; DA itself notes the preceding sentence labels the figure "a derived projection". **Resolution: SHOULD, first-ranked.** Rule (a) requires a false or internally inconsistent statement; a true memory bound with an adjacent projection label is an omission — the S-1 species, graded SHOULD in Round 4 and by R3 this round "the same". Rule (c) needs three blocking; one blocks. R3's "would page someone at 3 a.m." is why it is ranked first among the SHOULDs and folded into the same clause as S-3.

**B3 — Q3 ("about 30 GB free"): DA vs R0, R1, R3.** R1's own reading corroborates that `REPORT.md:41`'s column is `MemTotal` ("total as `free` reports it") and still grades it acceptable at ~29 GB free; R0 and R3 accept the source. DA's ~7 GB comes from a design-note risk line, not a measurement, and no saved profile covers `ax-1`. **Resolution: SHOULD, DA's wording.** "A 32 GB board with about 30 GB of RAM" is exact under every reading and costs nothing; the ~7 GB claim is recorded and not upheld, because it cannot be verified and the testbed is closed.

**B4 — Q13 (Zhang "show … must"): require (DA) vs acceptable (R0) vs safer verb (R2).** **Resolution: SHOULD.** Rule (a) reaches "the authors' own files, code or logs"; a characterisation of a cited abstract is outside it, and R0 — who read the abstract — grades "must" a shade strong but acceptable. The fix uses R2's verb and R0's abstract quote ("a complete process combining"); DA's "in one formulation" is avoided because DA's own reading says the method is two-step.

**B5 — Q8 (LAN rate): "nothing to print" (R3) vs "the rate was measured" (DA).** Both are right: the saved `network_profile.bandwidth_bps` is an application-level gRPC probe (1 MiB payloads), not a link rate. **Resolution: SHOULD, labelled.** Print the probe *as the scheduler's probe*, or the nominal link rate if the authors hold it; do not print the probe as a link rate.

**B6 — Q6 (JetPack): MUST (DA; R0 "the only rule-(a) candidate") vs "SHOULD-sized" (R1) vs accepted (R3, R2).** R3 read `PHASES.md:1995` and accepted "R36.4" as covering both patch levels — true of the L4T clause, silent on the JetPack clause; R2 checked release-pair consistency only. **Resolution: MUST.** Three reviewers verify the contradiction from the repository line; R1's size argument is declined because the panel's R4 SHOULD-graded numbers (9.8, ±26) were panel-supplied and hedged by "about", while this is an author-supplied label with no hedge. The MAXN-SUPER ⇒ 6.2 inference is recorded as release-note knowledge (R1's caveat) and is not part of the ruling.

**B7 — L8 (Round-3 preamble): "still uncorrected" (R1-L12) vs corrected (R0 item 16, R2-N4-5, DA errata-5 row).** Three reviewers quote L85 verbatim in its amended form ("… one numeric clause ('within two standard errors') was false and is corrected"). **Resolution: closed.** R1's L12 is overtaken by the quotation.

**B8 — L4 (IoT-J count): 7 (R0, R1, R3, DA) vs 6 (R2).** R2 counted EdgeShard plus the five §II-B entries and omitted `zhong2024casit`, which enters through the introduction; R0 lists all seven by key. **Resolution: 7.** The letter under-claims in the authors' own favour.

**B9 — Q7 (`ao-2` mode): "read it and print it" (R3) vs the closed testbed.** `nvpmodel -q` cannot be run. **Resolution:** the closed-testbed form — "power mode not recorded; the factory default is 30 W" — unless the authors hold a log line the panel did not find; R1's "say the mode or say 'not recorded'" is the form adopted.

**B10 — Accept (R0, R1, R2, R3) vs accept-after-errata (DA).** All five name at least one item for the editor; two (R0, R2) invoke rule (a) for one item each while writing "Accept". **Resolution: Accept with editor-verified errata**, for consistency with the panel's own Round-4 practice (§6): the label follows the rule, not the size.

---

*Prepared by the Editorial Synthesizer. Every item above traces to a named Round-5 reviewer report and item id. The manuscript, its sources, code, logs and result files were not read or modified in producing this decision; all quotations, line numbers and file references are taken from the five reviewer reports and the Round-4 decision.*
