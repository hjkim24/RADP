# Editorial Decision — Round 4 (Third Verification) — KV-CARE (IEEE IoT-J)

**Date:** 2026-09-04 · **R1 decision:** Major (5/5) · **R2 decision:** Major · **R3 decision:** Minor (5/5 Minor-or-better, one round, editor-verified)
**Basis:** five Round-4 verification re-reviews (`R0/R1/R2/R3/DA_round4.md`) checked against `EDITORIAL_DECISION_R3.md` §7, roadmap items 1–36 (MUST 1–9, SHOULD 10–26, OPTIONAL 27–36).
**Standing constraints honoured:** the testbed is closed — nothing below asks for a measurement. The authors hold the paper at **10 pp. deliberately**; declared page-budget declines are graded as disclosed absences. A new **Fig. 1** (architecture) was added to §III-A after the roadmap commit; it is assessed in §5.
**Rule for MUST-FIX this round** — the Round-3 rule, unchanged: an item is MUST-FIX only if (a) it is a **verified factual or internal-consistency error** against the authors' own files, code or logs, or (b) it is an **upheld Devil's Advocate CRITICAL**, or (c) **three or more reviewers name it blocking**. One clarification the round required: **an R3 MUST-FIX clause that was neither applied nor declared carries over** (it was already MUST); a clause whose *purpose* the panel finds discharged by the half that was applied is closed explicitly (see conflict **B9**).

---

## 1. Panel table

| Reviewer | R3 rec | **R4 rec** | Counts this round | Items still named blocking | Score movement (R1 → R2 → R3 → **R4**) |
|---|---|---|---|---|---|
| **R0** Journal-Fit | Minor, unconditional | **Accept, subject to editor-verified corrections — no further review round** | 13 roadmap rows citing R0: A 6 · P 4 · N 2 (declared/optional) · W 1 (*R0's own number*). Own R3 items N9–N13/M1–M3: A 6 · N 1 · **withdrawn 1 (N12)**. Panel MUST 1–9 checked independently: A 6 · P 2 (items 2, 5 second clauses) · A-by-disclosure 1 (item 7). 14 numbers re-derived; 1 fails (its own). | N14 (493 ms), N16 (offline clause), N15 (Pareto boundary), letter §2a–d | Fit 6→6→6→**6** · Originality 6→7→7→**7** · Significance 5→5→5→**5** · **Clarity 6→7→8→9** · Claims-vs-evidence 4→6→8→**8** |
| **R1** Methodology | Accept subject to minor editorial corrections (2 conditions) | **Accept, subject to two one-clause corrections — no further external review** | 26 roadmap rows: A 15 · P 3 · N 7 (6 declared, **J8 undeclared**) · W 1 (J15). **Both R3 conditions (NEW-11, NEW-12) ADDRESSED.** 16 derived numbers recomputed: 14 ✓ · 1 own rounding (±26) · 1 ✗ (493). | NEW-13 (§V-A "backup room"), NEW-14 (493 ms) | Exp. design 5→6→6→**6** · **Stat. validity 4→4→5→6** · Metric defs 6→7→8→**8** · **Reproducibility 3→4→5→6** · Limitations 6→7→9→**9** |
| **R2** Domain | Minor, unconditional ("I performed the check") | **Accept, subject to one editor-verified clause** | All three R3 blocking items (N3-1, N3-2, N3-3) ADDRESSED; N3-4/5/6 closed; **C-2 withdrawn as a residual**; fatal triggers F1–F6 none fired; new N4-1…N4-5. | N4-1 (§V-A "backup room") | Literature 5→6→7→**7** · **Tech. soundness 5→6→7→8** · **Novelty 5→5→5→6** (first movement in four rounds) · Fairness 5→7→8→**8** |
| **R3** IoT practitioner | Minor ("fix the three errata and I would accept") | **Accept, subject to one editor-verified erratum — no further review round** | 20 graded rows: A 12 · P 4 (J7, J10, J12, S4) · N 2 (J8 mis-filed; item 32 optional) · D 5 · **W 0**. All three R3 blocking errata (NEW-A, NEW-4, NEW-B) ADDRESSED. | NEW-1 (§V-A "backup room") | Deployability 4→5→6→**6** · Motivation–eval 3→5→6→**6** · **Practical impact 5→6→6→7** · **Clarity for practitioners 6→6→6→7** |
| **DA** Devil's Advocate | Minor, conditional on NEW-3 | **"Nothing blocks acceptance" — accept after editor-verified errata** | **NEW-3 CRITICAL withdrawn** ("solely" gone; replacement supportable). NEW-1/2/4/5/6 A · NEW-7 closed (A1) · **NEW-8 MADE WORSE** (493) · NEW-9 trivial N. C1-residual, C8, N7, M9 addressed and withdrawn. **0 new CRITICAL.** Every derived number reproduces except 493. | Errata 1 (493), 2 (Pareto), 3 (Fig. 1 caption + grey fill), 4 (Table I reasons); letter | no rubric |

**Panel arithmetic: 5 of 5 recommend Accept with editor-verified corrections; 0 Minor; 0 Major; 0 conditional. No reviewer asks for another external round.** Across the 18 scored dimensions, **7 moved up and none moved down**; the first-ever movement on R2's Novelty (5→6) came from a sentence the authors wrote and did not claim credit for (`related.tex` L69–71; R2 §2, R1 item 14, DA §2).

**What the panel unanimously records as delivered** (all five, independently, against the files): the three R3 items with four-plus reviewers blocking — the Conclusion's *weights/load* clause (J1 first clause; `discussion.tex` L129–130 verbatim the abstract's), R's domain (J2; the typed `R:S→D` removed, the A3 rationale "the head's backup serves replay" present and **verified true of `gateway.py` L1183–1186 by R2 and DA**), and the DejaVu slope now "about two standard errors" (J3; 2.038 SE reproduced by R0, R1, DA) — plus the DA CRITICAL "solely" deleted from §IV-E (S1; R1 additionally verified the SLO cap did not bind at 7B, so both pooled causes are Eq. (mem)), footnote (b) recredited (J4), both non-reproducing statistics fixed (S5/S6; R1 reproduces 0.027–0.231 s SD and 1.73–1.94), the solve time and enumeration formula printed (J5; 122 s at `REPORT.md:812`, $\sum P(|\mathcal D|,k)$ = 1 950 at six), the non-uniform ladder stated (J13), "two to four" (J11), the §I throughput premise (J14), the MDE ±26 % volunteered (S8), the 12.4 GiB charged in §IV-E (S12), the out-of-scope sentence deleted (S11), and the retained-input accounting **stated with both ratios, 3.0×/42 % printed beside 3.7×/46 %** (S3; R1 verified the behavioural claim in `worker/server.py:439–451`). Four reviewers again note the revision moved **against the authors' interest** where it mattered (R0 §6, R1 §6, R2 §6, R3 §6). A correct architecture figure was added at no page (§5).

---

## 2. Consensus residuals (≥2 reviewers)

| # | Defect (one line) | Location as quoted by reviewers | Raised by | Minimal fix |
|---|---|---|---|---|
| **K1** | **MUST-FIX 1 propagated to §IV-E and not to its Discussion twin** — the sole-cause attribution the panel upheld as CRITICAL survives in different words | `discussion.tex` L56–57 "the live solve rejected a quarter of its candidate placements **for want of backup room**" — **byte-identical to the pre-Round-3 snapshot** (R2). §IV-E L308–310 now reads "rejected as infeasible under Eq. (mem)". R1, R2, R3 each re-read `scheduler.py:403–416`: one `except (NoFeasibleSolutionError, NoRecoveryError)`, a silent `continue` on non-finite objectives, one `feasible_count`; the handler's own comment concedes the pooling (R2) | **R1-NEW-13 (blocking)** · **R2-N4-1 (blocking)** · **R3-NEW-1 (blocking)** | Six words: "as infeasible under Eq.~\ref{eq:mem}, the backup reservation among the causes" (R3's form; exact under every reading — conflict **B1**). Fourth consecutive round with a fix at the cited line and not its twin (R1). |
| **K2** | **The one new false number, and the panel supplied it.** The parenthetical added for J15 pools 15 dead $k{=}2$ stub rows | `evaluation.tex` L40–41 "500 ms at 7B (**493 ms pooled over the repeated trials**)". 493.4 = median over **all 75 rows** of `b1_ft_fleet_7b_rep3.json`, incl. 15 `raid6` rows with `fired:false, sequence_match:false`, 75–91 ms/token. Over the 60 rows that pass the paper's own gates: **499.6 ms** (R0 499.65 · R1 499.6 · DA 499.6/499.1/498.0). The figure generator uses the gated median and prints 500 (`make_recovery_latency.py:61–63`, R1). Origin: R0-N12 / roadmap 20 printed "493.4–499.65"; **R0 withdraws N12 as its own error** | **R0-N14 (blocking; "mine")** · **R1-NEW-14 (blocking)** · **DA §3 ✗ / errata 1** (NEW-8 MADE WORSE) | Delete the eight words (all three), or "(499.6 ms over the 60 gated trials)" (R1, DA). Nothing downstream uses 493 — R0 and R1 both checked (31 steps, 2.1–3.2×, 0.89 all use 500). Conflict **B7**. |
| **K3** | **R3 MUST-FIX 2's second clause not applied and not declared** — the Conclusion's feasibility sentence carries no evidence-class label where the abstract does | `discussion.tex` L138–141 "with backups confined to pipeline devices, joint placement remains feasible at every per-device memory cap at which the model fits"; `grep -n offline discussion.tex` → no hit (R0, R2, DA); abstract L20 has "in an offline placement analysis". Not on any not-applied list (R0-2c, R1, R2-L-c, R3-L14) | **R0-N16 (blocking)** · R1-NEW-19 · R2 MUST-2 row · R3-NEW-4 ("my MAJOR-6 second ask, third round") · DA NEW-4 row (all 5) | Four words, the abstract's own. Carries over as MUST under the clarified rule. R2's "misleads no one" is noted (conflict **B9**). |
| **K4** | **J7 applied by moving the threshold instead of adding the clause** — the Pareto class is now drawn 0.08 s above KV-CARE's own point and *inside* its own printed range | `evaluation.tex` L206–207 "among the families that recover **within three decode steps**, \sys{} retains the least state" (was "within two seconds"). 3 × 500 ms = 1.50 s; KV-CARE $k{=}1$ at $P{=}32$ = 1.42 s (inside by 0.08 s); Petals 2.31 s = 4.6 steps. But the paper prints "2.1–3.2×" at L157 and "2.1–3.9× … both victims" at L188; the $P{=}4$ worst trial is 3.17 steps and the secondary victim 3.92 (R1, DA from `rep3.json` / `7b_mid.json`). The requested clause ("Petals is the low-state endpoint at 40 kB/tok and 2.3 s") does not appear; the direction of Petals' inequality is still never stated (R2, R3) | **R0-N15 (blocking)** · R1-NEW-15 · R2 SHOULD-12 row (PARTIALLY) · R3-NEW-2 · **DA errata 2** ("the second time this boundary has been redrawn to admit exactly one family") (all 5) | Add the clause four reviewers asked for last round; if a class is kept, scope it "at $P{=}32$" (DA) or draw it at a round number not 0.08 s from one's own result (R0). Conflict **B2**. |
| **K5** | **Fig. 1's caption states the design need where §IV-C now states the prototype's behaviour** — the S3 two-ways problem, resolved in §IV-C, re-enters in the figure a reader sees first | Caption (`design.tex` L8–11) "retains **the interrupted position's** input" vs `evaluation.tex` L219 "the prototype retains this per-position input mirror **for every family**"; code keeps `dict[position, bytes]` per stage, 256 MB, whole-request eviction (DA, `activation_cache.py:15–43`; R1, `worker/server.py:439–451`). Same tension at `design.tex` L31–32 "the current position" (R0, R3) | R0 §3(i) · **R1-NEW-18** · R2 §3 (cosmetic) · R3 §3(4)/§1c · **DA §4(1) / errata 3** (all 5) | Caption: "retains the stage inputs by position (the interrupted position's suffices for the parity path)" (R1/DA wording). One cross-reference clause at §III-A L31–32 (R3 §1c) closes the same tension upstream. |
| **K6** | Fig. 1 draws $R$ as a permutation — one backup per worker — where the measured 7B mapping concentrates backups | `make_architecture.py` L15–16 `STAGES`: head→[stage n], 2→[3], 3→[head], n→[2]. Live 7B mapping (`REPORT.md:818`): `on-5` and `on-1` each host **two** backups; `on-2`, `ao-2` **none** — the report's own words "backup이 on-5·on-1에 몰림". That concentration is the mechanism behind §IV-E's coupling result and the 350M 30.3 s intercept (R3, DA). §IV-A L29–30 says the deployment is "of the form in Fig. 1" | R1 §3(1) (minor, caption-level) · **R3 §3(1) ("the one change that would most improve it")** · DA §4(2) ("false of the prototype as measured") | Caption clause "(one backup per worker as drawn; the scheduler may assign several or none)" — minimal, editor-checkable. Redrawing via the `STAGES` literal is the better fix and the generator already supports lists (R3 L70–73) — OPTIONAL. Conflict **B3**. |
| **K7** | Fig. 1's head is filled grey; no legend entry or caption clause says why | `make_architecture.py` `fc="#F2F2F2" if head`; the legend's five entries do not cover it; a reader infers "unprotected" from the missing red arrow "or does not" (R3) | R0 §3(ii) · R2 §3 ("head: no parity") · R3 §3(2) · DA §4(3) / errata 3 (4) | One legend or caption word: "shaded: outside the parity domain (the head's backup serves replay)". |
| **K8** | The coordinator — the one component with no backup and no replay — is the only unmarked box | `design.tex` L25–26 "is outside the protection domain"; the figure gives it the heaviest border and no marking; the head, which *has* a backup, is the one shaded (R3) | R3 §3(3) · DA §4(5) (2) | Shade the coordinator and let K7's legend entry cover both (R3's combined fix), or five caption words "coordinator: outside the protection domain". |
| **K9** | "Unprotected" now carries two meanings — and the head-failure replay path the new §III-A clause asserts was never exercised | `design.tex` L21 (new) "the head's backup serves replay" vs `discussion.tex` L69–70 "the head stage and the coordinator are unprotected"; the coordinator is *lost*, the head is *replayed*; no trial fails the head (R2 verified the path exists at `gateway.py` L1186; R3 notes a deployer reads "unprotected" as "lost") | R2-N4-3 · R3-NEW-3 (2) | §V-A: "the head stage is outside the parity guarantee (a head failure falls to full-prefix replay on its preloaded backup, a path we did not measure) and the coordinator is unprotected". |
| **K10** | "about 9.8 million at ten devices" is 9 864 090 — the editorial's own truncation, transcribed | `design.tex` L223; $\sum_{k=2}^{10}P(10,k)=9\,864\,090$ (R0, R1, R2, DA, and `scheduler.py:339–343`'s formula) | R0 nit · R1 row 7 · R2-N4-4 · DA M9 row (4) | "about 9.9 million". |
| **K11** | The headline "3.7× less coordinator state" is the design-need figure; by the paper's own new disclosure the coordinator-resident total is 3.0× | `abstract.tex` L17, `discussion.tex` L134 "3.7× less coordinator state"; `evaluation.tex` L219–224 "Charged to every family, the ratios become 3.0× … 42 %"; Table I note says "coordinator **KV** state". R0: the metric (L86–89) supports charging by path, so this is a **disclosed** accounting choice | R0-N17 (soft) · R1 item 7 residual · **R2-N4-2** · R3 §1c ("the word 'coordinator' points at the box whose memory is actually 3.0× smaller") (4) | One word at both sites: "3.7× less coordinator **KV** state" (R2). This is exact under either mirror count and sidesteps DA's six-vector residual (S-2 below). Conflict **B4**. |
| **K12** | Table I note (a) is now true but withholds reasons that are evidence *for* the paper — two of three discarded Reconfigure attempts were the rebuilt pipeline failing to boot | `evaluation.tex` L135 "no valid trial at $P{=}16$, 24, 32". Log `b1_ft_fleet_7b_reactive_log_20260901.json` `invalid`: 16 "survivor worker socket closed mid re-solve", 24 "**boot not ready** (… load_head died)", 32 "**boot not ready** (same regime instability)" — verified by R0, R1, R3, DA. `discussion.tex` L67–68 "Reconfigure was measured at two positions" still reads as a choice (R1) | R0 item 6 (honest short form) · R1 row 9/item 6 · R2 MUST-6 row (PARTIALLY) · R3 §1c (P) · **DA M7 row / errata 4** ("hidden by its own conservatism") (all 5) | One clause in the note or in §V-A: "three further positions were attempted; two were discarded because the reconfigured pipeline did not come up". Runs in the authors' favour. Conflict **B6**. |
| **K13** | The "tolerated failures" sentence (S9's substitute) over-states in one direction and under-states in the other | `evaluation.tex` L225–226 "Replication and the replay families tolerate **any number** of simultaneous protected-stage failures that backups can host; parity tolerates $k$." R3: "parity tolerates $k$" reads as if the $(k{+}1)$-th failure loses the request, which §III-B L50–53/L61–63 denies (falls to replay). DA: "any number" is untested for replication — `_recover_replicate` (L946ff) attributes **one** dead stage; only `parity_k == 2` has a multi-victim dispatch | R3 §1c · DA S9 row (2) | One sentence carrying both: "By construction, replication and replay tolerate any number of simultaneous protected-stage failures that backups can host (one was measured); parity restores up to $k$ without recomputation and falls to replay beyond." |
| **K14** | J8 (unit conventions) is neither applied nor on any not-applied list, while the letter says it is | Table I note L134 "kB denotes 1 024 bytes" remains the only declaration; "832 MB / 224 MB" (L234–235), "12.4 GiB" (L25, L90), Table III "4.8 GB / 23 GB" coexist. Letter appended ¶: "J8 … remain on the not-applied lists above" — **on none of the three lists** | R0 item 16/§2b · R1 item 16/L3 · R2-L-c · R3 item 16/L13 (4) | Manuscript: cosmetic, OPTIONAL. **Letter: file it** (SHOULD). |
| **K15** | `kosaian2019parity` still lacks pages (SOSP '19, 30–46); `rashmi2016eccache` now has pages, correctly no DOI | `references.bib` L330–336 | R0 item 21 · R1 row 16 · R2 SHOULD-21 row · R3 item 21 · DA N8 row (5) | One field; production office. |
| **K16** | "about ±26 %" recomputes to 26.6–26.7 — R1's own Round-3 rounding, transcribed | `evaluation.tex` L373–374; $t_{0.975,2}\cdot10.737/\sqrt3=26.67$ | R0 · R1-NEW-17 ("mine") · R2 · DA (4; all say "about" covers it) | Nit; ±27 or leave. |
| **K17** | R3 MUST-FIX 5's second clause (recovery-path fetch) not added — but the contradiction it targeted is resolved by the scoping | `evaluation.tex` L227–229 "During failure-free decoding, …" ✓; L165–166 "four RPC round trips" already carries the recovery-path traffic | R0 item 5 (P; "worth doing") · R1 item 5 (no contradiction remains) · R2 N3-3 (none) · R3-L10 (4) | **Closed as MUST** — three of four say the purpose is discharged (conflict **B9**). OPTIONAL. |
| **K18** | `background.tex` still on disk and un-`\input` | `main.tex` L93–97 | R0-W11 · R3 item 34 (D, declared) (2) | Delete the orphan; costs no page. Declared; OPTIONAL. |
| **K19** | 7B "whole fleet" membership never defined; `on-3`/`on-4` unnamed | `evaluation.tex` L317–318 "the six CUDA workers" | R0 item 31 · R1 item 17 residual (2; both optional) | OPTIONAL. |
| **K20** | The environment paragraph (J9) — declared "deferred desk work", still the top non-blocking ask of the same two reviewers | grep jetpack/l4t/nvpmodel/jetson_clocks → zero hits; `design.tex` L237 "Python with PyTorch and gRPC" | R1 item 13 ("still my top non-blocking ask") · R3 item 13 ("if a page appears at proof, item 20 first") (2) | Declared; recorded, not pressed. Both reiterate the power mode is a configuration fact the authors hold. |

---

## 3. Single-reviewer residuals worth acting on

| # | Defect | Location | Reviewer | Minimal fix |
|---|---|---|---|---|
| **S-1** | The prototype **caps the search at eight devices**; at ten it enumerates nothing — beside the sentence that prints "≈9.8 million at ten devices" | `scheduler.py:289` `max_search_devices: int = 8`; `server.py:26`; `group_vars/all.yml.example:79` "<M → skip the search, use heartbeat order" | **R1-NEW-16** | One clause after `design.tex` L223: "the prototype searches up to eight devices (109 592 orderings) and falls back to heartbeat order above that." A factual gap adjacent to a printed number; no testbed. |
| **S-2** | The prototype's mirror is **six** vectors per token, not five: the coordinator primes the *head's* input at every position, and full replay consumes it | `gateway.py:1735` `self.cache.put(request_id, first_key, position, blob)`; `_replay_through_chain` L1642–1668. At 48 kB/tok the all-family ratios are **2.9× / 41 %**, not 3.0× / 42 % | **DA S3 row** (MINOR; "the paragraph's words cover it, its numbers do not") | Either recompute §IV-C's all-family ratios on six vectors, or say "five non-head vectors (the head's primed input adds 8 kB/tok)". K11's "KV" fix keeps the headline exact either way. |
| **S-3** | §IV-E's replacement wording "infeasible under Eq. (mem)" is *mostly* right — `NoFeasibleSolutionError` is also raised at `scheduler.py:279` for ten-iteration non-convergence, and the non-finite `continue` is a third exit | `evaluation.tex` L308–310 | **DA NEW-3 row** (MINOR) · R0 item 1 soft residual (non-finite continue) — R1 row 14 holds it "precise" | Not blocking, all three. If touched: "rejected as infeasible (Eq. (mem) is the binding constraint)" (DA). K1's §V-A wording already uses the exact form. Conflict **B8**. |
| **S-4** | J15's other half — the 527 ms protection-off interval is still not named as a distinct quantity, as 183 ms was at 350M | `evaluation.tex` L367–368 "median token interval of 527 ms" | **DA errata 1 (second sentence)** · R0 item 20 (cosmetic) · R3-L9 (not claimed, not applied) | "these runs' median token interval of 527 ms" — the same two-quantity naming applied at 350M. |
| **S-5** | The letter **does not mention Fig. 1** — a new figure introduced during a verification round, renumbering every other figure | Commit `57324dd` after roadmap commit `6a4da75`; Round-2 rows now point at the wrong figures | **R2-L-d** ("the one an editor should ask about") | One line in the letter: figure added, all figures renumbered (+1). |
| **S-6** | The letter's Round-3 preamble "No false statement was found in the Round-2 letter" contradicts the letter's own row 14 twenty lines earlier ("Round 2 had written 'within two', which was false") | `response_to_reviewers_2026-09-03.md` | **R1-L1** | "One false clause (row 14) and three completeness overstatements are corrected above." |
| **S-7** | A deployer sizing the coordinator from §III/Fig. 1 under-provisions by 40 kB/tok per request; the absolute the deployer needs (152 / 264 / 456 kB/tok; +80 MiB mirror per 2 048-token request) is not printed | `evaluation.tex` §IV-C L234–236 gives only the design number "224 MB" | **R3 §1c** | One parenthetical with the as-built absolute at $k{=}1$. OPTIONAL. |
| **S-8** | Stage $n$'s column is drawn as protected identically to stage 2's; a tail victim has no downstream survivor and falls to replay | Fig. 1 red arrows uniform over $n-1$ stages; §IV-C "retain the tail stage's column, whose recovery falls back to replay"; `gateway.py` L1119–1121 | **DA §4(4)** | OPTIONAL caption clause; §IV-C already states it. |
| **S-9** | "42 % for two" lacks the word "less" that its parallel "46 % less than replication" carries | `evaluation.tex` L224 vs L214 | **R0 nit** | One word. |
| **S-10** | Pareto caption "the mean of its two measured positions" vs `make_recovery_pareto.py:6,52` `median` — identical at $n{=}2$ | Fig. 3 caption | **DA-NEW-9** (trivial) | OPTIONAL. |
| **S-11** | Fig. 1's grey-on-white backup labels (5.6 pt source, ≈7 pt printed) are the only element R0 would darken | rendered p. 3 at 200 dpi | **R0 §3** | OPTIONAL; production. |
| **S-12** | The contract qualifier "whose recovery contract holds" is absent from §V-B's closing sentence (R3's C3 residual, folded into roadmap 2 by arbitration C3) | `discussion.tex` L141–143 | **R0-N16 (part of blocking #2)** — R1 item 32 grades it **declared by choice**; R3 item 32 "optional; I do not press it" | OPTIONAL under the standing rule on declared declines. Conflict **B5**. |

---

## 4. Letter-vs-file findings

**Headline, unanimous for the second round running: no false change description.** R0 checked the Round-3 header, all 11 rows and every clause of the appended paragraph (17 references); R1 the 11 rows and 16 clauses; R2 the same plus the Round-2 corrections; R3 15 clauses; DA every clause. **All four Round-2 corrections asked for in roadmap 10 are made in place**: "Fourteen of the fifteen MUST-FIX items" (L2), row 14 "(2.04 SE; Round 2 had written 'within two', which was false)" (L1), "growth formula … and the 122 s solve time stated (Round 3)" (L3, and now *true of the file*), "DOIs added to three of the four" (L4); the not-applied list carries 42, K22 and 7's note (L7); "no data" became "deferred desk work (facts or logs we hold but did not write up this round)" (L8 — R1: "the wording I asked for"). R3: "the letter remains a usable verification instrument; its remaining faults are bookkeeping."

What remains — all scope-level, none of the Round-2 kind:

| # | Letter clause | Verified status | Found by |
|---|---|---|---|
| **L1** | Row "'spread' is a sample SD → 'round-to-round standard deviation' **in the abstract, §Cost and the conclusion**" | **Stale.** True of §IV-F L368–369 only; the abstract (L23) and conclusion (L135–136) were re-edited the same day under S7 to "+0.6 % (±11 % across three rounds)" and contain no "standard deviation". The appended paragraph records the later edit; the row was not updated. | R0-2a · R1-L2 · R2-L-b · R3-L5 · DA §2 (all 5) |
| **L2** | Row "7B decode step … '500 ms (493 ms pooled over the repeated trials)'" | **True as a change description; the number is false** (K2). The letter is faithful to the file; the file transcribed the roadmap's wrong end. | R0 · R1 · DA |
| **L3** | Appended ¶: "J6, J8, J9, J10, S10, S13 remain on the not-applied lists above" | **Five of six; and one under-claim.** J8 is on no list (K14). J10's second clause — the exactness sentence — **was applied** (`related.tex` L69–71) and is reported as not applied. | R0-2b · R1-L3 · R2-L-c/§2 · R3-L13 · DA §2 (all 5) |
| **L4** | Header: "**All Round-3 residuals** are sentence-scale and applied" | **Overstates by two or three**: MUST-2's offline clause (K3), MUST-1's Discussion twin (K1), MUST-6's reasons (K12). Same species as Round 3's "all fifteen", one round on. | R0-2c · R2-L-a |
| **L5** | Round-2 rows: "Fig. 1 regenerated … Fig. 2 caption … Fig. 4 y-axis"; Round-1 "Fig. 2 (Pareto)" | **Off by one.** The architecture figure is now Fig. 1; latency 2, Pareto 3, cost 4. Historical rows; one bracketed note fixes them. | R0-2d · R1-L4 · R2-L-d · R3-L15 (4) |
| **L6** | **Fig. 1 is not mentioned anywhere in the letter.** | A new figure introduced after the roadmap commit, during a verification round. The figure is correct (§5); the silence is the fault. | **R2-L-d** |
| **L7** | Round-3 preamble: "No false statement was found in the Round-2 letter" | Contradicts row 14 twenty lines earlier and the R3 decision's L1. One false clause *was* found; say so. | **R1-L1** |
| **L8** | Not-applied lists not exhaustive | Missing: MUST-2's second clause, MUST-5's second clause, `kosaian2019parity` pages, J8. R2: "the third round I have written that sentence." | R0 · R1 · R2 · R3 |
| **L9** | Round-2 closing "Abstract 258 words" | Now 253 (R0's count 253; DA `wc -w` 253) after the S7 edit. Harmless. | R0 item 35 · DA §2 |

**Editorial note.** The letter's faults have shrunk again: Round 2 had four false change descriptions; Round 3 one false clause and three completeness overstatements; Round 4 one stale row, one list omission, one under-claim, one undisclosed figure. Because this decision is *Accept with editor-verified errata* and no reviewer is re-engaged, the letter's remaining role is the record: the authors should submit L1, L3, L6 and L7 as a one-paragraph addendum alongside the errata so the file matches the letter one last time.

---

## 5. Fig. 1 (system architecture, §III-A) — consolidated assessment

**Provenance and build** (R0, R2, R3): `make_architecture.py` 21:53:16 → `fig_architecture.{pdf,png}` 21:53:17 → `main.pdf` 21:56:41, after the last section edit; fonts embedded and subset (Times + STIX), matching the body; 10 pages held (`pdfinfo`), **zero overfull boxes** (`main.log`, R2); referenced from §III-A and §IV-A L29–30 ("a six-stage pipeline **of the form in** Fig. 1" — R1 and R2 both call this the right hedge for a generic $n$-stage drawing).

**Accuracy — verified by all five against the generator and §III, and by R0, R1, R2 and DA against the code.** Every reviewer independently confirms the same seven elements: (1) four coordinator roles = §III-A's four verbs; (2) one stage per worker with a dashed preloaded backup beneath = `design.tex` L23–25 and `memory_check.py:60` `eager_backup=True` (DA); (3) **the head has a backup and hosts one — the J2 fix drawn**; every backup on a device other than its primary, i.e. a derangement as Eq. (mem) requires (R2); (4) **red "KV + input" arrows from the non-head stages only, none from the head** — `gateway.py:1183` "Head is coord-sourced and never ships KV" (R0, R2, DA) and `worker/server.py:441` `start_layer > 1` guard (R1, DA); (5) dotted control bus $(\psi,R)$ to every worker; (6) request path client → coordinator → head, stage $n$ → coordinator = §III-F's RPC-trailer detection; (7) head's KV unprotected shown by omission. R1 and R2 both note the head hosting stage $n$'s backup echoes the live table (`on-6` backed by `on-5`, hosting `ao-1`'s). **Nothing in the figure contradicts the design text** (R0, R1, R2, R3, DA — five for five). DA, whose brief was to attack it: "honest about more than most architecture figures"; "nothing in the figure is false of the *design*."

**Legibility** (R0): smallest labels 5.5 pt source → ≈7 pt printed after the 1.3× scale, above IEEE's 6 pt floor; line weights survive; ≈40 % of a column; no page cost.

**Effect on scores.** R0 Clarity 8→9 ("the figure is the reason"; closes W11, carried since Round 1). R3 Clarity-for-practitioners 6→7 ("the largest single move toward that bar in four rounds") and a contributor to Practical impact 6→7. R2: "technically accurate against §III on every point I checked." R0: "the cheapest improvement the paper has received in four rounds" — it makes the paper *read* as a system for IoT-J's audience; it does not move the bibliographic-fit objection, which is a different thing.

**Caption** (`design.tex` L8–11): "KV-CARE architecture. The coordinator places layers and backups ($\psi$, $R$), folds the protected stages' KV columns into parity, retains the interrupted position's input, and detects failures; each worker serves one stage and preloads the backup weights $R$ assigns to it (dashed)." Adequate (R0, R2); names every role drawn and the one encoding the legend lacks. Its one defect is the one every reviewer found: "retains the interrupted position's input" is the design *need*, and §IV-C — written in the same revision — says the prototype keeps every position (K5).

**Three changes the panel consolidates from the five reports** (all caption/legend-level, editor-checkable, no regeneration required for the first two):

1. **Caption, the mirror** (K5; R0, R1, R2, R3, DA): "retains the stage inputs by position (the interrupted position's suffices for the parity path)". MUST-FIX — an internal inconsistency between the caption and §IV-C L219.
2. **Legend, the two unmarked domains** (K7 + K8; R0, R2, R3, DA): one entry for the head's grey fill ("outside the parity domain; backup serves replay") and a marking for the coordinator ("outside the protection domain"). R3's combined form — shade the coordinator too and add "shaded: outside the parity/protection domain" — closes both in one legend line. SHOULD-FIX.
3. **$R$ as it lands** (K6; R1, R3, DA): the drawing is a permutation; the measured 7B mapping puts two backups on `on-5` and `on-1` and none on `on-2`/`ao-2`, and that concentration is the mechanism behind §IV-E's coupling result and the 350M 30.3 s intercept. Minimal: caption "(one backup per worker as drawn; the scheduler may assign several or none)". Better, if the authors rebuild anyway: change the `STAGES` literal — the generator's `for k, l in enumerate(hosted)` loop already renders a list (R3). SHOULD (caption) / OPTIONAL (redraw).

Also noted, not required: no return arrow to the client (R2); memory — Eq. (mem)'s subject — appears nowhere on the drawing (R3); stage $n$'s column drawn identically to stage 2's though never parity-recoverable (DA §4(4), S-8); grey backup labels could be darker (R0, S-11); the letter must disclose the figure and the renumbering (L6).

---

## 6. Devil's Advocate adjudication

Every DA CRITICAL and every item DA asks the editor to require, adjudicated visibly. **DA raised no new CRITICAL this round** and opens its verdict with "Nothing blocks acceptance."

| DA item | DA's R4 status | Corroboration / contradiction | **Adjudication** |
|---|---|---|---|
| **NEW-3 (R3's only CRITICAL)** — "496 rejected *solely* because no peer could reserve the backup weights" | **ADDRESSED — CRITICAL withdrawn.** §IV-E now "rejected as infeasible under Eq. (mem), which charges backup reservations beside each device's own stage"; the letter's account of the handler "is exactly what `scheduler.py:405` does". Residual MINOR: non-convergence at L279 is an uncounted cause, so "under Eq. (mem)" is "*mostly* right rather than *provably* wrong" | R0 item 1 A (reads the handler; notes the non-finite `continue` as a soft third cause); **R1 row 14 A and stronger** — verified the throughput-mode SLO cap did not bind at 7B (`slo_tbt_seconds: 1.0` vs 72 ms max stage), so both pooled causes are the two terms of Eq. (mem); R2 A; R3-L3 true. **But R1-NEW-13, R2-N4-1 and R3-NEW-1 each find the same attribution surviving in `discussion.tex` L56–57 "for want of backup room" — a site DA did not check** | **RESOLVED in §IV-E; the Discussion twin is UPHELD as MUST-FIX (K1)** on rule (a) — a stated cause contradicted by the scheduler the authors still hold, re-read independently by three reviewers — and rule (c), three reviewers blocking. DA's own §IV-E residual and R0's are graded MINOR by both and stay OPTIONAL (S-3); K1's wording ("the backup reservation among the causes") is exact under every reading, so the §V-A edit does not inherit the question. |
| **Errata 1** — "(493 ms pooled over the repeated trials)" includes fifteen unfired $k{=}2$ stub rows; 500 was right (NEW-8 MADE WORSE) | Require: delete or print 499; then name the 527 ms as the protection-cost runs' interval | **R0-N14 and R1-NEW-14 reproduce it independently from `rep3.json`** (60 gated rows → 499.65 / 499.6 ms); R1 adds that the figure generator gates the same way and prints 500. R2 "did not recompute"; R3 used 493 without objecting. **R0 withdraws its Round-3 N12 as the source** | **UPHELD as MUST-FIX (K2)** — rule (a), verified by three reviewers from the raw file. The panel records that the number came from its own roadmap (item 20 printed "493 ms or 'about 500 ms'"); the fix is a deletion that restores what the authors had. The 527 ms naming is SHOULD (S-4): R0 grades it cosmetic and the roadmap's own item 20 already asked for it once. |
| **Errata 2** — "within three decode steps" straddles KV-CARE's own 2.1–3.9× | Require: "at $P{=}32$" or the roadmap's Petals clause | R0-N15 blocking; R1-NEW-15 minor, non-blocking; R2 PARTIALLY, should-fix at proof; R3-NEW-2 minor, "strongly recommended". All five say the sentence is *true of Fig. 2's $P{=}32$ means*; R1 and DA locate the inconsistency against L157 ("2.1–3.2×") and L188 ("2.1–3.9×") in the same section | **UPHELD as MUST-FIX (K4), the weakest of the five**, on rule (a) as an internal-consistency defect (the class boundary sits below the paper's own printed maximum two paragraphs earlier) and because it is the second round the identical one-clause ask was not taken and the change went the other way (R0: "does that more visibly than the old"). Two reviewers block, three do not — rule (c) alone would not carry it; (a) does. Conflict **B2**. |
| **Errata 3** — Fig. 1 caption "the interrupted position's input" contradicts §IV-C; the head's grey fill has no legend entry | Require both | Caption: R0 §3(i), R1-NEW-18, R2 §3, R3 §3(4) — five for five (K5). Grey fill: R0, R2, R3 (K7) | **Caption UPHELD as MUST-FIX (K5)** — an internal inconsistency between two passages written in the same revision, verified against `worker/server.py` and `activation_cache.py`. **Grey fill is SHOULD-FIX (K7)** — an omission, not an error; R2 calls it cosmetic. |
| **Errata 4** — "no valid trial" hides that two of three discards were the reconfigured pipeline failing to boot | Require one clause | R0 "the honest short form"; R1 "true; reasons unpublished"; R2 PARTIALLY; R3 "P; not blocking; the reasons matter more than the count". All five confirm the note is **true** of the log | **DOWNGRADED to SHOULD-FIX (K12).** The note is true and retracts a false one; DA's point is that the omission runs *against* the authors — which is a reason to recommend it, not to block on it. Rule (a) is not met; no other reviewer blocks. Conflict **B6**. |
| **Errata 5** — letter: stale "spread" row, J10 under-claim, abstract 253 | Require | All five find L1; R1, R2, DA find L3; R0 and DA the word count | **SHOULD-FIX (§4).** No reviewer round follows; the letter is the record. |
| **C7 residual** — "sequential procedures fail" on three procedures; no retrying baseline | NOT ADDRESSED; "stays MAJOR-not-blocking, as adjudicated" | R2-M-7 adjacent; unchanged since R3 | **Standing MAJOR, not blocking, unchanged.** No testbed can settle it; DA does not re-open it. |
| **NEW-8 (J15)** | MADE WORSE | = Errata 1 | Folded into K2 / S-4. |
| **S3 residual** — the mirror is six vectors (head primed), so 3.0×/42 % are 2.9×/41 % as built | MINOR | Single reviewer, code-level (`gateway.py:1735`); not contradicted — R1 verified the five worker-shipped vectors, DA adds the coordinator-primed sixth | **SHOULD-FIX (S-2)**; K11's "KV" keeps the headline exact under either count. |
| **S9 residual** — "tolerate any number" untested for replication; `_recover_replicate` attributes one victim | should-fix | R3 §1c raises the other half of the same sentence | **SHOULD-FIX (K13)**, merged. |
| **§5 "strongest remaining counter-argument"** — "when a reviewer supplies a number, it is transcribed rather than recomputed" (493; the Pareto boundary) | — | R1: "the one number in the manuscript a reader following the stated protocol cannot reproduce, and it arrived by copying the roadmap's 493.4 without re-deriving it"; R0 owns the source | **Accepted as the characterisation of this round's residue**, with the amendment that the panel co-owns it (B7). It is the same failure class that produced 9.8 (K10) and ±26 (K16), both also the panel's numbers. |

**Summary.** DA's Round-3 CRITICAL is discharged at the site it named and upheld at the site it did not check. DA's five errata: two upheld MUST (K2, K5), one upheld MUST on a different ground (K4), one downgraded to SHOULD (K12), one SHOULD (letter). No new CRITICAL. **DA withdrew or closed nine items this round** (NEW-3 CRITICAL, NEW-1, -2, -4, -5, -6, -7, C1-residual, C8, N7), corroborating the R3 note that its verification is actually performed.

---

## 7. Decision

### **ACCEPT WITH EDITOR-VERIFIED ERRATA** — five MUST-FIX items, each a word, a clause or a deletion; no further reviewer round.

**Rationale.**

*The panel has converged past Minor.* Five of five recommend acceptance subject to editor-verified corrections; none asks for another external review; the DA opens with "Nothing blocks acceptance." Every R3 MUST-FIX item is applied (1, 3, 4, 6, 7, 8, 9), half-applied with the applied half correct (2, 5), and **verified against the files, code and result JSON by every reviewer independently rather than taken from the letter**. Both R1 conditions, all three R2 blocking items, all three R3 errata and the DA CRITICAL are discharged. Seven of eighteen scored dimensions moved up and none down; R2's Novelty moved for the first time in four rounds on a sentence the authors wrote without claiming it.

*What holds this short of an unconditional Accept is five items, each verified, each one line.* Two are propagation failures of the class the panel has now corrected in every round — the sole-cause attribution surviving in the Discussion (K1; three reviewers blocking, three independent readings of `scheduler.py`) and the "offline placement analysis" label still missing from the Conclusion (K3; the unapplied half of R3 MUST-FIX 2, neither applied nor declared). One is a false derived number **that the panel itself supplied** (K2; R0 withdraws its Round-3 N12; the correction is to delete eight words and restore the 500 ms the authors had printed). One is an internal inconsistency between a figure caption and the §IV-C paragraph written in the same revision (K5; five for five). One is a class boundary redrawn instead of the clause four reviewers asked for, now sitting inside the paper's own printed range (K4; two blocking, five raising). None requires the closed testbed; none requires a page; each is verifiable by grep and, for K2, one median over the 60 gated rows of `b1_ft_fleet_7b_rep3.json`.

*Why not Minor Revision.* The Round-3 decision set the yardstick: Minor when "four statements … are contradicted by the authors' own files" plus two self-created contradictions and one undocketed consistency question. This round the count is one contradicted statement (K1, the twin of one already fixed), one false number the panel wrote, and two internal inconsistencies — and no item that a reviewer, as opposed to an editor, needs to see again. R2: "It is six words and a grep; I would not convene the panel for it." R1: "What remains is two clauses and a caption." R3: "An editor can check it by grep; it does not need a reviewer."

*Against that, recorded.* The propagation pattern is real and R1 is right that "the fix is trivial, the pattern is not" — four rounds, four twins missed (R2 Conclusion; R3 Conclusion + $R$; R4 Discussion + Conclusion's second clause). The editor should grep for each MUST-FIX string in **every** section file, not the cited one. And the panel's own transcription errors (493, 9.8, ±26) are the reason MUST-FIX 2 below is a deletion rather than a substitution: the authors should be asked to re-derive, not to copy, any number a reviewer supplies.

**Verification instruction for the editor.** Check MUST 1–5 against `sections/discussion.tex`, `sections/evaluation.tex` and `sections/design.tex` by grep; for MUST 2, either confirm the parenthetical is gone or confirm any printed value against the median of `median_tbt_seconds` over the rows of `b1_ft_fleet_7b_rep3.json` with `fired`, `recovery_visible` and `sequence_match` all true (≈499.6 ms). For MUST 1, additionally grep `backup room` across `sections/` and expect zero hits. No reviewer needs to be re-engaged.

**Ceilings (recorded, not blocking; unchanged across four rounds).** Fit 6 (R0) and Significance 5 (R0): one IoT-J entry in 34, the §II paragraphs a declared page-budget decline (A5), the replication advantage still "a derived projection". R0 **withdraws the §II paragraphs as a condition** this round — "an associate editor should not re-impose a condition the panel has weighed and set aside" — and records the figure as real progress on presentation fit. R3's Deployability is held at 6 by item 17 alone, "an absence, not an error". Both reviewers call the resulting ceiling a publishable outcome for IoT-J.

---

## 8. Errata list R4

Numbered, deduplicated, conflicts resolved. Every item is text, a symbol or a number the authors already hold; none needs the testbed or a page.

### MUST-FIX (editor-verified before the version of record)

1. **`discussion.tex` L56–57** — "for want of backup room" → "as infeasible under Eq.~\ref{eq:mem}, the backup reservation among the causes". Then `grep "backup room" sections/` → 0. *(K1 — R1-NEW-13, R2-N4-1, R3-NEW-1)*
2. **`evaluation.tex` L40–41** — delete "(493\,ms pooled over the repeated trials)"; "500 ms" stands. If a value is kept: "(499.6 ms over the 60 gated trials)". *(K2 — R0-N14, R1-NEW-14, DA errata 1; panel-supplied number)*
3. **`discussion.tex` L138–141** — insert "in an offline placement analysis" before "with backups confined to pipeline devices". The abstract's own words (L20). *(K3 — R0-N16, R1-NEW-19, R2, R3-NEW-4, DA; R3 MUST-FIX 2 carried over)*
4. **`evaluation.tex` L206–207** — add "Petals is the low-state endpoint at 40 kB/tok and 2.3 s"; scope any retained class "at $P{=}32$" or drop it. *(K4 — R0-N15, R1-NEW-15, R2, R3-NEW-2, DA errata 2)*
5. **`design.tex` L8–11, Fig. 1 caption** — "retains the interrupted position's input" → "retains the stage inputs by position (the interrupted position's suffices for the parity path)". *(K5 — R0 §3, R1-NEW-18, R2 §3, R3 §3, DA errata 3)*

### SHOULD-FIX (same pass; no page)

6. **`abstract.tex` L17, `discussion.tex` L134** — "3.7× less coordinator state" → "3.7× less coordinator **KV** state"; optionally "(3.0× as built)". *(K11 — R2-N4-2, R0-N17, R1, R3 §1c; conflict B4)*
7. **Fig. 1 legend/caption** — one entry for the head's grey fill and a marking for the coordinator: "shaded: outside the parity domain (head: backup serves replay; coordinator: unprotected)". *(K7+K8 — R0, R2, R3 §3(2)(3), DA §4(3)(5))*
8. **Fig. 1 caption** — "(one backup per worker as drawn; the scheduler may assign several or none)". *(K6 — R1 §3(1), R3 §3(1), DA §4(2); conflict B3)*
9. **`discussion.tex` L69–70** — "the head stage is outside the parity guarantee (a head failure falls to full-prefix replay on its preloaded backup, a path we did not measure) and the coordinator is unprotected". *(K9 — R2-N4-3, R3-NEW-3)*
10. **`evaluation.tex` L135 Table I note (a) or §V-A L67–68** — "three further positions were attempted; two were discarded because the reconfigured pipeline did not come up". *(K12 — DA errata 4, R3 §1c, R1, R0, R2; conflict B6)*
11. **`evaluation.tex` L225–226** — "By construction, replication and replay tolerate any number … (one was measured); parity restores up to $k$ without recomputation and falls to replay beyond." *(K13 — R3 §1c, DA S9 row)*
12. **`design.tex` L223** — "about 9.8 million" → "about 9.9 million"; add "the prototype searches up to eight devices (109 592 orderings) and falls back to heartbeat order above that". *(K10 — R0, R1, R2, DA; S-1 — R1-NEW-16)*
13. **`evaluation.tex` L367–368** — name the protection-off interval as its own quantity: "these runs' median token interval of 527 ms", as 183 ms was at 350M. *(S-4 — DA errata 1, R0, R3-L9)*
14. **`evaluation.tex` L219–224** — either recompute the all-family ratios on the six vectors the prototype keeps (2.9×, 41 %) or say "five non-head vectors; the head's primed input adds 8 kB/tok". *(S-2 — DA S3 row)*
15. **`design.tex` L31–32** — "(the prototype keeps every position's input for the replay ladder; Section IV-C charges it)". *(K5 upstream — R3 §1c, R0, R1)*
16. **Letter addendum** — refresh the "spread" row; file J8; credit J10's applied clause; disclose Fig. 1 and the +1 renumbering; correct "No false statement was found" to "one false clause"; make the not-applied list exhaustive; abstract 253. *(§4 L1–L9 — all five; R2-L-d; R1-L1)*

### OPTIONAL

17. `evaluation.tex` L308–310 — "rejected as infeasible (Eq. (mem) is the binding constraint)" if the non-convergence exit is to be covered. *(S-3 — DA NEW-3 row, R0 item 1; conflict B8)*
18. `evaluation.tex` L227–229 — append "the parity path additionally fetches the surviving columns at recovery". *(K17 — R0; closed as MUST, conflict B9)*
19. `discussion.tex` L141–143 — "whose recovery contract holds" in the closing sentence. *(S-12 — R0-N16; declared by choice per R1 item 32; conflict B5)*
20. `make_architecture.py` L15–16 — change the `STAGES` literal so one worker hosts two backups and one none; regenerate. *(K6 stronger form — R3 §3)*
21. `evaluation.tex` L224 — "42 % less"; L373–374 — "±27 %" or keep "about". *(S-9 — R0; K16 — R1-NEW-17, R0, R2, DA)*
22. `references.bib` L330–336 — `pages = {30--46}` for `kosaian2019parity`; production office. *(K15 — all five)*
23. `evaluation.tex` §IV-C — the as-built absolute at $k{=}1$ (152 kB/tok; ≈80 MiB mirror per 2 048-token request). *(S-7 — R3 §1c)*
24. `evaluation.tex` L317–318 — name the 7B "whole fleet" (`on-3`/`on-4`). *(K19 — R0 item 31, R1 item 17)*
25. `main.tex` / `background.tex` — delete the orphan or `\input` it. *(K18 — R0-W11, R3 item 34; declared)*
26. Fig. 3 caption "mean" → "median" to match `make_recovery_pareto.py`; Fig. 1 grey backup labels darker; a caption word that stage $n$'s column falls to replay. *(S-10 — DA-NEW-9; S-11 — R0; S-8 — DA §4(4))*
27. `design.tex` §III-F / `evaluation.tex` §IV-A — the environment paragraph (JetPack/L4T, versions, `nvpmodel`/`jetson_clocks`, LAN rate, artifact statement). Declared; both reviewers' first use of any page found at proof. *(K20 — R1 item 13, R3 item 13)*

---

## 9. The four-round trajectory

Round 1 was a unanimous Major: eight Devil's Advocate CRITICALs, a Pareto sentence that credited a competitor 0 kB/tok, a footprint that omitted the backup weights, and no figure of the system. Round 2 stayed Major on a different ground — the manuscript had improved but the response letter carried four false change descriptions, two figures were stale, and 163 ms and 183 ms fought each other across sections; the panel's decision turned as much on the letter as on the paper. Round 3 reached Minor because the letter retracted its falsehoods by name and in place, the fifteen MUST-FIX items became nine word-level ones, and the DA recomputed every derived number and found exactly one wrong (2.038 SE) plus one over-attribution ("solely"). This round the nine are applied or half-applied, the CRITICAL is discharged at the site it named, the letter has no false change description, the figure the paper lacked since Round 1 was added at no page and verified against the code by four reviewers, and the residue is five lines — one of which the panel wrote. The class of what remained shrank each round: false letter statements, then false scope claims, then stale rows; false headline claims, then one false derived number, then one false parenthetical transcribed from a reviewer. What did not shrink is the pattern R1 names — a fix landing at the cited line and not its twin, four rounds running — and the panel's own habit of supplying numbers it had not gated, which the authors then copied. Both are procedural, both are one grep from closed, and neither touches a measurement. What four rounds bought, in R2's words from Round 3 and repeated by every reviewer this round, is a paper in which "every remaining claim … is one a reader can check against a printed number, a printed log or the authors' code — and I have checked them." The ceilings — Fit 6, Significance 5, Novelty 6 — were reached in Round 1 and are the paper's honest size; the revision consistently moved against the authors' interest to reach them. **Accept with editor-verified errata.**

---

## Reviewer conflicts arbitrated

**B1 — §V-A wording: "under Eq. (mem)" (R1, R2) vs "…, the backup reservation among the causes" (R3) vs DA's note that Eq. (mem) is only "mostly" the cause.** DA-NEW-3 row: `NoFeasibleSolutionError` is also raised at `scheduler.py:279` for ten-iteration non-convergence; R0 item 1: the non-finite-objective `continue` is a third exit. R1 row 14 verified the SLO cap did not bind and calls §IV-E "precise". **Resolution: use R3's form in §V-A.** It is exact under every reading (Eq. (mem) *among* the causes is true whether or not non-convergence contributed), and it is the phrase the R3 roadmap item 1 itself gave. §IV-E's "under Eq. (mem)" is left standing — three reviewers accept it and DA grades its residual MINOR (S-3, OPTIONAL).

**B2 — The Pareto boundary: MUST or SHOULD.** R0 (blocking #3) and DA (errata 2) require it; R1, R2, R3 grade it minor and non-blocking. All five agree the sentence is true of the $P{=}32$ means. **Resolution: MUST-FIX under rule (a)** — R1 and DA both locate an internal inconsistency against the same section's printed "2.1–3.2×" (L157) and "2.1–3.9×" (L188): a class defined by "within three decode steps" does not contain every KV-CARE trial the paper reports, so the sentence's own subject is inside the class only at the point plotted. Rule (c) alone would not carry it (two blocking, not three); (a) does. The panel also weighs that this is the second round the identical one-clause fix was asked for and the edit went the other way. It is the weakest of the five MUSTs and the editor may accept "at $P{=}32$" alone as the minimum.

**B3 — Fig. 1's $R$: caption clause (R1) vs redraw (R3) vs "false of the prototype as measured" (DA).** R0 and R2 did not flag it. **Resolution: SHOULD-FIX the caption clause; OPTIONAL the redraw.** The caption clause is editor-checkable by grep and needs no rebuild; the redraw is the better fix and the generator supports it (R3 verified the `hosted` loop), but a regenerated figure at the errata stage is something the editor cannot verify from the text. If the authors regenerate for any other reason, take R3's form.

**B4 — The 3.7× headline: "(3.0× as built)" parenthetical (R0-N17) vs "coordinator KV state" one word (R2-N4-2) vs DA's six-vector 2.9×.** **Resolution: R2's one word.** "3.7× less coordinator KV state" is exactly Table I's own row label, is true regardless of whether the mirror is five or six vectors, and does not depend on DA's single-reviewer code reading. R0's parenthetical is optional; DA's six-vector count is docketed separately as S-2 for §IV-C, where the 3.0×/42 % numbers live.

**B5 — The contract qualifier in §V-B's closing sentence.** R0 folds it into blocking #2 (per R3's arbitration C3); R1 item 32 grades it "declared by choice"; R3 item 32 "optional; I do not press it". **Resolution: OPTIONAL.** It is on the authors' declared list and the standing rule protects declared declines. R0's point that it is the abstract's own words is recorded.

**B6 — Table I's Reconfigure reasons: DA requires, four reviewers do not block.** All five confirm "no valid trial" is true of the log. **Resolution: SHOULD-FIX.** DA's argument — that two boot failures are evidence *for* the thesis — is correct and is the reason to recommend the clause, but a true statement that omits evidence in the authors' own favour is not an error under rule (a). R3's "the reasons matter more than the count" is the practitioner's version of the same recommendation.

**B7 — Who owns the 493.** R0: "my error"; R1: "on the panel as much as the authors"; DA: "adopted from a Round-3 reviewer remark without a check" — the transcription pattern. **Resolution: the panel owns the number, the authors own the transcription, and the fix is a deletion.** R3's roadmap item 20 printed "493 ms or 'about 500 ms'"; the authors took the end the panel had vouched for. This is recorded in the decision letter as a panel error, and the same class (9.8 M, ±26 %) is why item 12 asks for re-derivation rather than copying. Both branches R1 and DA offer (delete; or print 499.6 over the 60 gated trials) are acceptable.

**B8 — Is §IV-E's "under Eq. (mem)" precise?** R1: yes (SLO cap did not bind). DA: mostly (non-convergence uncounted). R0: possibly a third cause (non-finite `continue`), unquantified. **Resolution: not blocking — all three say so.** OPTIONAL wording (item 17). An offline solver re-run with a per-reason counter would settle it and needs no testbed, as A6 established; the panel does not require it.

**B9 — Which unapplied second clauses carry over as MUST.** R3 MUST-FIX 2's second clause (offline label) and MUST-FIX 5's second clause (recovery-path fetch) were both left unapplied and undeclared. **Resolution: 2 carries over (K3, MUST); 5 is closed (K17, OPTIONAL).** The rule: an R3 MUST clause carries over unless the panel finds its purpose discharged by the half that was applied. For 5, R1, R2 and R3 each verify that the "during failure-free decoding" scoping alone removed the contradiction with L165–166, which already states the recovery-path traffic — the clause's purpose is met. For 2, no reviewer finds the Conclusion's feasibility sentence labelled by anything else; R2's "misleads no one" (because "per-device memory cap" implies a capped analysis) is the strongest case for closing it, and the panel declines it because the abstract, §IV-E and §V-A all carry the label and the Conclusion is where a skimming reader lands (R1-NEW-19) — the same twin-propagation defect as K1.

---

*Prepared by the Editorial Synthesizer. Every item above traces to a named Round-4 reviewer report and item id. The manuscript, its sources, code and result files were not read or modified in producing this decision; all quotations are taken from the five reviewer reports.*
