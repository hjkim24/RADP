# Editorial Decision — Round 3 (Second Verification) — KV-CARE (IEEE IoT-J)

**Date:** 2026-09-03 · **R1 decision:** Major (5/5) · **R2 decision:** Major (1 unconditional Minor, 2 conditional Minors evaluating to Major, 2 Majors)
**Basis:** five Round-3 verification re-reviews (`R0/R1/R2/R3/DA_round3.md`) checked against `EDITORIAL_DECISION_R2.md` §7, roadmap items 1–42.
**Standing constraints honoured:** the testbed is closed — testbed-bound items are judged only on scoping and disclosure. The authors hold the paper at **10 pp. deliberately**; page-budget declines that are *declared* are graded as disclosed absences, not as errors (R3 §Context). Every MUST-FIX below is word- or clause-level and costs no page.
**Rule used for MUST-FIX this round** (tightened from R2, because the panel has converged): an item is MUST-FIX only if (a) it is a **verified factual or internal-consistency error** against the authors' own files, code or logs, or (b) it is an **upheld Devil's Advocate CRITICAL**, or (c) **three or more reviewers name it blocking in their own recommendation**. Everything else is SHOULD/OPTIONAL. Bare "≥2 reviewers raise it" is no longer sufficient — at this stage it produces a list the authors cannot distinguish from polish.

---

## 1. Panel table

| Reviewer | R1 rec | R2 rec | **R3 rec** | A | P | N | W | Items traced | Score movement (R1 → R2 → **R3**) |
|---|---|---|---|---|---|---|---|---|---|
| **R0** Journal-Fit | Major | Minor *(conditional; condition unmet)* | **Minor — "and this time not conditionally"** | 15 | 10 | 0 | **0** | 25 (17 R1 + 8 R2-new) + 20 roadmap rows | Fit 6→6→**6** · Originality 6→7→**7** · Significance 5→5→**5** · Clarity 6→7→**8** · **Claims-vs-evidence 4→6→8** |
| **R1** Methodology | Major | Minor ("accept after 1–8") | **Accept subject to minor editorial corrections** (2 named conditions) | 13 | 13 | 4 | **1** | 31 (24 R1 + 7 R2-new) + 15 roadmap rows | Exp. design 5→6→**6** · Stat. validity 4→4→**5** · Metric defs 6→7→**8** · Reproducibility 3→4→**5** · **Limitations 6→7→9** |
| **R2** Domain | Major | Minor *(conditional on a propagation check)* | **Minor — "not conditional on anyone else's verification, because I performed it"** | ~19 | ~11 | ~5 | 0 | 35 (C-1–C-4, M-1–M-11, m-1–m-12, NEW-1–NEW-8) + 17 roadmap rows | Literature 5→6→**7** · Tech. soundness 5→6→**7** · **Novelty 5→5→5** · Fairness 5→7→**8** |
| **R3** IoT practitioner | Major | **Major** | **Minor — "fix the three errata and I would accept"** | 17 | 9 | 7 *(6 on the declared not-applied list)* | **0** | 33 (25 R1 + 8 R2-new) + 12 roadmap rows | Deployability 4→5→**6** · Motivation–eval match 3→5→**6** · **Practical impact 5→6→6** · **Clarity for practitioners 6→6→6** |
| **DA** Devil's Advocate | Major | **Major** | **Minor — "the first time I have written that for this manuscript"**, conditional on NEW-3 | 12/15 MUST clean | — | — | — | 21 (C1–C8, M1–M13) + 9 new | no rubric. **8 items withdrawn** (C3, C5-blocking half, M1, M2, M10, N3, N4, N6-as-arithmetic); **1 new CRITICAL** (NEW-3) |

**Panel arithmetic: 5 Minor-or-better, 0 Major, 0 conditional-Minor-evaluating-to-Major.** R2's Minor was conditional on a propagation check in R2; R2 ran that check itself this round and reports it "substantially passes". R0's R2 Minor was conditional on its items 1–2 (the conclusion; 163/183 + stale figures); R0 verified both fixed **from the files, the figure binaries, the generators and the result JSON**, not the letter. R3 and DA both moved Major → Minor.

**What the panel unanimously records as delivered** (all five, independently): the Conclusion is materially rewritten and carries six or seven qualifiers (DA verified 7/7); "regardless of the failure position" returns zero grep hits; the Fig. 2 reading sentence is rewritten and *true* of Table I (R1 checked it); 163 ms and 183 ms are two genuinely distinct measurements, reproduced from raw JSON by R0, R1, R2 and DA independently; all three figures were regenerated **after** the source edits and before the build; the inverted Reconfigure pairing was corrected **against the authors' own log** (P=4→465.805 s, P=8→413.069 s), verified by four reviewers; and the response letter's four Round-2 falsehoods are retracted *by name, in place*. Four reviewers separately note the revision repeatedly moved **against the authors' interest**: charging the head's full 12.4 GiB, publishing that at n=3 "these figures bound the cost rather than rank the modes", disclosing the DejaVu baseline's centralization, retiring the "four times the bytes" story, and carrying 2.7× beside 3.7× into the abstract.

---

## 2. Consensus residuals (≥2 reviewers)

| # | Defect (one line) | Location as quoted by reviewers | Raised by | Minimal fix |
|---|---|---|---|---|
| **J1** | MUST-FIX 4 / K10 / DA-C6 reached the abstract, §I and §III-E and **stopped at the Conclusion** — which now asserts what §III-E explicitly denies | `discussion.tex` L128–129: "so that **the recovered state has a device to run on**", against `abstract.tex` L11–13 "the failed stage's **weights** have a device to **load** on" and `design.tex` L198–200 "Eq. (mem) charges weights only" | **R0-N9 (MAJOR)** · **R1-NEW-11 (blocking)** · **R2-N3-2 (blocking #2)** · **R3-NEW-B (blocking #3)** · DA-NEW-4/C6 ("third round of asking") | Five words: paste the abstract's own wording. Add "in an offline placement analysis" to the Conclusion's feasibility clause while there (R2-N3-2, R3-MAJOR-6). |
| **J2** | The K17 fix moved the contradiction from §IV-A's prose into §III-A's **notation**: R's declared domain excludes the stage the same sentence says it covers | `design.tex` L8 "$R:\mathcal{S}\to\mathcal{D}$ … **every stage $s$, the head included**" vs L31 "Let $\mathcal{S}$ denote the **protected non-head stages**"; $\mathcal{S}$ carries that narrower meaning in Eqs. (1)–(3), the $\ell_{\max}$ padding rule, the completeness rule and Alg. 1's guard $F\nsubseteq\mathcal{S}$ | **R0-N10 (MAJOR)** · **R1-NEW-12 (blocking)** · **R2-N3-1 (blocking #1)** · **R3-NEW-A (blocking #1)** · DA-NEW-2 (MAJOR) | One symbol: $R:\mathcal{S}^{+}\!\to\mathcal{D}$ with $\mathcal{S}^{+}=\mathcal{S}\cup\{\text{head}\}$. Then one clause of rationale — see conflict **A3**. |
| **J3** | The hedge added for K8 is the manuscript's **only false derived statement** | `evaluation.tex` L160–161 "$-6\pm3$ ms per position, **within two standard errors of zero**". Both reviewers recomputed from `b1_ft_fleet_7b_rep3.json → fits.replicate`: $0.0060417/0.0029646=\mathbf{2.038}$ SE — outside. On the rounded table values $6.0/3.0$ is exactly 2.0, i.e. *at*, not within | **DA-NEW-1 (MAJOR)** · R1-NEW-8 (minor; independently 2.04) | Write "about two standard errors", or give the ratio. DA's own sentence works: "2.0 standard errors from zero, in the one family that does no per-position work, which we read as an unmodelled systematic." |
| **J4** | Table I's new footnote (b) credits a probe that measured the opposite thing | `evaluation.tex` L134–135 "byte identity **verified in the fidelity probe** (§IV-D)". The probe is Table II and reports **26.9 % of KV elements differing** on the cross-tier *replay* path; §IV-D attributes byte identity to "the **unit tests** assert layer-wise identity" (L261–262) | **R0-N11** · **R2-N3-5** | Three words: "byte identity **asserted in unit tests** (§IV-D)". R0's note stands: this makes the annotation *stronger* — "by construction, unit-tested" is an adequate basis; borrowing the probe's authority is what is wrong. |
| **J5** | Item 18/K12 is reported "growth stated" but the manuscript states a direction, not a bound — and the solve time exists in the authors' own report | `design.tex` L206–209 "ten iterations", "1 950 on the six-worker fleet", "so the search grows with the number of ordered subsets". **DA located the missing number: `experiments/REPORT.md:812` records "solve 122 s (1950 후보 중 1454 feasible / 496 infeasible)"** | R0-W8/M2 · R1-item 18 · R2-item 18 · R3-MAJOR-10 · DA-M9 (all 5) | Two numbers: "the solve takes 122 s on this fleet and enumerates $\sum_{k=2}^{|\mathcal{D}|}P(|\mathcal{D}|,k)$ ordered subsets — 1 950 at six devices, ≈9.8 M at ten." No testbed; no page. |
| **J6** | Table II still reports an absolute difference with no relative error or scale | `evaluation.tex` Table II: 26.9 %, $2^{-8}$, unchanged | R1-M8 · R2-M-5 · R3-MAJOR-9 · DA-M6 (4) | One column or one clause: relative error beside the $2^{-8}$ absolute. R1 notes this sub-part is neither applied nor on the not-applied list. |
| **J7** | The Pareto sentence's qualifying class is drawn at the one threshold that excludes the one competitor holding less state | `evaluation.tex` L204–205 "among the families that recover **within two seconds**, \sys{} retains the least state" — Petals is 2.31 s / 40 kB/tok, i.e. **2.8× less state**, excluded by 0.31 s; the paragraph never states the direction of that inequality | R0-N13 · R2-N3-4 · R3-NEW-C · DA-C5 residual (4) | One clause, in the authors' established style: "Petals is the low-state endpoint at 40 kB/tok and 2.3 s." All four call the current sentence *true*; all four call the boundary conspicuous. |
| **J8** | Three unit conventions coexist; the new declaration reaches one of them | Table I note L132 "kB denotes 1 024 bytes" (declared, table-scoped) vs §IV-C L223–224 "832 MB / 224 MB" (binary, undeclared), "12.4 GiB" (L25, L88), Table III "4.8 GB / 23 GB", §IV-A "3.1–5.8 GB" | R0-W14 · R1-m18 · R2-m-3 · R3-MINOR-7 (4) | Move the declaration to §IV-A and extend it to MB/MiB, or convert. |
| **J9** | The environment paragraph is the top non-blocking ask of two reviewers and is **not** testbed-bound | grep for jetpack/l4t/nvpmodel/jetson_clocks/tegrastats/Gb·s⁻¹ over `sections/` → **zero hits**; `design.tex` L218 "Python with PyTorch and gRPC"; no artifact statement | R1-m22 ("the single most consequential missing environment fact") · R3-MINOR-1/-2 ("if a page becomes available at proof, I would spend it here first") | Two lines: JetPack/L4T, PyTorch/CUDA/gRPC versions, `nvpmodel`/`jetson_clocks` power mode, LAN rate, artifact statement. Both reviewers stress the power mode is a **configuration fact the authors still hold**, not a measurement. |
| **J10** | The RAID-5/6 lineage that Eqs. (1)–(2) instantiate is still unnamed, and the novelty clause the correction unlocked is still unwritten | `grep -niE "raid\|evenodd\|rdp"` over `sections/` hits only the GhostServe description; §II-E still closes "\sys{} applies the same idea to the KV state of a pipeline" | R2-C-2 · R3-MAJOR-7 · DA-N8 (3) | Two clauses. R2 calls the second "the cheapest available raise to my Novelty score", held at 5 for three rounds: stored KV bytes admit an **exact** linear code where prediction serving admitted only a learned approximation. |
| **J11** | "two to three decode steps" survives unqualified against the paper's own 2.1–3.9× | `abstract.tex` L15–16, `discussion.tex` L142, against `evaluation.tex` L185–187 "$2.1$–$3.9\times$ for every $P$ and **both victims**" (3.9× is four steps) | R1-M14(c) · R2-N3-6/m-5 · R3-NEW-D · DA-NEW-5 · R0-item 39 (*declines to press*) | "two to four", or scope to the primary-victim sweep. See conflict **A2** — the silent drop from the not-applied list is the larger fault. |
| **J12** | `rashmi2016eccache` carries neither DOI nor pages, while the letter reports "DOIs added" | `references.bib` L338–343 | R0-M3 · R3-item 22 · DA-N8 · R2 (notes OSDI entries commonly lack DOIs) | One line, or one word in the letter ("three of four"). |
| **J13** | The 40-cap grid is non-uniform and the prose reads as a regular sweep | `evaluation.tex` L300–302 "over 40 caps from 0.2 to 23 GB"; the ladder is 4 000 MB steps at the top, **200 MB steps exactly at the reported 4.6/4.8 boundary**, 50 MB at the bottom | R1-M10 · R0-W5 residual · DA-NEW-6 (3) | Six words: "a non-uniform ladder densened to 0.2 GB near the threshold". The bracketing is legitimate; the description of how it was found is not. |
| **J14** | §I ¶1's capacity premise was never reconciled with §IV-A's concession | `introduction.tex` L15 "KV cache **can exceed the memory of a single edge device**" vs `evaluation.tex` L27–29 "either AGX Orin could, so on this fleet the pipeline is the scheduler's throughput choice" | R3-NEW-6 · DA-C8 (2) | One clause in §I. Roadmap 27 named both sites; only the evaluation side was written. |
| **J15** | The 7B twin of the fixed 163/183 split is left undisambiguated, and 500 ms is a rounded 493.4 | `evaluation.tex` L41 "500 ms at 7B" (recomputed 493.4–499.65 ms over 60 valid trials) vs L353 "527 ms" protection-off; §IV-G's derived "0.89 steps/position" inherits the rounding (true value 0.91) | R0-N12 · DA-NEW-8 (2; R2-m-5 holds it is *coherent*, see **A4**) | Apply the same two-quantity naming at 7B, and print 493 ms or "about 500 ms". Both note the rounding runs **against** the authors. |

---

## 3. Single-reviewer residuals worth acting on

| # | Defect | Location | Reviewer | Minimal fix |
|---|---|---|---|---|
| **S1** | **"496 rejected *solely* because no peer could reserve the backup weights"** is an attribution the instrumentation cannot make. `scheduler.py:403-416` catches `NoFeasibleSolutionError` and `NoRecoveryError` in one handler, drops non-finite objectives at a third silent `continue`, and counts only `feasible_count`; 496 = 1950 − 1454 is a residue of three causes. **At least 12 are provably something else**: `per_layer_bytes` = 406 953 984 → 32 layers = 12.13 GiB, and the largest Nano pair (`on-1`+`on-6`) holds 11.32 GiB, so all $P(4,2)=12$ ordered two-Nano subsets fail on **active** capacity before backups are considered | `evaluation.tex` L296–299 — the **only live datum in §IV-E** | **DA-NEW-3 (CRITICAL)** | Delete "solely": "496 were rejected as infeasible, the backup reservation among the causes." Optionally restore a per-reason counter and re-derive — that is an **offline** solver re-run over saved profiles, so the closed testbed is no defence. See §5. |
| **S2** | The new traffic concession is unscoped and contradicts §IV-B two subsections earlier | `evaluation.tex` L217–218 "Parity and replication ship the same KV columns from the workers, so their difference is coordinator memory, not traffic" vs L163–164 "\sys{} first fetches the four surviving columns from their workers, **four RPC round trips**" — traffic DejaVu does not pay, on the failure path, growing with the prefix | **R2-N3-3 (blocking #3)** | Four words: "…ship the same KV columns **during failure-free decoding**, so their steady-state difference is coordinator memory, not traffic; the parity path additionally fetches the surviving columns at recovery." |
| **S3** | KV-CARE's own **retained-input accounting** is asserted two ways, and the headline 3.7× depends on which is true. The metric charges Petals 40 kB/tok for per-position stage inputs; §III-B says KV-CARE's own ladder replays "from **the retained stage inputs**"; §III-A says the coordinator retains the input activation of "**the current position**" (singular) | `design.tex` §III-A/§III-B, unchanged; `evaluation.tex` Table I | **R3-NEW-4** — raised in R2, **never docketed by the panel** | One sentence, either direction. R3 correctly separates this from K4: K4's 2.7× is $(\Sigma\ell-\ell_{\text{tail}})/\ell_{\max}=304/112$, a stage-coverage adjustment; this route would give $416/152$ — the same number by a different derivation, which is why the conflation was easy. The panel must not assume the worse branch: **ask the authors to state it.** |
| **S4** | Table I's note says a position was "**not measured**"; the authors' own log says it was attempted and discarded | Table I note "not measured at $P{=}32$"; `b1_ft_fleet_7b_reactive_log_20260901.json` records `n_valid: 2` and `invalid: {"16": "survivor worker socket closed mid re-solve", "24": "boot not ready…", "32": "boot not ready…"}` — **three of five Reconfigure positions attempted and discarded, with reasons**, against a baseline whose 439 s is quoted as "minutes" | **DA-M7** (R1-M11 asks for the general accounting; DA names the case) | Replace "not measured" with the log's own words. This also discharges the cheapest half of the exclusion-accounting ask (roadmap 21) in one clause. |
| **S5** | "a per-position **spread** of 0.03–0.23 s" is the per-position **standard deviation** | `evaluation.tex` L156–157. R1 recomputed from the 15 valid parity trials: spread (max−min) = 0.053, 0.223, 0.154, 0.173, **0.443** s at $P=8,16,24,32,4$; SD = 0.027, 0.126, 0.077, 0.088, 0.231 s. The printed range understates the trial-to-trial range ~2× at $P{=}4$ | **R1-NEW-9** | Relabel "standard deviation", or quote 0.05–0.44 s. |
| **S6** | The restored cross-scale pair "1.6–1.9" does not reproduce | `evaluation.tex` L376. R1: the five valid 350M parity trials are 0.282–0.316 s → **1.73–1.93** against the paper's own 163 ms step (1.54–1.73 against 183 ms). The pair was restored from the pre-Round-1 text without being recomputed under the decode-step definition this round introduced | **R1-NEW-10** | Print 1.7–1.9. |
| **S7** | ±11 % is quoted where the unprotected baseline's own round-to-round SD is in the file and is 2.6 % | abstract L22–24 / `discussion.tex` L134–135; `protection_off.throughput_tokens_per_sec_sample_std` $=0.0492/1.9249=2.6\%$ | **DA-N7** | One clause, or drop the comparison. "Quoting 11 % where the measured baseline spread is 2.6 % still flatters the null." |
| **S8** | No MDE for the protection-cost result | `evaluation.tex` §IV-F. At $n=3$ the 95 % interval is $\pm t_2\cdot10.7/\sqrt3=\pm26\%$; a 20 % protection cost remains unexcludable | **R1-M4** | One clause. Note the authors *volunteered* the harder half ("these figures bound the cost rather than rank the modes"), which four reviewers credit. |
| **S9** | The benefit-of-$k$ row that arbitration **C2** substituted for the deleted `fig_storage_tolerance` was never added | Table I carries the State column (416 / 112 / 224 kB/tok); no tolerance row or column; replication's own tolerance is never stated | **R3-MAJOR-7** | One Table I-adjacent row: tolerated simultaneous failures against retained bytes. The arbitration granted the figure's deletion **in exchange for** this; half the bargain is unpaid. |
| **S10** | Journal fit: **1 IEEE IoT-J entry in 34**, 5 TII entries used only as §I motivation, no §II engagement with the journal's edge-reliability / redundancy-placement literature | `references.bib`; §II | **R0** — "I would make that a condition of acceptance … the difference between a paper that belongs here and a systems paper that happened to be submitted here" | Two or three §II paragraphs. The one dimension where three rounds of good-faith revision produced **no movement** (C1 held at 6, 6, 6). See conflict **A5** on where the page comes from. |
| **S11** | §IV-F's second out-of-scope declaration survives in the subsection whose own cost is network traffic | `evaluation.tex` L365–366 "Network traffic itself is not an evaluation axis in this paper" | **R3-CRITICAL-1 residual** | Delete the sentence; §V-A's "Network traffic and per-token energy were not measured" now covers it. |
| **S12** | §IV-E never receives the 12.4 GiB the metric section promises it | `evaluation.tex` metric section "…are accounted in Recovery Feasibility, not here", against §IV-E L331–334 | **DA-C1 residual** | One sentence in §IV-E receiving the byte total and stating DejaVu pays the identical reservation. |
| **S13** | rank($s$) inheritance after promotion is still undefined, so the validity of pre-failure $Q$ entries is undetermined | `design.tex` §III-D; $a_s=g^{\operatorname{rank}(s)}$ at L77 — a re-rank silently invalidates every existing $Q$ | **R2-M-8** (declared not-applied, item 30) | One sentence. Needs no testbed and no page. |

---

## 4. Letter-vs-file findings

**The headline finding is a clean one and the panel records it unanimously: no reviewer found a false statement of the Round-2 kind.** R0 checked all 15 MUST-FIX rows, both closing lists and the closing sentence clause by clause; R1 checked §§1–8 and the new Round-2 block; R2 checked the fifteen new rows plus the not-applied list; R3 checked fifteen MUST rows and sixteen SHOULD clauses; DA checked every clause against the files. The four Round-2 falsehoods (L1–L4) are **retracted by name, in place** — §1 now reads "Conclusion: not changed in Round 1 (the panel caught this); rewritten in Round 2"; §8 concedes the coordinator sentence "was already in the submitted version and is unchanged". R2: "the letter is now a document an editor can read alongside the files without being misled." R3: "the letter is now a usable verification instrument. That is a material change in how I read this submission." **Record this in the decision letter.**

What remains:

| # | Letter clause | Verified status | Found by |
|---|---|---|---|
| **L1** | Item 14: "'within two standard errors of zero'" | **The one false clause this round.** The k=2 half is honest (1.07 SE, verified by R1 and DA); the DejaVu half states what the authors' own fit contradicts (2.038 SE). | **DA §4 ("HALF FALSE")** · R1-NEW-8 |
| **L2** | Preamble: "**All fifteen MUST-FIX items** … are applied below" | **True but overstates completeness.** Item 1 is 6 of 7 (J1); item 7 is half (five-worker note absent); item 13 introduces a contradiction (J2). R0: "it is fourteen and a half … the completeness claim should be accurate, because that is precisely what this round was convened to check." | **R0-M1** · R2 §2 · DA §4 |
| **L3** | Applied-list 18: "**growth stated**" | **True of a direction, not of a bound.** "grows with the number of ordered subsets" is a restatement; the roadmap asked for the growth as a number and the measured solve time. Neither is in the paper — and DA located the solve time (122 s) in the authors' own `REPORT.md`. | **R0-M2** · R3 §2 · DA-M9 |
| **L4** | SHOULD-22: "**DOIs added**" | **Three of four.** `rashmi2016eccache` has neither DOI nor pages; `kosaian2019parity` lacks pages. R2's independent field audit found **no wrong field** in any of the five entries and recommends the production office resolve the DOIs mechanically. | **R0-M3** · R3 §2 · DA-N8 · R2 bib audit |
| **L5** | Row 13: "R defined over every stage, the head included" | **True of the implementation, false of the file.** Verified in `recovery_table.py:97-102` (skips only devices with no primary stage), the live 7B table (`REPORT.md:818`, head `on-6` backed) and `build_execution_plan`. The manuscript contradicts it at `design.tex` L31. The letter's own parenthetical rationale is the sentence the manuscript needs and lacks. | **R1 §2** · **R2 row 13** · DA §4 |
| **L6** | SHOULD-19: fidelity | **A skipped sub-part filed as applied.** The relative-error half was not applied and is **not on the not-applied list**. | **R1 §2** |
| **L7** | The not-applied list (17, 20, 21, 25, 30, 31, 34, 38, 41) | **Accurate and complete for MUST/SHOULD** — R1, R2, R3 and DA each verified it against the files. **Three or four items are missing from it**: roadmap 39 ("two to four"), 42 (restore the deleted hedge), K22's RAID lineage, and the K5 five-worker note. The list should be exhaustive. | R3 §2 · DA §4 · R1 §2 |
| **L8** | The reason given: "no data without the testbed, or out of page budget" | **A stretch for at least four items.** Items 20 (power mode / versions) and 21 (exclusion accounting) are desk work on facts and logs the authors hold; 30 (rank inheritance) is a design question; 41 (naming the decomposed trial) is in a log R1 and DA both read. R1: say "**deferred for space**", not "no data". | **R1 §2** · **R0 §2** |

**Editorial note.** The letter's remaining fault is a different and much smaller species than Round 2's: *scope claims* rather than *change descriptions*. Every change the letter describes is in the file, in the words the letter uses (R0). The authors should correct L1–L4 and complete L7 — this panel has now audited two letters clause by clause, and R0 is right that the value of the exercise depends on the scope claims being as exact as the change descriptions.

---

## 5. Devil's Advocate CRITICAL adjudication

Every DA CRITICAL, adjudicated visibly. C4 was resolved in R2 and is not re-opened.

| DA item | DA's R3 status | Corroboration / contradiction | **Adjudication** |
|---|---|---|---|
| **C1** — footprint excludes 12.4 GiB backup weights | PARTIAL; **DA withdraws its own Round-2 arithmetic objection**: "the implementation does back up the head and the number was right", verified in `recovery_table.py` and the live table | R1-NEW-7, R2, R3-MAJOR-2 all confirm 12.4 GiB is the full model and the correct branch was chosen | **RESOLVED as to the number.** The metric repair and the head charge are accepted by all five, and the charge runs *against* the authors (it tightens Eq. (mem) and moves Table III's band). Two residuals re-docketed elsewhere: the notation contradiction as **J2 (MUST)**, and §IV-E never receiving the byte total as **S12 (SHOULD)**. DA's self-correction is recorded. |
| **C2** — "flat in P" for a Θ(N) loop | Conclusion half **ADDRESSED**; **noise half MADE WORSE** | R0, R1, R2, R3 all grade the conclusion half ADDRESSED and independently reproduce the k=2 gap as 1.07 SE (honest). R1 independently computes the DejaVu slope at 2.04 SE | **UPHOLD the noise half as MUST-FIX (J3); the conclusion half is RESOLVED.** The fix delivered for the k=2 intercept is correct and under-specified only in that the SE of the difference is unprinted. The DejaVu clause is the manuscript's only false derived number and it undermines the *correct* 12 ms 2-SE bound two paragraphs above it — DA: "a reader who checks one 2-SE claim and finds it false has no reason to trust the other." |
| **C3** — latency conditional on the contract gate | **Withdrawn by DA**: "exactly the six-word fix asked for" | R1-M7, R2 row 5, R3-CRITICAL-3 all verify `abstract.tex` L24 | **RESOLVED.** Residual is cosmetic and single-reviewer: §V-B's closing sentence carries no contract qualifier where the abstract's does (R3-CRITICAL-3, six words) — folded into J1's edit, same paragraph. |
| **C5** — Petals credited 0 kB/tok; the domination claim | **ADDRESSED**, with a threshold residual | R0-item 2, R1-C2 ("a better sentence than the one the editorial asked for"; verified true against Table I), R2-C-1, R3-MINOR-10 all grade ADDRESSED; all four flag the 2-second boundary | **RESOLVED as the blocking item; residual DOWNGRADED to SHOULD-FIX (J7).** This was MUST-FIX #1 in R2 and it landed. Four reviewers independently confirm the rewritten sentence is not merely non-contradictory but *true* of Table I, and the generator docstring was fixed with it. What remains is one clause naming Petals as the low-state endpoint — a framing ask, not a correctness one, and nothing is hidden (the 40 kB is printed two paragraphs later). |
| **C6** — Eq. (mem) reserves weights while the abstract promised state | ADDRESSED in abstract and §III-E; **NOT in the Conclusion — now a self-contradiction** | **All four other reviewers name it, three as blocking**: R0-N9, R1-NEW-11, R2-N3-2, R3-NEW-B | **UPHELD, as MUST-FIX (J1).** Third round of asking; five words; in a paragraph the authors otherwise rewrote this round. This is the single most-cited item on the panel. |
| **C7** — feasibility: sequential baselines, offline label, the moved band | PARTIAL; four residuals | Abstract's "offline placement analysis" label verified by R0, R1, R2, DA (roadmap 37 ✓). Cap rule, 40-cap grid and six-worker pool verified against `d29_coupling_threshold_20260903.json` by R0, R1, R2 and DA independently; Table III's bands reproduce exactly | **Stays MAJOR; its residuals split.** The **"solely" attribution is promoted to the round's only CRITICAL (below)**. The five-worker band note is closed (conflict **A1**). The non-uniform grid is **J13 (SHOULD)**. The "sequential procedures fail" framing on three specific procedures and the absence of a *retrying* baseline (R2-M-7) stay MAJOR-but-not-blocking: no testbed can now settle them. |
| **C8** — a single AGX could host the whole model | PARTIAL, as before; the reconciliation was written on the evaluation side only | R3-NEW-6 independently: §I ¶1 unchanged beside §IV-A's concession | **ACCEPT the standing MAJOR; residual is SHOULD-FIX (J14).** §IV-A's "either AGX Orin could, so on this fleet the pipeline is the scheduler's throughput choice and capacity binds on the Nanos" is a volunteered, amplified concession. One clause in §I finishes roadmap 27. |
| **NEW-3 (new CRITICAL)** — "496 rejected **solely** because no peer could reserve the backup weights" | **The only remaining CRITICAL; DA makes its Minor conditional on it** | **No reviewer contradicts it, and no reviewer checked it.** R0, R1, R2 and R3 each verified the *1 950* enumeration and the letter's claims about it; none audited the 496 counter. DA's evidence is code-level and arithmetic: one `except` handler for `NoFeasibleSolutionError` **and** `NoRecoveryError`, a third silent `continue` on non-finite objectives, `feasible_count` the only counter, 496 = 1950 − 1454; and 12 of the 496 provably fail on active capacity (largest Nano pair 11.32 GiB < 12.13 GiB of layer weights). R1-M9(ii) is adjacent — it objects that the 496 figure is offered as evidence about a *sequential* procedure — but is a different point | **UPHOLD as MUST-FIX #1.** It qualifies on two independent grounds: an upheld DA CRITICAL (rule b) and a verified factual error against the authors' own source (rule a). This is exactly the class of over-attribution the panel has been retracting for three rounds, and it sits on the **only live datum in §IV-E** — DA is right that it is "the difference between 'we observed the coupling on a live fleet' and 'we simulated it'." **Two things follow, and they cut opposite ways.** (i) The closed testbed is **no defence**: the per-reason counter would be restored by re-running the *offline* solver over saved profiles, which the authors have already run twice. (ii) The correction is a **one-word deletion** that reduces a claim's strength, so upholding it does **not** return the paper to Major — see §6. The authors may take either branch; the panel does not require the re-derivation. |

**Summary.** Of eight original DA CRITICALs: **C4 resolved (R2), C1, C3 and C5 now resolved with residuals re-docketed, C6 upheld and MUST-FIX, C2 upheld in its noise half only, C7 and C8 standing MAJOR.** One new CRITICAL (NEW-3) upheld. DA additionally graded NEW-1 and NEW-2 as MAJOR; both are MUST-FIX here on other grounds (J3, J2). **DA withdrew eight items this round and corrected one of its own Round-2 errors on the record** — the panel notes this as the mark of a verification that was actually performed.

---

## 6. Decision

### **MINOR REVISION** — one round, editor-verified. No further external review; the corrections are word-level and checkable by grep and by one arithmetic recomputation each.

**Rationale.**

*The panel has converged.* Five of five recommend Minor or better, against 2 Minor / 2 Major / 1 conditional at R2. The two conditions that produced the Round-2 Major are discharged **and were verified against the files rather than the letter by every reviewer independently**: R0's items 1–2 (the conclusion, the 163/183 conflict, the stale figures) are fixed, with 163.3 ms and 183.17 ms reproduced from raw JSON by four separate reviewers; R2 ran the propagation check it had conditioned on and reports it "substantially passes"; R3's four silently-unanswered testbed-free items are now one fix (MAJOR-8, "the most serious untouched item", closed in two clauses) plus three items on an explicit, honest not-applied list attributed to a stated 10-page decision — which R3 rightly calls "a legitimate editorial trade openly made, and categorically different from silence."

*The letter is no longer the problem.* Round 2's decision turned as much on four false letter statements as on the manuscript. All four are retracted by name, in place. Five reviewers checked the new letter clause by clause and found one false clause (L1, the DejaVu 2-SE half), three completeness overstatements and three or four omissions from the not-applied list. That is a different and much smaller fault.

*What holds this back from Accept is small, specific and verified.* Four statements in the manuscript are contradicted by the authors' own files, code or logs: the DejaVu slope's "within two standard errors" (2.038 SE — J3), the "solely" attribution on the 496 rejected subsets (S1), Table I's "not measured at P=32" against a log recording three attempted-and-discarded positions (S4), and two small statistics that do not reproduce (S5, S6). Two more are internal contradictions the Round-2 edits themselves created: R's domain (J2, four reviewers blocking) and the Conclusion's un-propagated weights/state clause (J1, four blocking + DA). One consistency question about the headline 3.7× has been raised twice and never docketed (S3). A manuscript with those in it cannot be accepted as-is; a manuscript in which every one of them is a word, a clause or a symbol does not need another Major round.

*Against that.* Nothing on this list requires the closed testbed, and nothing requires a page — every MUST-FIX item below is a substitution inside an existing sentence. The intellectual work continued to run against the authors' own interest: the head charged at the full 12.4 GiB, the $n=3$ non-separation volunteered ("these figures bound the cost rather than rank the modes" — a concession no reviewer demanded in that form), the DejaVu baseline's centralization disclosed, the "four times the bytes" causal story retired rather than defended, 2.7× carried into the abstract beside 3.7×, the inverted Reconfigure pairing corrected against their own log. DA recomputed **every** derived number in the manuscript and found exactly one that fails; R0 and R1 did the same independently with the same result.

**Verification instruction for the next round.** The editor should check items 1–9 of the roadmap directly against the source and, for items 1, 4 and 6, against `b1_ft_fleet_7b_rep3.json`, `b1_ft_fleet_7b_reactive_log_20260901.json` and `radp/coordinator/scheduler.py`. No reviewer needs to be re-engaged.

**Significance ceiling (recorded, not blocking; unchanged for three rounds).** R0 holds Significance at 5 and R2 holds Novelty at 5, both deliberately and both saying they expect no movement: the benefit over replication is projected, never demonstrated in a configuration replication could not serve, and the experiment that would settle it can no longer be run. R2 adds the one thing that would move Novelty and costs a clause — J10's exactness argument. Both reviewers call the resulting ceiling a publishable outcome for IoT-J. What three rounds of revision bought instead is the property R2 names: "every remaining claim in the paper is one a reader can check against a printed number."

---

## 7. Revision Roadmap R3

Numbered, deduplicated, apply directly. **Every item is text, a symbol or a number the authors already hold. None requires the testbed. None requires a page** — MUST-FIX items 1–9 are substitutions inside existing sentences.

### MUST-FIX (blocks acceptance)

1. **`evaluation.tex` L296–299 — delete "solely".** "496 were rejected as infeasible, **the backup reservation among the causes**"; the counter is `1950 − feasible_count` over three failure paths. *(S1 / DA-NEW-3, upheld CRITICAL. Optional stronger fix: restore a per-reason counter and re-run the offline solver.)*
2. **`discussion.tex` L128–129 — propagate MUST-FIX 4.** "the recovered **state** has a device to **run** on" → "the failed stage's **weights** have a device to **load** on"; add "in an offline placement analysis" to the same paragraph's feasibility clause. *(J1 — R0-N9, R1-NEW-11, R2-N3-2, R3-NEW-B, DA-C6)*
3. **`design.tex` L8 — give R its own domain.** $R:\mathcal{S}^{+}\!\to\mathcal{D}$ with $\mathcal{S}^{+}=\mathcal{S}\cup\{\text{head}\}$, keeping $\mathcal{S}$ for the parity-protected non-head stages used by Eqs. (1)–(3), the padding rule and Alg. 1's $F\nsubseteq\mathcal{S}$. *(J2 — R0-N10, R1-NEW-12, R2-N3-1, R3-NEW-A, DA-NEW-2)*
4. **`evaluation.tex` L160–161 — the DejaVu slope is 2.038 SE, not "within two".** Write "about two standard errors from zero, in the one family that does no per-position work, which we read as an unmodelled systematic". *(J3 — DA-NEW-1, R1-NEW-8; the manuscript's only false derived number)*
5. **`evaluation.tex` L217–218 — scope the traffic concession.** "…ship the same KV columns **during failure-free decoding**, so their steady-state difference is coordinator memory, not traffic; the parity path additionally fetches the surviving columns at recovery." *(S2 — R2-N3-3; otherwise it contradicts L163–164)*
6. **`evaluation.tex` Table I note — "not measured at $P{=}32$" contradicts the log.** Use the log's own words: attempted at $P=16, 24, 32$ and discarded (survivor socket closed mid re-solve; boot not ready). *(S4 — DA-M7; also discharges the cheapest half of roadmap 21)*
7. **`design.tex` §III-A/§III-B — resolve KV-CARE's retained-input accounting**, in either direction: per-position stage inputs retained (Table I's row becomes 152 kB/tok) or one position only (and §III-B's fallback-ladder wording is corrected). The abstract's 3.7× depends on the answer. *(S3 — R3-NEW-4, raised twice, never docketed)*
8. **`evaluation.tex` L134–135 — footnote (b) credits the wrong artefact.** "byte identity **asserted in unit tests** (§IV-D)" — the fidelity probe measured 26.9 % divergence on the replay path. *(J4 — R0-N11, R2-N3-5; the change makes the annotation stronger, not weaker)*
9. **`evaluation.tex` L156–157 and L376 — two statistics that do not reproduce.** "per-position spread 0.03–0.23 s" is the SD (spread is 0.05–0.44 s); the restored "1.6–1.9" recomputes to **1.7–1.9** under this round's own decode-step definition. *(S5, S6 — R1-NEW-9, R1-NEW-10)*

### SHOULD-FIX

10. **`docs/paper/response_to_reviewers_*.md`** — correct L1 (item 14's DejaVu clause is false), L2 ("fourteen and a half of fifteen"), L3 ("growth stated" → a direction), L4 ("three of four DOIs"); add roadmap 39, 42, the RAID lineage and the K5 note to the not-applied list; change "no data without the testbed" to "deferred for space" for items 20, 21, 30, 41. *(§4 — all five)*
11. **`design.tex` §III-E + `evaluation.tex` §IV-E** — the solve is **122 s** (`experiments/REPORT.md:812`) and the enumeration is $\sum_{k=2}^{|\mathcal{D}|}P(|\mathcal{D}|,k)$: 1 950 at six devices, ≈9.8 M at ten. *(J5 — all five; DA located the number)*
12. **`evaluation.tex` §IV-C + §IV-A** — one clause: "Petals is the low-state endpoint at 40 kB/tok and 2.3 s", so the frontier reads as confidence rather than a boundary chosen to win. *(J7 — R0-N13, R2-N3-4, R3-NEW-C, DA-C5)*
13. **`design.tex` §III-F / `evaluation.tex` §IV-A — the environment paragraph.** JetPack/L4T, PyTorch/CUDA/gRPC versions, `nvpmodel`/`jetson_clocks` power mode, LAN rate, artifact statement. Two lines; all configuration facts the authors still hold. *(J9 — R1-m22 and R3-MINOR-1 each name it their top non-blocking ask)*
14. **`related.tex` §II-E** — name the RAID-5/RAID-6 (EVENODD/RDP) lineage Eqs. (1)–(2) instantiate, and add the novelty clause the corrected Parity Models wording unlocked: stored KV bytes admit an **exact** linear code where prediction serving admitted only a learned approximation. *(J10 — R2-C-2, R3-MAJOR-7, DA-N8; R2 calls the second clause the cheapest available raise to Novelty)*
15. **`evaluation.tex` Table II** — relative error beside the $2^{-8}$ absolute, or a line in the not-applied list. *(J6 — R1-M8, R2-M-5, R3-MAJOR-9, DA-M6)*
16. **`evaluation.tex` §IV-C, §IV-A, Table III** — move the "kB denotes 1 024 bytes" declaration out of Table I's note and extend it to the MB/MiB values; three conventions currently coexist. *(J8 — R0-W14, R1-m18, R2-m-3, R3-MINOR-7)*
17. **`evaluation.tex` L300–302** — say the 40-cap ladder is **non-uniform**, densened to 0.2 GB at the reported boundary. *(J13 — R1-M10, R0-W5, DA-NEW-6)*
18. **`abstract.tex` L15–16 + `discussion.tex` L142** — "two to three decode steps" → "two to **four**", or scope to the primary-victim sweep. *(J11 — R1, R2, R3, DA; conflict **A2**)*
19. **`introduction.tex` ¶1** — one clause reconciling the capacity premise with §IV-A's "either AGX Orin could". *(J14 — R3-NEW-6, DA-C8; finishes roadmap 27)*
20. **`evaluation.tex` L41 / L353** — apply the 163/183 two-quantity naming to the 7B twin (500 vs 527 ms), and print **493 ms** or "about 500 ms". *(J15 — R0-N12, DA-NEW-8; conflict **A4**)*
21. **`references.bib`** — DOI and pages for `rashmi2016eccache`; pages for `kosaian2019parity`. R2's field audit found no wrong field; have the production office resolve all five mechanically. *(J12 — R0-M3, R2, R3, DA-N8)*
22. **`evaluation.tex` Table I (adjacent row)** — the benefit of $k$: tolerated simultaneous failures against retained bytes (416 / 112 / 224 kB/tok), plus replication's own tolerance. This is the half of arbitration **C2** that was traded for deleting `fig_storage_tolerance` and remains unpaid. *(S9 — R3-MAJOR-7)*
23. **`evaluation.tex` §IV-F** — delete the surviving "Network traffic itself is not an evaluation axis in this paper"; §V-A's limitation now covers it. *(S11 — R3-CRITICAL-1)*
24. **`related.tex` §II + `references.bib`** — two or three paragraphs situating cross-stage parity against the edge-service fault-tolerance and redundancy-placement literature IoT-J itself publishes. One IoT-J entry in 34 is the only remaining scope objection an editor could raise. *(S10 — R0, "a condition of acceptance"; conflict **A5**)*
25. **`design.tex` §III-D** — one sentence on whether a promoted backup inherits rank($s$) and what becomes of pre-failure $Q$ entries. Design question; no testbed, no page. *(S13 — R2-M-8)*
26. **`evaluation.tex` §IV-E** — one sentence receiving the 12.4 GiB the metric section promises it, and stating DejaVu pays the identical reservation. *(S12 — DA-C1 residual)*

### OPTIONAL

27. `evaluation.tex` §IV-F — the MDE: at $n=3$ the 95 % interval is ±26 %, so a 20 % protection cost is unexcludable. *(S8 — R1-M4)*
28. `abstract.tex` / `discussion.tex` — note that the unprotected baseline's own round-to-round SD is 2.6 %, against the ±11 % quoted. *(S7 — DA-N7)*
29. `evaluation.tex` §IV-A — full exclusion accounting: trials attempted vs reported per cell, per-gate discards. Roadmap item 6 above covers the concrete case; this is the general form. *(R1-M11, DA-M7)*
30. `evaluation.tex` §IV-A — state the injection exception where the "one injection mechanism" claim is made (L54–55), not 24 lines later. *(R1-C3(c))*
31. `evaluation.tex` §IV-E — define the 7B "whole fleet" membership; name `on-3`/`on-4`. *(R1-M10, R0-item 26)*
32. `discussion.tex` — restore the deleted hedge "the case the recovery contract is designed for" (roadmap 42, silently dropped); §V-B's closing sentence takes the abstract's contract qualifier. *(R1-M7, R3-CRITICAL-3)*
33. `evaluation.tex` §IV-B — the freeze/fetch/decode/install/promote breakdown of the ~1.2 s intercept, and the implied ~70 ms per round trip. Logging change, not a measurement. *(R2-M-6, R3-MAJOR-3, DA-M11)*
34. `main.tex` — `\input` a trimmed operating-envelope table from `background.tex`, **or delete the orphan file**. Three reviewers note it is still on disk and un-`\input`. *(R2-m-1, R3-MINOR-5, DA-N9)*
35. `abstract.tex` — trim 258–260 words to <250 (roadmap 36, unapplied); the freed lines fund item 13 or 24. *(R0-W17, R1-item 36)*
36. `evaluation.tex` §IV-C / `discussion.tex` — GQA/INT4 generality arithmetic; `design.tex` §III-E — an $\ell_{\max}$ term in $\rho$; `evaluation.tex` §IV-A — coordinator capacity and availability arithmetic. All three are **declared** page-budget declines (items 31, 34, 17); R0 and R3 both record them as the items that would move Significance and Practical impact. *(R3-MAJOR-4/-11/-1, R0-C3)*

---

## Reviewer conflicts arbitrated

**A1 — The five-worker 6.5 GB provenance (roadmap 7's second clause): "still not addressed" vs "no longer worth an erratum".**
R2-NEW-7 grades it NOT ADDRESSED ("item 7's second clause unfilled") and DA-NEW-7 notes four reviewers asked and it remains letter-only. Against: R0-M1 declines to press it — "an unpublished draft's superseded number needs no erratum, and the printed band is the one the six-worker artifact supports"; R1-NEW-6 grades the item ADDRESSED "since the 6.5 GB band was never published"; R3-NEW-7 explicitly downgrades it "from a verification concern to a nit" because the pool and grid are now fully specified. **Resolution: CLOSE it.** Three of the four reviewers who raised it in Round 2 have withdrawn or downgraded on the same reasoning, and the number never reached a reader. What survives is a *letter* fault, not a manuscript fault: the letter should stop implying item 7 was fully applied (roadmap item 10). Removed from MUST and SHOULD entirely.

**A2 — "two to three decode steps": press it or not.**
R0 grades item 39 NOT ADDRESSED and then says "Optional in the roadmap; I do not press it", noting §IV-B added the reconciling scope sentence. R3-NEW-D calls it "the only place where the summary sentences round in the paper's favour" now that the other six qualifiers are in. R1-M14(c), R2-N3-6 and DA-NEW-5 all list it open. **Resolution: SHOULD-FIX (item 18), and the real fault is procedural.** The substantive gap is one word. But R3 and DA both observe it was *neither applied nor listed as declined* — and roadmap 42 with it. A not-applied list is only a verification instrument if it is exhaustive, which is why the letter correction (item 10) carries as much weight here as the text edit.

**A3 — R's domain: widen the symbol, or shrink the number.**
R0, R1, R2 and DA all say widen ($\mathcal{S}^{+}$ / $\mathcal{S}_{\mathrm{all}}$). R3-NEW-A offers the alternative: "drop 'the head included' and use ~10.1 GiB in §IV-A". **Resolution: widen the symbol.** R2 and DA each verified against the implementation (`recovery_table.py:97-102` skips only devices with no primary stage; the live 7B table backs up head `on-6`; the head exclusion exists only for KV parity in `gateway.py:971`) that the system **does** back up the head, so 12.4 GiB is the number the code supports and R3's alternative would print one it contradicts. Take R2's supplement in the same edit — add the letter's own rationale to the paper: the head's weights are preloaded so a head failure degrades to full-prefix replay on a live worker rather than losing the request, although its KV is not parity-protected. That also answers DA-NEW-2's second-order point (why ~2.3 GiB is reserved for a stage §III-B puts outside the guarantee), which is otherwise unexplained.

**A4 — 500 vs 527 ms at 7B: a defect or already coherent?**
R2-m-5 holds the 7B half is "now coherent by the same construction" — L38–39 defines the decode step as the sweep's median, L353's 527 ms is the protection-off runs, so no error exists. DA-NEW-8 and R0-N12 both say a reader just taught that 163 ≠ 183 will ask about 500 vs 527 and find no answer, and R0 adds that 500 is a rounded 493.4 that propagates a 1.3 % offset into §IV-G's 0.89. **Resolution: SHOULD-FIX, not MUST (item 20).** R2 is right that nothing is *wrong*; the other two are right that the fix the authors just applied at 350M was not applied to its twin — the same failure mode as J1 and J2, one register down. Note that the rounding runs **against** the authors (0.91 is nearer their own "about one decode step" claim), so this is a precision disclosure, not an inflation.

**A5 — Where the page goes, if one is found.**
R0 makes two or three §II paragraphs on IoT-J's own edge-reliability literature "a condition of acceptance" — the one dimension where three rounds produced zero movement. R3 says "if a page becomes available at proof, I would spend it on item 20's environment paragraph before anything else." **Resolution: no real conflict — both fit.** The environment paragraph is two lines (item 13); the §II paragraphs are roughly two-thirds of a page (item 24). Roadmap item 35 recovers space the authors already agreed to (abstract 258–260 → <250, roadmap 36 unapplied), and items 23 and 34 remove two dead passages. Both are SHOULD-FIX, and the panel does not require either against the authors' declared 10-page trade. But R0's point stands on its own terms: this is the only remaining reason an editor could give for declining on scope, and it costs an afternoon.

**A6 — DA-NEW-3 and the closed-testbed rule.**
The standing constraint says items needing new fleet measurement are judged on scoping alone. **It does not apply here.** DA's requested fix is a one-word deletion, and its stronger alternative — a per-reason counter — needs only a re-run of the *offline* solver over profiles the authors have already replayed twice. Neither branch touches the fleet. The constraint protects the authors from being asked for measurements they can no longer make; it does not protect an attribution the code they still hold contradicts.

---

*Prepared by the Editorial Synthesizer. Every item above traces to a named reviewer report and item id. The manuscript was not read or modified in producing this decision.*
