# Editorial Decision — Round 2 (Verification) — KV-CARE (IEEE IoT-J)

**Date:** 2026-09-03 · **Round-1 decision:** Major Revision (unanimous, 5/5)
**Basis:** five verification re-reviews (`R0/R1/R2/R3/DA_rereview.md`) checked against `EDITORIAL_DECISION.md`.
**Standing constraint honoured:** the testbed is closed. Items needing new fleet measurements are judged only on whether the limitation is stated and the claim scoped.
**Rule used for MUST-FIX:** an item is MUST-FIX only if (a) ≥2 reviewers raise it, (b) it is an upheld Devil's Advocate CRITICAL, or (c) it is a verified factual/internal-consistency error. Everything else is SHOULD/OPTIONAL.

---

## 1. Panel table

| Reviewer | R1 rec | R2 rec | ADDRESSED | PARTIAL | NOT | WORSE | Items traced | Score movement |
|---|---|---|---|---|---|---|---|---|
| **R0** Journal-Fit | Major | **Minor** (conditional: "if items 1 and 2 are not fixed, this returns to Major") | 3 | 8 | 4 | 2 | 17/17 | Fit 6→6 · Originality 6→7 · Significance 5→5 · Clarity 6→7 · **Claims-vs-evidence 4→6** |
| **R1** Methodology | Major | **Minor** ("accept after items 1–8") | 2 | 13 | 8 | 1 | 24/24 | Exp. design 5→6 · **Stat. validity 4→4** · Metric defs 6→7 · Reproducibility 3→4 · Limitations 6→7 |
| **R2** Domain | Major | **Minor** (conditional on editorial verification of propagation) | 4 | 12 | 11 | 1 | 28 (27 issues + missing-ref row) | Literature 5→6 · Tech. soundness 5→6 · **Novelty 5→5** · **Fairness 5→7** |
| **R3** IoT practitioner | Major | **Major** ("materially closer; no new measurements required") | 3 | 10 | 11 | 1 | 25/25 | Deployability 4→5 · Motivation–eval match 3→5 · Practical impact 5→6 · **Clarity for practitioners 6→6** |
| **DA** Devil's Advocate | Major | **Major** ("one round; verification of the conclusion and §IV-B against the tables required") | 2 | 11 | 7 | 1 | 21/21 (C1–C8, M1–M13) | no numeric rubric; verdict: **1 CRITICAL withdrawn (C4), 3 CRITICAL clusters remain blocking** |

**Panel arithmetic:** 3 Minor / 2 Major. But R0's Minor is explicitly conditional and **its condition is currently unmet** (its items 1–2 are the unfixed conclusion and the 163/183 ms + stale figures), and R2's Minor is conditional on a propagation check that this panel has now performed and failed. Net: 1 unconditional Minor (R1), 2 conditional Minors evaluating to Major, 2 Majors.

**Where the panel agrees on progress** (recorded, because it is unanimous): the abstract's over-claims are genuinely repaired; Table I's two errors are fixed and the Petals correction runs *against* the authors; the Θ(N) loop, the 2-SE bound and the refusal to extrapolate are stated; the P≈10 Petals crossover, the 2.7× tail-stage markdown and the "either AGX Orin could" concession are all volunteered; a real second sequential baseline (headroom 0.75×/0.5×) was built and reported; the coded-computation lineage is cited. R1 and DA independently recomputed **every** derived number in the paper and found no arithmetic that fails to reproduce.

---

## 2. Consensus residuals (≥2 reviewers)

| # | Defect (one line) | Location as quoted by reviewers | Raised by | Minimal text fix asked for |
|---|---|---|---|---|
| **K1** | The Conclusion is byte-identical to the pre-revision file and reasserts six claims the abstract retracted | `sections/discussion.tex` L115–132 (§V-B): "regardless of the failure position"; "3.7× less **state**"; "costs **no measurable** failure-free throughput"; "restores two simultaneous stage failures … in about a second"; "at **every** per-device memory cap"; "turns **a stage failure** … into a delay of two to three decode steps" | R0-N1/W1 · R1-NEW-4 · R2-NEW-4 · R3-C2/C4/M6 · DA-N2 (§3, "six for six") | Paste the abstract's own six qualifiers into that one paragraph. |
| **K2** | The paragraph that interprets Fig. 2 contradicts the revision's own Table I, figure and abstract | `evaluation.tex` L193–196: "Recompute, **Petals**, and Reconfigure **retain nothing**"; "DejaVu **matches** \sys{} in latency"; "\sys{} **alone** sits in the low-latency, low-state corner". Docstring of `figures/paper/make_recovery_pareto.py` carries the retracted claim too | R1-NEW-1 · R2-NEW-5 · R3-NEW-2/MINOR-10 · DA-N1/C5 (**"the item I would put first in an editor's letter"**) | One sentence naming the three non-dominated points at P=32 — DejaVu (0.77 s, 416 kB), KV-CARE (1.42 s, 112 kB), Petals (2.31 s, 40 kB) — and dropping "matches"/"alone". |
| **K3** | Two different quantities are called "one decode step" at 350M | `evaluation.tex` L36–38 ("163 ms at 350M") vs L48 ("183 ms token interval") vs L344 ("where a token takes 183 ms") | R0-N2 · R1-NEW-2 · R2-NEW-3 · R3-NEW-5 · DA-N5 | Pick one, or name the two quantities separately; R1 reproduced 163.3 ms as the pooled sweep median, so both are real. |
| **K4** | The headline ratio 3.7× stands in the abstract and conclusion against the body's own 2.7× | abstract L19; `discussion.tex` L128; against `evaluation.tex` L204–206 "over the four stages the parity path recovers, the ratio is 2.7×" | R0-N4 · R2-NEW-6 · R3-C4/NEW-4 · DA-M2 | Carry "3.7× overall (2.7× over the stages the parity path recovers)" in the abstract and conclusion. |
| **K5** | Table III's band moved in the authors' favour (6.5→4.8 GB) on an unannounced offline re-run; explained only in the letter | `evaluation.tex` L277 "4.8 GB ≤ c ≤ 23 GB (uncapped)"; "Neither" 6.0→4.6 GB | R1-NEW-6 · R2-NEW-7 · R3-NEW-7 · DA-C7(3) | One clause in §IV-E: the sweep uses all six CUDA workers; an earlier five-worker pool gave the narrower band. |
| **K6** | Figures were not regenerated and now conflict with the corrected prose | `fig_recovery_latency.pdf` (mtime Sep 3 11:14 < source edits 16:35) legend "Reconfigure (**median** 439.44 s)", k=1 series unlabelled, both KV-CARE series same colour; `fig_recovery_pareto` x-axis "recovery latency at $P=32$" with Reconfigure plotted at its median over P=4,8; Fig. 2 caption unchanged | R0-W12/W13/N3 · R1-C3(b)/M4(c) · R3-NEW-3 | Regenerate: "mean" for n=2, label k, distinct markers/colours, drop or footnote the "at P=32" axis label, un-clip the k=1 whisker (`make_protection_cost.py` `YLIM=(-4,15)` vs a −11.36% whisker). |
| **K7** | "Four times the bytes" is offered as the cause of a position-independent 0.28 s intercept, and the ratio is wrong | `evaluation.tex` L158–160 | R1-NEW-3 (280 ms needs a ~10 Mb/s LAN; the payload is ~344 kB) · DA-N3 (geometry gives 5.5×, not 4×; at the intercept N=0 so no columns are fetched) | Mark it an interpretation, or say **round-trips** rather than bytes; a byte-volume cause belongs in the slope. |
| **K8** | Table I's implausible fits pass without comment: DejaVu −6.0±3.0 ms/pos, and k=2's intercept (1128±77) below k=1's (1234±63) | `evaluation.tex` L121, L157–158; Table I | R1-M5 · R2-M-6 · R3-MINOR-4 · DA-C2 residual | One sentence each: the negative slope signals an unmodelled systematic (state trial ordering / power mode); the k=2 < k=1 difference is 106±100, i.e. within noise. R1 additionally asks for HC standard errors, since the same OLS produces the ±3.2 on which the 12 ms bound rests. |
| **K9** | Three axes the letter files as "stated as limitations" are absent from Limitations: energy, concurrency, prompt length | `sections/*.tex`: grep for energy/power/watt/joule = **zero hits**; no Limitations sentence naming concurrency or prompt length as unswept — while `discussion.tex` L10–11 newly *claims* the regime "many concurrent requests" | R0-W3/N5 · R1-M16 · R2-M-10/NEW-8 · R3-MINOR-3/-6 · DA-M12 | Three clauses in Limitations: per-token energy of protection not measured; every measurement is single-request; prompt length and concurrency not swept. |
| **K10** | The abstract's feasibility guarantee is not what Eq. (mem) enforces | abstract: "so the recovered **state** always has a device to run on"; `design.tex` L192–195 Eq. (mem) contains only weight terms and is unchanged; `introduction.tex` L52–54 still claims coupling "in **one system**" | DA-C6 (upheld CRITICAL) · R2-C-3 | Five-word edit: "the failed stage's **weights** always have a device to load on", plus one sentence in §III-E that Eq. (mem) charges weights only and KV/parity memory is coordinator-side. |
| **K11** | The abstract's closing operational claim carries none of the injection conditioning that Limitations supply | abstract: "On site, a protected-stage failure becomes a delay of a few decode steps", against `discussion.tex` L72–77 "the reported latencies characterize the parity path rather than the average over all failure timings" | DA-C3 residual · R3-C3 fix 3 · R1-M7 residual | Six words: "…when the recovery contract holds". |
| **K12** | Placement search cost, iteration bound and complexity are absent for the paper's third contribution | `design.tex` L200–205 "after **a bounded number of iterations**" (no number); `evaluation.tex` L285–287 reports 1,950 enumerated subsets with no wall-clock | R0-W8 · R2-M-4 residual · R3-MAJOR-10 · DA-M9 | One sentence: iteration bound, measured solve time (in the authors' logs), and the enumeration's growth in \|D\| (1,950 at 6 devices → ~9.8M at 10). |
| **K13** | "Bit-exact" printed bare in a table captioned "(live, OPT-6.7B)" while argued by construction; fidelity has no demonstrated consequence | `evaluation.tex` L111–112 caption; Table I fidelity column; Table II reports max abs diff 2⁻⁸ with no relative error or scale | R0-W9 · R1-M8 · R2-M-5 · R3-MAJOR-9 · DA-M6 | Annotate the column "(by construction)"; add relative error beside the absolute; add one sentence that **no measured trial's decoded output diverged**, including the 26.9%-divergent replay families. |
| **K14** | kB used for 1024 B throughout, now propagated to a new number | `evaluation.tex` L200–201 "425 984 bytes … (416 kB)", L212–213 "832 MB / 224 MB", Table I header "State (kB/tok)", new Petals 40 960 B → "40" — beside a correct "12.4 GiB" and decimal-looking "4.8 GB" | R0-W14 · R1-m18 · R2-m-3 · R3-MINOR-7 | Declare KiB/MiB, or convert; three conventions currently coexist in one section. |
| **K15** | Reproducibility metadata absent, and now permanently unobtainable | `design.tex` L219 "Python with PyTorch and gRPC" (no versions); grep for JetPack/nvpmodel/jetson_clocks/tegrastats = zero hits; "a shared LAN" with no rate; no artifact statement | R1-m22/M5 · R3-MINOR-1/-2 | One environment paragraph: JetPack/L4T, PyTorch/CUDA/gRPC versions, Jetson power mode, LAN rate, prompt text, artifact availability. R1: the absent `nvpmodel` statement is "the single most consequential missing environment fact". |
| **K16** | Exclusion accounting absent, and the hedge that covered it was deleted | `evaluation.tex` L95 "all trials below passed these gates" (pre-revision read "…**unless stated**") | R1-M11 · DA-M7 | One sentence with trials attempted vs reported per cell, and per-gate discards — a strong result if the count is zero. |
| **K17** | 12.4 GiB is described as "a second copy of **every** stage" although R is defined over protected, non-head stages | `evaluation.tex` L86–87 and L32–33 vs `design.tex` L9–13, L31–32 | R1-NEW-7 · DA-N6/C1(3) | Fix one of the two; protected-only is ~10.1 GiB. (Runs against the authors' interest, so it is a consistency defect, not inflation.) |
| **K18** | Operator TTR is never stated, although the revision newly publishes a 5 s heartbeat timeout against a 1.2–1.4 s recovery | `design.tex` L216 "1 s ticks, 5 s timeout" vs `evaluation.tex` L70–73 excluding detection | R3-NEW-1 · R1-M7 · DA-C3 | One sentence: with asynchronous forwarding, TTR = recovery + up to 5 s detection; the reported figures are the recovery term. |
| **K19** | The named baselines' deviations are unstated; the DejaVu baseline centralizes where the real design distributes | `evaluation.tex` L63–65 (re-implementation flag added) vs `related.tex` L36–39 (real DejaVu streams "to a replica") | R1-M15 · R2-M-9 | One clause: our DejaVu baseline stores replicas at the coordinator; a faithful distributed DejaVu is ~83 kB/tok per node, below parity's 112 kB. Plus: whether KV shipping blocks the forward path. |
| **K20** | "Two to three decode steps" coexists with the paper's own 2.1–3.9× | abstract; `evaluation.tex` L150 and L179–181; `discussion.tex` L132 | R0-W1 · R1-M14(c) · R2-m-5 | "two to four", or scope the claim to the first-victim sweep. |
| **K21** | No preliminaries / operating-envelope section; `background.tex` exists on disk and is not `\input` | `main.tex` L93–97 | R2-m-1 · R3-MINOR-5 · DA-N9 | Either `\input` a trimmed envelope table (fault model, forwarding mode, network, concurrency, protected stages, out-of-scope) or delete the orphan file. |
| **K22** | Citation defects in the added lineage | `related.tex` L40 names PagedAttention with **no citation**; RAID-5/6 (EVENODD/RDP) lineage that Eqs. (1)–(2) instantiate still unnamed; four new bib entries lack DOIs, two lack pages (the letter itself says "please verify volume/page fields") | R2-C-2(d)/bib audit · R3-MAJOR-7 · DA-N8 | Add `kwon…`; one clause naming the RAID lineage; complete the four entries' DOI/page fields. |
| **K23** | Network declared out of scope twice, with no derived bytes-moved figure | `evaluation.tex` L351 and `discussion.tex` L99–100 | R2-M-1 · R3-C1 · DA-M10 | **Do not add a network axis** (see §7 conflicts). One concession sentence: parity and replication ship identical bytes by construction, so the comparison is purely coordinator memory. |

---

## 3. Single-reviewer residuals worth acting on (text/arithmetic only)

| # | Defect | Location | Reviewer | Minimal fix |
|---|---|---|---|---|
| S1 | Network partition is listed as fail-stop with no fencing/lease/epoch anywhere; a returning partitioned worker's behaviour is undefined; R does not model failure domains | `design.tex` §III-B, unchanged word for word; grep for epoch/lease/fenc/split-brain/failure domain = **zero hits** | R3-MAJOR-8 (calls it "the most serious untouched item"; "the testbed is closed is no excuse") | Two sentences in §III-B/§III-E: either describe fencing, or state plainly that a returning partitioned worker is out of scope and that R does not model correlated failure domains. |
| S2 | Coordinator capacity unstated, no term in Eq. (mem), no availability arithmetic — and the letter cites a **pre-existing** sentence as the fix | `evaluation.tex` §IV-A "The coordinator runs on a Jetson AGX Xavier" (8/16/32 GB variants exist) | R3-MAJOR-1 | State the coordinator's memory, add the per-request sizing formula (k·ℓ_max·N_ctx), and one paragraph on what an unprotected coordinator does to fleet availability. |
| S3 | fp16-MHA-OPT is never named as a generality threat; no GQA or INT4 arithmetic | `evaluation.tex` §IV-C 416 kB/tok, 832 MB; Table III band | R3-MAJOR-4 (arithmetic on published model geometry; testbed-free; marked mandatory in R1) | Recompute the footprint for a comparable GQA model, name fp16-MHA-OPT as a threat, and say whether the Table III band is expected to survive INT4. |
| S4 | The placement objective ρ has no ℓ_max term although ψ sets ℓ_max and therefore the whole coordinator memory cost | `design.tex` §III-E Eq. (rank), unchanged | R3-MAJOR-11 | One paragraph: ψ couples to R **and** to the parity footprint; the current objective ignores the second coupling. This extends the paper's own thesis. |
| S5 | "Advantage widens with **pipeline depth**" — the ratio Σℓ_s/ℓ_max is a balance statistic, and Limitations say so three pages later | `evaluation.tex` §IV-G; `discussion.tex` "The storage advantage depends on placement geometry" | DA-M1 | Delete three words ("with pipeline depth"). |
| S6 | α in Eq. 5's blended mode is undefined, never valued, and the mode is never used | `design.tex` L166–168 | R0-W7 | Delete the blended mode (cheapest), or define α. |
| S7 | Symbol collisions: D (device set vs KV block D_s[r,q], now aggravated by the new R:S→D) and c (cost c(i,j,d) vs memory cap c) | `design.tex` L9, L151, L154, Eq. 8 vs Eq. 1; Table III | R0-W7/N8 | Rename the device set (e.g. 𝒟) and the memory cap; W_ψ and P were fixed the same way. |
| S8 | Table I pairs Reconfigure's values with positions in the wrong order | `evaluation.tex` L122 "413.1 and 465.8 at P=4,8" vs the log: **P=4→465.805 s, P=8→413.069 s** (`b1_ft_fleet_7b_reactive_log_20260901.json`) | R1-NEW-5 | Swap. Verified factual error against the authors' own data. |
| S9 | The letter's "decompose the logged trial" leaves ~59 s unattributed and does not identify which trial | `evaluation.tex` L166–168 (250+130=380 s vs 439.4 mean; 33 s remainder at 413.1 s, 86 s at 465.8 s) | R1-m20 · DA-M8 | Name the trial and the remainder. |
| S10 | Cross-scale ratios lost their supporting numbers in this revision and are still unnormalized | `evaluation.tex` §IV-G: "(24.25 to 439.4 s at the median)" and "(1.6–1.9 to 2.1–3.2)" both deleted; sentence now ungrammatical | R1-M17 (**MADE WORSE**) | Restore the numbers and normalize by each deployment's token interval: Recompute is 164/163 = 1.01 steps/pos at 350M vs 447/500 = 0.89 at 7B — one pipeline pass per position at both scales. |
| S11 | No roadmap sentence at the end of §I, in a paragraph the revision otherwise rewrote | `introduction.tex` L72–82 | R0-W16 | One sentence. |
| S12 | Length moved the wrong way twice: abstract 253→270 words, paper 9→10 pp. against an 8 pp. target | compiled `main.pdf` | R0-W11/W17/N6 | Trim the abstract's first four framing sentences; compress §IV-G (see §7 conflict resolution). |
| S13 | The ±11% is attributed to a quantity the body never reports | abstract L23–25 vs `evaluation.tex` L340 (±10.7% is the SD of the parity/off **ratio** over three rounds) | R0-N7 · DA-N7 | "within the ±11% round-to-round spread of this measurement". |
| S14 | The 12 ms/pos bound is never carried through to the 2048-token context the paper itself projects storage for | `evaluation.tex` §IV-C projection | R3-C2 residual | Either carry it through or state explicitly that the projection is a storage projection, not a latency one. |
| S15 | Deleted disclosure: "the case the recovery contract is designed for" was removed from Limitations | pre-revision `discussion.tex` L72–73 | R1-M7 | Restore the clause. |
| S16 | Table III cap semantics still prose; sweep grid unstated; "whole fleet" undefined at 7B | `evaluation.tex` L288–292 vs L49–50 and "the CUDA workers" | R1-M10 · R0-W5 | Write M(d)←min(free(d),c) as an equation, state the 0.2 GB grid, and name the 7B "whole fleet" membership. |
| S17 | Promoted-backup correctness after re-ranking is unaddressed: does the backup inherit rank(s), and what happens to existing Q entries? | `design.tex` §III-D | R2-M-8 | One sentence. Needs no testbed. |
| S18 | m23: the sequence-equality gate is weakest exactly where the prefix is longest (36-token generations, P=32 exercises 4 tokens) | `evaluation.tex` §IV-A gates | R1-m23 | One clause acknowledging the gate's varying strength. |

---

## 4. Response-letter mismatches (claims not supported by the file)

| # | Letter claim | Verified status | Found by |
|---|---|---|---|
| L1 | §1: "**Conclusion aligned.**" | **False.** `diff` of the `\subsection{Conclusion}` block against the pre-revision snapshot returns empty; six retracted claims still stand. | R0 §0 · R1-NEW-4 · R2 §0/NEW-4 · R3-NEW-8 · DA §3 (all five, independently) |
| L2 | Closing block: "Not addressed (require the testbed) — stated as limitations … **energy**" | **False.** `grep -riE "energy\|power\|watt\|joule" sections/*.tex` → no match anywhere in the manuscript. | R0-N5 · R2-NEW-8 · R3-MINOR-3 |
| L3 | Closing block: "**longer prompts and concurrency**" stated as limitations | **False.** Neither appears in the Limitations paragraph (`discussion.tex` L63–100) or Future Work; concurrency appears only as a driver of coordinator memory — and, newly, as a regime where the claim *holds*. | R2-M-10/NEW-8 · R1-M16 · DA-M12 |
| L4 | §8: the coordinator item, citing a Limitations sentence as the change | **Not a change.** The cited sentence was already present in the submitted version. | R3-MAJOR-1/NEW-8 |
| L5 | §2: "Fig. 2 (Pareto) moves the Petals point accordingly" | **True of the figure and table, false of the text that reads them** — `evaluation.tex` L193–196 was never opened; the generator's docstring still asserts "Only KV-CARE sits in the low-latency AND low-storage corner." | R2-NEW-5 · DA-C5/N1 · R1-NEW-1 · R3-MINOR-10 |
| L6 | §4: "no 'median'" | **Partly false.** Removed from the table and prose; `fig_recovery_latency.pdf` legend still reads "Reconfigure (median 439.44 s…)" and `make_recovery_latency.py:79` still formats it that way. | R0-N3 |
| L7 | §5: the six-worker re-run and the five-worker pool error | **True but manuscript-invisible.** The explanation exists only in the letter; §IV-E and Table III carry no note, so a reader holding the earlier 6.5 GB band cannot reconcile it. | R1-NEW-6 · R2-NEW-7 · R3-NEW-7 · DA-C7(3) |
| L8 | §6: "Bibliography entries added (**please verify volume/page fields before submission**)" | **Self-flagged as unverified.** R2's independent field audit found no wrong field, but all four lack DOIs and two lack pages, breaking the property that the prior 28 entries all carried DOIs/canonical URLs. | R2 bib audit · DA-N8 |
| L9 | DA-C6 (Eq. mem reserves weights only) | **Absent from the letter entirely** — not in §1–§8 and not in the "not addressed" list. | DA-C6 |
| L10 | R3-MAJOR-4, -8, -11 and R0-W8 | **Not mentioned anywhere in the letter** — silent non-response on four testbed-free items. | R3 traceability · R0-W8 |

**Editorial note.** Two reviewers (R2 §0, R3-NEW-8) say explicitly that a false letter claim caused them to stop trusting the letter and verify the files line by line. DA calls L1 "a verification failure on the response letter itself, independent of the merits." The authors must correct the letter before resubmission; the next round's letter will be checked against the files clause by clause.

---

## 5. Devil's Advocate CRITICAL adjudication

| DA item | DA's R2 status | Corroboration / contradiction | **Adjudication** |
|---|---|---|---|
| **C1** — footprint excludes 12.4 GiB backup weights; 3.7× over the residue | PARTIAL; three residuals | Metric repair confirmed by R0-W2, R1-C2, R2-C-3, R3-MAJOR-2. The 12.4 GiB "every stage" inconsistency independently found by R1-NEW-7. | **DOWNGRADE to MAJOR.** The scoping move (request-proportional vs deployment-constant) is accepted by four reviewers. Two residuals survive as MUST-FIX under other headings: the conclusion's unscoped "3.7× less state" (K1) and the every-stage/protected-only contradiction (K17). One SHOULD-FIX: §IV-E never receives the byte total the metric section promises it. |
| **C2** — "independent of the failure position" for an O(N) loop from a 32-position window | ADDRESSED in abstract/body; NOT in conclusion; noise half NOT addressed | R0-W6 grades it **ADDRESSED**; R1-C1, R2-C-4, R3-C2 grade PARTIAL, all with the same single residual (the conclusion). | **DOWNGRADE to MAJOR as a standalone item** — the fix DA asked for was delivered with a mechanism, a 2-SE bound and an explicit refusal to extrapolate, which R0 calls "more than I asked for". The conclusion half is upheld inside K1 (MUST-FIX); the noise half is upheld as K8 (MUST-FIX). |
| **C3** — latency conditional on a contract gate the injection guarantees | PARTIAL; abstract carries no trace | R3-C3 fix 3 and R1-M7 residual make the identical request (the qualifier must reach the abstract). | **UPHOLD as MUST-FIX (K11), reduced in size.** The Limitations disclosure DA quotes is exemplary; what remains is a six-word edit to the abstract's closing sentence. |
| **C4** — "matches replication's latency" contradicted by the paper's own endpoint | **Withdrawn by DA** | R0, R1, R2, R3 all grade the abstract sentence fixed. | **RESOLVED.** But note the banned word "matches" survives at `evaluation.tex` L194 — that is K2, not C4. |
| **C5** — Petals credited 0 kB/tok | **MADE WORSE**; "the single most damaging item"; "the item I would put first in an editor's letter" | Independently the top or near-top item for R1 (NEW-1), R2 (NEW-5, must-fix #2), R3 (NEW-2, required #2). Four reviewers. | **UPHOLD as MUST-FIX #1 (K2).** The revision corrected the table and the figure and left the sentence that reads them asserting the opposite, in the same subsection. One sentence. |
| **C6** — Eq. (mem) reserves weights only while the abstract promises the *state* has a device to run on | NOT ADDRESSED **and not acknowledged** | R2-C-3 independently asks for the same two-sentence fix (say so in §III-E, soften the intro's "in one system"). | **UPHOLD as MUST-FIX (K10).** Two reviewers, a five-word edit, no testbed, and the only DA CRITICAL the letter does not mention at all. |
| **C7** — feasibility beats one strawman; offline; unlabelled in the abstract | **Downgraded to MAJOR by DA**; the headroom baseline is "the best-executed part of the revision" | R1 accepts the headroom baseline "in place of the top-m variant I suggested"; R0-W5 and R3-MAJOR-7 credit it; R2-M-7 still wants a *retrying* sequential baseline. | **ACCEPT the downgrade to MAJOR.** One residual promoted to MUST-FIX because four reviewers raise it: the six-worker pool note (K5). The "offline" label in the abstract and the retrying baseline are SHOULD-FIX. |
| **C8** — a single AGX on this fleet can host the whole model | PARTIAL; disclosure without evaluation; DA notes the testbed defence does not fully cover it | R3-NEW-6 independently: §I's capacity premise now sits beside §IV-A's "either AGX Orin could" with no reconciliation. | **ACCEPT the downgrade to MAJOR.** The concession is volunteered and honest; SHOULD-FIX is one sentence reconciling §I's capacity premise with the throughput rationale (R3-NEW-6), and optionally an offline single-AGX throughput estimate from the saved profiles. |

**Summary:** of eight DA CRITICALs, **1 resolved/withdrawn (C4)**, **3 upheld as blocking (C5, C6, C3-in-reduced-form)**, **2 downgraded to MAJOR by DA and accepted (C7, C8)**, **2 downgraded by this panel (C1, C2)** with their live residuals re-docketed as MUST-FIX items K1/K5/K8/K17. DA's joint CRITICAL "C1+C2+C3+C7 via the Conclusion" is upheld in full as **K1**.

---

## 6. Items that require the testbed — limitation-statement audit

Judged only on whether the limitation is stated adequately.

| Item (from the Round-1 decision) | Status | Evidence / missing sentence |
|---|---|---|
| Kill/OOM and random-timing injection | ✅ **Adequately stated** | `discussion.tex` L72–77, quoted approvingly by DA-C3 and R1-M7: "…process kills, out-of-memory terminations, failures before the mirrored input lands (contract check (iv)) … were not measured, so the reported latencies characterize the parity path rather than the average over all failure timings." |
| Detection-inclusive latency | ⚠️ **Scoped, but the consequence is missing** | `evaluation.tex` L70–73 scopes it. **Missing sentence:** "With asynchronous forwarding, TTR = recovery + up to the 5 s heartbeat timeout; the figures reported here are the recovery term." (R3-NEW-1: detection dominates operator TTR by ~4×.) |
| Lossy / intermittent links | ✅ **Adequately stated, with the induced failure mode named** | `discussion.tex` L63–67 "…lossy or intermittent links, **under which a missed KV column fails the contract into replay**". R0, R2 and R3 all call this more than a ritual limitation. |
| Post-recovery re-protection | ✅ **Stated** | `discussion.tex` L88–89. Residual is a *correctness* question, not a measurement (S17). |
| CUDA-vs-CUDA fidelity probe | ⚠️ **Scoped, two free additions missing** | `evaluation.tex` L262 "The probe covers one tier pair and one stage." **Missing:** (a) that the probed CUDA/CPU pair **does not occur in the 7B fleet** (R3-MAJOR-9.2); (b) that no measured trial's decoded output ever diverged (R1-M8, R2-M-5, DA-M6). |
| Reconfigure n≥3 at 7B | ⚠️ **Reviewers disagree — resolve by writing it explicitly** | R2-NEW-8 lists "Reconfigure n=2 (L67–68)" as properly stated; R1-C3(a) says "**n is still never stated**" and resolved n=2 from the authors' log (one trial per position). **Missing sentence:** "Reconfigure was measured twice, one trial at each of P=4 and P=8." |
| Longer-P sweeps / prompt length | ❌ **Not stated** | **Missing sentence:** "Prompt length was not swept; all requests use a 19-token prompt." (R2-M-10, R3-MINOR-9 residual.) |
| Concurrency sweeps | ❌ **Not stated — and newly claimed as a winning regime** | `discussion.tex` L10–11 asserts the parity case "holds when that state matters: … many concurrent requests". **Missing sentence:** "Every measurement is single-request; concurrency was not varied." (R1-M16, R2-NEW-8, R3-MINOR-6, DA-M12.) |
| Energy | ❌ **Not stated anywhere** | Zero occurrences in `sections/*.tex`. **Missing sentence:** "We did not characterise the per-token energy cost of protection." (R0-N5, R2-NEW-8, R3-MINOR-3.) |
| Head/tail victim sweep | ✅ **Stated in Future Work** | "a head/middle/tail victim sweep would test the generality of the flat-in-P result" (DA-M3, R2-M-2, R3-C4). DA: "Accept as a stated limitation." |
| Post-recovery service quality (backup now carrying two stages) | ❌ **Not stated** | R1-M12: the re-protection sentence answers a different question. **Missing sentence:** "Recovery latency ends at the first valid token and excludes post-recovery steady-state degradation; a promoted backup carrying two stages decodes more slowly than the pre-failure 500 ms, which flatters every backup-based family against Reconfigure." |
| k=1/k=2 victim confound | ❌ **Not stated** | R1-M13: the added qualifier constrains *backup placement*, not victim identity (k=2 fails `on-2` **and** `ao-2`, a 7-layer AGX stage; k=1 fails `on-2` alone). **Missing sentence:** naming the confound. |
| ρ (serving-latency) cost of joint placement | ❌ **Not stated as a gap; and R1 argues it needed no testbed** | `evaluation.tex` L318–319 says feasibility results "say nothing about serving latency" but does not concede this is an unmeasured cost of the recommended procedure. R1-M9: "the cheapest remaining experiment in the paper and it was skipped" — the profiles were in hand for two other offline reanalyses. |

**Verdict on §6:** 5 of 13 adequately stated, 3 scoped-but-incomplete, 5 not stated. **Every one of the eight gaps closes with a single declarative sentence and no measurement.** Three of them (energy, concurrency, prompt length) are the ones the response letter already claims are there.

---

## 7. Decision and Revision Roadmap R2

### Decision: **MAJOR REVISION** — one round, no new experiments required; editorial verification of the Conclusion and §IV-B against the tables before any further review.

**Rationale.** Three reviewers moved to Minor and two held at Major, but the two Minors are conditional and their conditions are unmet: R0 states "if items 1 and 2 are not fixed, this returns to Major Revision" — item 1 is the unrevised conclusion, item 2 is the 163/183 ms conflict and the stale figures, and both are still open; R2 makes its Minor conditional on "editorial verification of propagation", which this panel performed and which failed. The unconditional Minor is R1's alone.

Three things push past polish. First, the manuscript is now **internally self-contradictory in load-bearing places**: the sentence that reads Fig. 2 asserts the opposite of the table above it (four reviewers), the conclusion asserts six claims the abstract retracts (five reviewers), and one quantity carries two values ten lines apart (five reviewers). R0 puts it precisely: before the revision, abstract and conclusion over-claimed *together*; now they disagree, "which is a worse failure mode for a reader." Second, the **response letter contains four verified false or unsupported statements** (§4, L1–L4), two of which describe the exact repairs the panel was asked to verify. An editor cannot resolve that by trusting a second letter. Third, **four testbed-free items received no response at all** — R3-MAJOR-4, -8, -11 and R0-W8 are not mentioned in the letter, in any section, or in the not-addressed list; R3-MAJOR-8 (a fault-tolerance paper that lists network partition as fail-stop and describes no fencing) is a design-model gap, not a measurement gap.

Against that: nothing on this list requires the closed testbed. The intellectual work of the revision was done well and, repeatedly, against the authors' own interest — Petals charged 40 kB/tok, the P≈10 crossover published, 3.7× marked down to 2.7×, "either AGX Orin could" volunteered, a real second sequential baseline built and reported, a pool error in the authors' own sweep found and corrected. R1 and DA each recomputed every derived number in the paper and found no arithmetic error. This is a paper converging, not a paper to reject. Had the conclusion been propagated as the letter says it was, this decision would have been Minor Revision.

**Significance ceiling (recorded, not blocking):** R0 holds significance at 5 and R2 holds novelty at 5 — the benefit over replication is projected, never demonstrated in a configuration replication could not serve, and the coupling experiment that would settle it can no longer be run. That caps this at a solid, honest, useful contribution, which R0 correctly calls a publishable outcome for IoT-J rather than a barrier.

---

### Revision Roadmap R2

Numbered, deduplicated, apply directly. Every item is text, arithmetic, or a figure regeneration — **none requires the testbed.**

#### MUST-FIX (blocks acceptance)

1. **`sections/discussion.tex` L115–132 (Conclusion).** Paste the abstract's six qualifiers: "over the measured positions"; "coordinator state"; the ±11% bound in place of "no measurable"; "when their backups sit on distinct nodes"; "with backups confined to pipeline devices"; "protected-stage failure". *(K1 — all five reviewers)*
2. **`sections/evaluation.tex` L193–196 + `figures/paper/make_recovery_pareto.py` docstring.** Replace with the three-point frontier at P=32 — DejaVu (0.77 s, 416 kB), KV-CARE (1.42 s, 112 kB), Petals (2.31 s, 40 kB); delete "retain nothing", "matches", and "alone". *(K2 — R1, R2, R3, DA; DA's first item)*
3. **`sections/evaluation.tex` L36–38 / L48 / L344.** Resolve 163 vs 183 ms at 350M — one value, or two distinctly named quantities. *(K3 — all five)*
4. **`sections/abstract.tex`.** "the recovered state always has a device to run on" → "the failed stage's **weights** always have a device to load on"; add one sentence to `design.tex` §III-E that Eq. (mem) charges weights only; soften `introduction.tex` L52–54 "in one system". *(K10 — DA-C6 upheld, R2-C-3)*
5. **`sections/abstract.tex`, closing sentence.** Add the contract qualifier: "…becomes a delay of a few decode steps **when the recovery contract holds**." *(K11 — DA-C3 upheld, R3, R1)*
6. **`sections/abstract.tex` + `discussion.tex`.** Carry "3.7× overall (2.7× over the stages the parity path recovers)" wherever 3.7× is stated. *(K4 — R0, R2, R3, DA)*
7. **`sections/evaluation.tex` §IV-E / Table III.** One clause: the sweep uses all six CUDA workers; an earlier five-worker pool produced the narrower 6.5 GB band. *(K5 — R1, R2, R3, DA)*
8. **Regenerate `fig_recovery_latency.pdf`, `fig_recovery_pareto.pdf`, `fig_protection_cost.pdf`.** "mean" not "median" for the n=2 Reconfigure; label the k=1 series and give the two KV-CARE series distinct colours; drop/footnote the "at P=32" x-axis where Reconfigure is plotted at P=4,8; extend `YLIM` so the −11.36% k=1 whisker is not silently clipped. *(K6 — R0, R1, R3)*
9. **`sections/evaluation.tex` L158–160.** "four times the bytes" is dimensionally wrong (5.5× by geometry; ~344 kB cannot cost 280 ms; the intercept extrapolates to N=0). Say **round-trips**, or mark it an interpretation. *(K7 — R1-NEW-3, DA-N3)*
10. **`sections/discussion.tex` Limitations — three sentences** the letter already claims are there: per-token energy not measured; every measurement is single-request with concurrency unvaried; prompt length not swept. *(K9, §6 — R0, R1, R2, R3, DA)*
11. **`sections/discussion.tex` Limitations — two more sentences:** recovery latency excludes post-recovery degradation (a promoted backup carrying two stages decodes more slowly); the k=1/k=2 comparison varies victim identity and backup tier together. *(§6 — R1-M12, R1-M13)*
12. **`sections/evaluation.tex` Table I + §IV-A.** State Reconfigure's n (two trials, one per position) and **fix the inverted pairing**: P=4→465.8 s, P=8→413.1 s per `b1_ft_fleet_7b_reactive_log_20260901.json`. *(K5/S8 — R1-C3, R1-NEW-5; resolves the R1/R2 disagreement in §6)*
13. **`sections/evaluation.tex` L86–87 / L32–33 vs `design.tex` L9–13.** Reconcile "a second copy of **every** stage, 12.4 GiB" with R defined over protected non-head stages (~10.1 GiB). *(K17 — R1-NEW-7, DA-N6)*
14. **`sections/evaluation.tex` Table I + §IV-B.** One sentence each on the −6.0±3.0 ms/pos DejaVu slope (unmodelled systematic; state trial ordering and Jetson power mode) and on k=2's intercept being 106±100 below k=1's, i.e. within noise. *(K8 — R1, R2, R3, DA)*
15. **`docs/paper/response_to_reviewers_*.md`.** Correct L1–L4 in §4 above before resubmission; the next letter will be checked clause by clause against the files. *(§4 — all five)*

#### SHOULD-FIX

16. **`design.tex` §III-B/§III-E.** Two sentences on fencing: either describe a lease/epoch, or state that a returning partitioned worker is out of scope and that R does not model failure domains. *(S1 — R3 treats this as blocking; it is single-reviewer, hence not MUST)*
17. **`evaluation.tex` §IV-A + `design.tex`.** Coordinator memory capacity, the per-request sizing formula k·ℓ_max·N_ctx, and one paragraph of availability arithmetic for the unprotected coordinator. *(S2 — R3-MAJOR-1)*
18. **`design.tex` §III-E + `evaluation.tex` §IV-E.** Iteration bound as a number, measured solve time for the 1,950-subset enumeration, and growth in \|D\| (~9.8M at 10 devices). *(K12 — R0, R2, R3, DA)*
19. **`evaluation.tex` Table I / Table II / §IV-D.** Annotate the fidelity column "(by construction)"; add relative error beside the 2⁻⁸ absolute; state that no measured trial's decoded output diverged and that the probed CUDA/CPU pair does not occur in the 7B fleet. *(K13 — R0, R1, R2, R3, DA)*
20. **`evaluation.tex` §IV-A + `design.tex` §III-F.** Environment paragraph: JetPack/L4T, PyTorch/CUDA/gRPC versions, `nvpmodel`/`jetson_clocks` power mode, LAN rate, prompt text, artifact availability. *(K15 — R1, R3)*
21. **`evaluation.tex` §IV-A.** Exclusion accounting: trials attempted vs reported per cell, per-gate discards, parity-path fallback count; restore or replace the deleted "unless stated" hedge. *(K16 — R1-M11, DA-M7)*
22. **`related.tex`.** Cite PagedAttention where it is named (L40); name the RAID-5/RAID-6 lineage Eqs. (1)–(2) instantiate; correct "Parity Models … a prediction-serving **replica**" → an unavailable **prediction**, reconstructed **approximately**, and add the resulting novelty clause (stored bytes admit an *exact* linear code); correct "bytes a repair must read" → repair **bandwidth**; complete the four new entries' DOI/page fields. *(K22 + R2-NEW-1/NEW-2 — R2, R3, DA)*
23. **`evaluation.tex` §IV-B / `discussion.tex`.** One sentence: with asynchronous forwarding TTR = recovery + up to 5 s detection; the reported figures are the recovery term. *(K18 — R3-NEW-1, R1-M7, DA-C3)*
24. **`evaluation.tex` §IV-F / `discussion.tex`.** One concession sentence: parity and replication ship identical bytes by construction, so the comparison is purely coordinator memory. Add the derived bytes-moved-per-recovery figures from the §IV-A geometry. *(K23 — R2-M-1, R3-C1, DA-M10; see conflict C5 below)*
25. **`evaluation.tex` §IV-A / `related.tex`.** State that the DejaVu baseline centralizes replicas at the coordinator where the published design distributes them (~83 kB/tok per node), and whether KV shipping blocks the forward path. *(K19 — R1-M15, R2-M-9)*
26. **`evaluation.tex` §IV-E / Table III.** Write the cap rule as M(d)←min(free(d),c), state the 0.2 GB sweep grid, and define the 7B "whole fleet" membership. *(S16 — R1-M10, R0-W5)*
27. **`introduction.tex` ¶1 + `evaluation.tex` §IV-A.** Reconcile the capacity premise with "either AGX Orin could": at 7B on this fleet the pipeline is a throughput choice; say where capacity genuinely binds. *(DA-C8, R3-NEW-6)*
28. **Units.** Declare KiB/MiB or convert throughout §IV-C, Table I's header and the new 40 kB Petals figure. *(K14 — R0, R1, R2, R3)*
29. **`evaluation.tex` §IV-G.** Delete "with pipeline depth" (S5, DA-M1); restore the deleted "(24.25 to 439.4 s)" and "(1.6–1.9 to 2.1–3.2)" numbers and normalize by each deployment's token interval (1.01 vs 0.89 steps/pos). *(S10 — R1-M17, the one MADE-WORSE item)*
30. **`design.tex` §III-D.** One sentence on whether a promoted backup inherits rank(s) and what happens to existing Q entries. *(S17 — R2-M-8)*
31. **`evaluation.tex` §IV-C / `discussion.tex`.** Recompute the footprint for a comparable GQA model; name fp16-MHA-OPT as a generality threat; say whether the Table III band is expected to survive INT4. *(S3 — R3-MAJOR-4, arithmetic only)*

#### OPTIONAL

32. `design.tex` — delete the blended DP mode (α undefined, never valued, never used), or define α. *(S6 — R0-W7)*
33. `design.tex` — rename the device set (𝒟) and the memory cap so D and c stop colliding with D_s[r,q] and c(i,j,d). *(S7 — R0-W7/N8)*
34. `discussion.tex` — one paragraph adding an ℓ_max term to the placement objective; ψ couples to R **and** to the parity footprint. *(S4 — R3-MAJOR-11; extends the paper's own thesis)*
35. `introduction.tex` — add the roadmap sentence at the end of §I. *(S11 — R0-W16)*
36. Length: trim the abstract's first four framing sentences (270→<250 words) and compress §IV-G; target ≤9 pp. *(S12 — R0-W11/W17)*
37. `abstract.tex` — say the feasibility result is a controlled offline analysis. *(DA-C7(2), R0-W1)*
38. `main.tex` — `\input` a trimmed operating-envelope table from `background.tex`, or delete the orphan file. *(K21 — R2-m-1, R3-MINOR-5, DA-N9)*
39. `evaluation.tex` — "two to three decode steps" → "two to four", or scope it to the first-victim sweep. *(K20 — R0, R1, R2)*
40. `abstract.tex` — "within the ±11% round-to-round spread of this measurement" so the referent matches the body. *(S13 — R0-N7, DA-N7)*
41. `evaluation.tex` — name the decomposed Reconfigure trial and account the ~59 s remainder. *(S9 — R1-m20, DA-M8)*
42. `discussion.tex` — restore the deleted clause "the case the recovery contract is designed for". *(S15 — R1-M7)*

---

### Reviewer conflicts arbitrated

**C1 — §IV-G "Across Scales": delete vs upgrade.** R0-W11 wants it deleted ("pure duplication", and the paper is 2 pp. over). R1-M17 wants it *strengthened* — normalize the cross-scale slopes by token interval, "a free upgrade to the paper's strongest scaling argument", and grades the revision's deletion of its numbers as MADE WORSE. **Resolution: compress, don't delete.** Cut §IV-G to 4–5 lines carrying (a) the restored numbers, (b) R1's normalization (1.01 vs 0.89 steps/pos), and (c) DA-M1's three-word deletion. Recover R0's page from the abstract (item 36) and from the §IV-B/§IV-C prose the other fixes tighten. Both reviewers' substance is preserved; only the restatement goes.

**C2 — `fig_storage_tolerance`: correctly removed vs must be restored.** R0-W11 credits its removal as the revision's one real page saving. R3-MAJOR-7 calls the reliability-vs-k curve "my single highest-value addition, requiring no testbed" and objects that the operator now sees neither the cost nor the benefit of k. **Resolution: do not restore the figure.** Add two sentences plus one Table I-adjacent row giving the benefit of k (tolerated simultaneous failures against retained bytes: 416 / 112 / 224 kB/tok for replication, k=1, k=2). Page pressure is real and the figure was the cost half only; R3's substance fits in prose.

**C3 — Reconfigure n: stated vs never stated.** R2-NEW-8 lists "Reconfigure n=2" among the properly stated limitations; R1-C3(a) says n is never stated and resolved it from the authors' log. **Resolution: write it explicitly** in Table I's note and §IV-A ("two trials, one at each of P=4 and P=8"), which satisfies both readings and is item 12.

**C4 — Petals' 40 kB/tok charge: conservative-toward-the-authors vs the number that breaks their domination claim.** R0-W2 calls charging Petals for all five stages' inputs "the conservative-toward-the-authors reading and defensible"; DA-C5 uses the same 40 kB to show Petals holds 2.8× *less* state than KV-CARE. **No conflict in the fix** — both accept the number; the disputed object is the "alone in the corner" sentence, which item 2 rewrites.

**C5 — network: out of scope vs quantify it.** R0-W3 finds §IV-F's "network traffic is not an evaluation axis" now *consistent* with the repaired motivation; R2-M-1, R3-C1 and DA-M10 want bytes-moved figures and a wire-cost concession. **Resolution: keep network out of scope as an evaluation axis** (no new measurement exists or can exist) and satisfy the other three with item 24 — a derived arithmetic figure from the placement geometry plus the one-line concession that parity and replication ship identical bytes. DA notes this omission actively obscures the protection-cost inversion it would explain (DA-N4), which is the strongest argument for adding the sentence.

**C6 — DA-C2 status: R0 says ADDRESSED, three others say PARTIALLY.** The disagreement is entirely about whether the conclusion counts against the item. **Resolution:** grade the abstract/body repair ADDRESSED (R0 is right that it exceeded what was asked) and dock the conclusion once, under item 1, rather than three times across C1/C2/C4/C7.

---

*Prepared by the Editorial Synthesizer. Every item above traces to a named reviewer report and item id. The manuscript was not read or modified in producing this decision.*
