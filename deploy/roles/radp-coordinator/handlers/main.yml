---
- name: Restart radp-coordinator
  ansible.builtin.systemd:
    name: radp-coordinator
    state: restarted
    daemon_reload: true
  become: true
  listen:
    - restart radp-coordinator
    # Cross-role rebroadcast (see radp-worker handler for the full note).
    - restart radp service
